---
phase: 13-aeropanelgroup
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt
autonomous: true
requirements: [PNL-05, PNL-06, PNL-08, PNL-09, PNL-10, PNL-18]
must_haves:
  truths:
    - "Dragging a divider between two adjacent expanded sections resizes both instantly with no animation lag"
    - "Drag does not snap back after a window resize mid-drag (rememberUpdatedState(totalPx) live read)"
    - "N-section clamp accounts for the sum of minSizes below the divider (no IllegalArgumentException)"
    - "Expansion is hybrid controlled/uncontrolled: onExpandedChange == null is uncontrolled, non-null is controlled pure renderer"
    - "onLayoutChange fires only on drag-end and on collapse/expand toggle, never per drag frame"
    - "KDoc references REQ-IDs and PITFALLs and forbids collapsing the two expansion branches into one"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt"
      provides: "Drag resize via aeroDragSplitter + clampPanelDividerPx, hybrid controlled/uncontrolled expansion, onLayoutChange firing contract, KDoc"
      contains: "rememberUpdatedState"
  key_links:
    - from: "AeroPanelGroup PanelGroupDivider aeroDragSplitter onDrag"
      to: "clampPanelDividerPx + direct sizePx write"
      via: "live-state read inside captured-once lambda"
      pattern: "aeroDragSplitter"
    - from: "AeroPanelGroup expansion derivation"
      to: "controlled (expandedKeys) vs uncontrolled (internal mutableState)"
      via: "val controlled = onExpandedChange != null"
      pattern: "onExpandedChange != null"
---

<objective>
Build-order steps 5 + 6: drag resize between adjacent expanded sections (aeroDragSplitter + clampPanelDividerPx N-section clamp, rememberUpdatedState(totalPx), onLayoutChange on drag-end), then the controlled expansion path (hybrid controlled/uncontrolled mirroring AeroAccordion) plus KDoc with REQ-ID/PITFALL references.

Purpose: Completes the interactive behavior. Drag must write px directly with NO animation wrapper and NO snap-back after window resize (PNL-05, FIXSP-01 carry-forward, PITFALL-A). The N-section clamp (PNL-10, PNL-PITFALL-04) uses the GREEN clampPanelDividerPx from 13-02. Grip divider only between two adjacent EXPANDED sections via zipWithNext filter, never expandedCount-1 (PNL-06). Hybrid expansion (PNL-08) must keep BOTH branches intentional — the KDoc forbids collapsing them. onLayoutChange firing contract (PNL-09): drag-end + toggle only. KDoc (PNL-18) matches neighboring layout components.
Output: AeroPanelGroup.kt with drag + controlled path + KDoc; visual polish (glassPanel/caret/grip dots) still deferred to 13-05.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-aeropanelgroup/13-RESEARCH.md
@.planning/phases/13-aeropanelgroup/13-CONTEXT.md
@.planning/phases/13-aeropanelgroup/13-01-SUMMARY.md
@.planning/phases/13-aeropanelgroup/13-02-SUMMARY.md
@.planning/phases/13-aeropanelgroup/13-03-SUMMARY.md

<interfaces>
aeroDragSplitter (reuse verbatim, AeroDragSplitter.kt): internal fun Modifier.aeroDragSplitter(orientation: Orientation, onDrag: (deltaPx: Float) -> Unit, onDragEnd: () -> Unit = {}, enabled: Boolean = true): Modifier. Keys pointerInput on (orientation, enabled) only — onDrag captured once; read live state.

clampPanelDividerPx (from 13-02 PanelDistribution.kt): internal fun clampPanelDividerPx(aboveSizePx, deltaPx, minAbovePx, minBelowPx, totalBudgetPx): Float. Inverted-range guard (coerceAtLeast) inside.

AeroSplitPane live-state drag (AeroSplitPane.kt 115-124): val liveTotalPx by rememberUpdatedState(totalPx); onDrag reads MutableState live, never a captured copy.

AeroAccordion hybrid (AeroAccordion.kt 117-164): val controlled = onExpandedChange != null; controlled = pure renderer over caller state, uncontrolled = internal mutableState.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drag resize — PanelGroupDivider + aeroDragSplitter + clampPanelDividerPx + onLayoutChange</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSplitPane.kt (lines 115-124 live-state drag; lines 179-232 SplitPaneDivider structure with 8dp hit-area + aeroDragSplitter + hover tint)
    - library/src/main/kotlin/com/mordred/aero/components/internal/drag/AeroDragSplitter.kt (signature; Orientation.Vertical; enabled param for resizable=false)
    - library/src/main/kotlin/com/mordred/aero/components/layout/internal/panelgroup/PanelDistribution.kt (clampPanelDividerPx + activeDividerCount)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt (current state from 13-03)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Pattern 2 live-state drag lines 193-227; Pattern 6 divider structure; PNL-PITFALL-03 mid-drag race; PNL-PITFALL-07 zipWithNext)
  </read_first>
  <action>
    Add a private `@Composable fun PanelGroupDivider(onDrag: (Float) -> Unit, onDragEnd: () -> Unit, enabled: Boolean)` mirroring SplitPaneDivider (vertical orientation): 8dp-tall hit-area `Box` + `hoverable` + `Modifier.aeroDragSplitter(orientation = Orientation.Vertical, onDrag = onDrag, onDragEnd = onDragEnd, enabled = enabled)` + `background(if (hovered) colors.buttonHover else Color.Transparent)` + centered 1dp horizontal line + (grip-dots row added in 13-05; for now the 1dp line is enough but keep the structure). When `enabled == false` (resizable=false) the hit-area renders the static line with no aeroDragSplitter drag effect.

    In the render loop, replace the static 1dp divider Box (between two adjacent expanded sections) with `PanelGroupDivider(...)`. Render condition is unchanged (PNL-PITFALL-09): `expandedState[i]` and `i < sections.lastIndex` and `expandedState[i + 1]`. Set `enabled = sections[i].resizable && sections[i + 1].resizable` (PNL-12 — resizable=false disables drag; full grip stripping is visual in 13-05).

    Drag lambda (live-state, FIXSP-01 / PITFALL-A): `val liveTotalPx by rememberUpdatedState(totalPx)`. `val onDragBetween: (Int, Int, Float) -> Unit = { above, below, delta -> ... }`. Inside, read `sizePx[above]` / `sizePx[below]` DIRECTLY (SnapshotStateList reads are always live — no captured plain-val copy). Compute the N-section budget: `minAbovePx = with(density){ sections[above].minSize.toPx() }` (the direct upper section's minimum), `minBelowPx = sum over all EXPANDED sections from below..lastIndex of their minSize.toPx()` (Σminima below the divider — PNL-10/PNL-PITFALL-04), `totalBudgetPx = sizePx[above] + sizePx[below]`. Call `val newAbove = clampPanelDividerPx(sizePx[above], delta, minAbovePx, minBelowPx, totalBudgetPx)`; `sizePx[above] = newAbove`; `sizePx[below] = totalBudgetPx - newAbove`. Direct writes, NO animation wrapper around the drag write.

    onLayoutChange firing contract (PNL-09): do NOT call onLayoutChange inside onDragBetween (per-frame). Pass `onDragEnd = { onLayoutChange?.invoke(sizePx.toList()) }` to the divider, AND call `onLayoutChange?.invoke(sizePx.toList())` inside the collapse/expand toggle handler. NOT on every drag frame.

    PNL-PITFALL-03 mid-drag collapse race: add a `var dragging by remember { mutableStateOf(false) }` flag; set true on first onDrag, false on onDragEnd; guard the toggle handler with `if (!dragging)`. (Dividers adjacent to a section already disappear the frame it collapses, so this guard plus the SnapshotStateList live read fully covers the race.)
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:compileKotlin -q 2>&1 | tail -6 && echo "splitter=$(grep -c 'aeroDragSplitter' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) live=$(grep -c 'rememberUpdatedState' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) clamp=$(grep -c 'clampPanelDividerPx' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `:library:compileKotlin` exits 0.
    - File contains `aeroDragSplitter`, `Orientation.Vertical`, `rememberUpdatedState`, `clampPanelDividerPx`.
    - onDrag lambda reads `sizePx[above]`/`sizePx[below]` directly; no captured plain-val of a section size before the lambda (FIXSP-01 negative criterion).
    - `onLayoutChange` is invoked from `onDragEnd` and from the toggle handler, NOT inside the per-frame onDrag lambda (grep: onLayoutChange does not appear inside onDragBetween body).
    - Divider `enabled` is `sections[i].resizable && sections[i + 1].resizable` (PNL-12).
    - `grep -c "remember(totalPx)"` returns 0; `grep -c "detectDragGestures"` returns 0.
  </acceptance_criteria>
  <done>Drag resizes adjacent expanded sections instantly via clampPanelDividerPx with N-section Σminima clamp; onLayoutChange fires drag-end + toggle only; live-state read prevents snap-back.</done>
</task>

<task type="auto">
  <name>Task 2: Add hybrid controlled/uncontrolled expansion path + KDoc</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroAccordion.kt (lines 100-186 hybrid: KDoc on params, val controlled = onExpandedChange != null, controlled vs internal derivation, onToggle dispatch)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSplitPane.kt (lines 44-75 KDoc style: REQ-ID + PITFALL references, ownership-model section)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt (current state)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Pattern 4 hybrid lines 268-314; "Do not collapse to one branch" mandate)
  </read_first>
  <action>
    Add controlled params to `AeroPanelGroup`: `expandedKeys: Set<String>? = null, onExpandedChange: ((Set<String>) -> Unit)? = null` (place after initiallyExpanded, before onLayoutChange). Implement `val controlled = onExpandedChange != null`. Keep the uncontrolled internal state from 13-03 (`internalExpanded: Set<String>` or the expandedState list) seeded from initiallyExpanded.

    `fun isExpanded(key: String): Boolean = if (controlled) expandedKeys?.contains(key) == true else key in internalExpanded`. `fun onToggle(key: String) { if (controlled) { val current = expandedKeys ?: emptySet(); val next = if (key in current) current - key else current + key; onExpandedChange!!(next) } else { internalExpanded = if (key in internalExpanded) internalExpanded - key else internalExpanded + key } }`. Drive the render loop's expanded check through `isExpanded(section.key)`. Both branches MUST remain present and intentional — do NOT collapse into one. Size-state (sizePx, lastExpandedFraction) stays uncontrolled in BOTH expansion modes; exposed only via onLayoutChange.

    KDoc (PNL-18) on `public fun AeroPanelGroup` matching AeroSplitPane/AeroAccordion style:
    - One-line summary citing PNL-01 (scope-DSL N vertical sections).
    - Ownership/expansion-model section: onExpandedChange == null → uncontrolled (PNL-08); non-null → controlled pure renderer. Include the EXACT comment: "Do not collapse to one branch — both paths are intentional (matches AeroAccordion hybrid-ownership convention) (PNL-08)."
    - Sizing section: fraction-as-stable-coordinate, derived from totalPx each recompose, survives window resize (PNL-04, PITFALL-A). lastExpandedFraction restore (PNL-03, PNL-PITFALL-06).
    - Divider section: grip only between two adjacent expanded sections (PNL-06, zipWithNext); N-section clamp (PNL-10, PNL-PITFALL-04). onLayoutChange fires drag-end + toggle only (PNL-09).
    - @param KDoc for modifier, initiallyExpanded, expandedKeys, onExpandedChange, onLayoutChange, content. Document section(...) params (minSize, collapsible PNL-11, resizable PNL-12, defaultExpanded, defaultSize, leadingIcon, headerActions PNL-14) on the scope.section function.
    - KDoc must NOT contain forbidden grep-gate tokens used by other package checks (e.g. avoid AeroScrollArea / stickyHeader / detectDragGestures — paraphrase per [Phase 09-data] decision).
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:compileKotlin -q 2>&1 | tail -6 && ./gradlew :library:test --tests "*.PanelGroupLogicTest" 2>&1 | tail -4 && echo "ctrl=$(grep -c 'onExpandedChange != null' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) branch=$(grep -c 'Do not collapse to one branch' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) detect=$(grep -c 'detectDragGestures' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `:library:compileKotlin` exits 0; `./gradlew :library:test --tests "*.PanelGroupLogicTest"` stays GREEN.
    - File contains `val controlled = onExpandedChange != null` and both controlled and uncontrolled branches in `onToggle`/`isExpanded`.
    - KDoc contains literal "Do not collapse to one branch" and references PNL-08, PNL-04, PNL-06, PNL-10, PNL-09 (grep each).
    - `grep -c "detectDragGestures"` returns 0 (token absent even in KDoc — Phase 09-data hygiene).
    - `expandedKeys` and `onExpandedChange` params present in the public signature.
  </acceptance_criteria>
  <done>Hybrid controlled/uncontrolled expansion present with both branches intentional; KDoc cites REQ-IDs + PITFALLs and forbids branch-collapse; pure-logic suite still GREEN.</done>
</task>

</tasks>

<verification>
- `:library:compileKotlin` green; `./gradlew :library:test --tests "*.PanelGroupLogicTest"` green.
- Drag tokens present (aeroDragSplitter, rememberUpdatedState, clampPanelDividerPx); controlled hybrid present; KDoc complete.
- Banned tokens absent (remember(totalPx), detectDragGestures, animateContentSize).
</verification>

<success_criteria>
- PNL-05 drag resize, PNL-06 grip only between expanded pairs, PNL-08 hybrid expansion, PNL-09 onLayoutChange contract, PNL-10 N-section clamp, PNL-18 KDoc all satisfied at code level.
- Visual polish (13-05) is the only remaining work.
</success_criteria>

<output>
After completion, create `.planning/phases/13-aeropanelgroup/13-04-SUMMARY.md`. Record final public AeroPanelGroup signature (all params), the PanelGroupDivider signature, the minBelowPx Σminima computation approach, and confirm the onLayoutChange firing sites.
</output>
