---
phase: 13-aeropanelgroup
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt
  - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt
autonomous: false
requirements: [PNL-05, PNL-11, PNL-12, PNL-14, PNL-17]
must_haves:
  truths:
    - "Header strip is a glassPanel(cornerRadius = 8.dp) Win7 Aero surface with CaretRight rotating 0 to 90 degrees on expand"
    - "leadingIcon and headerActions slots render; headerActions are clickable without bubbling to the header toggle and are visible in both collapsed and expanded states"
    - "PanelGroupDivider shows 3 grip dots + 1dp line + 8dp hit-area"
    - "collapsible = false hides the chevron and disables collapse; resizable = false strips the grip and disables drag"
    - "Showcase LayoutSection has an AeroPanelGroup demo; throwaway spike section + wiring are removed"
    - "Three-theme visual sign-off passes on AeroBlue, AeroDark, Classic"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt"
      provides: "AeroPanelGroup demo block (append-only) with leadingIcon + headerActions + collapsible/resizable examples"
      contains: "AeroPanelGroup"
  key_links:
    - from: "AeroPanelGroup header Row"
      to: "glassPanel(cornerRadius = 8.dp) + AeroIcons.CaretRight graphicsLayer rotationZ"
      via: "Aero gloss surface + animated caret"
      pattern: "glassPanel"
    - from: "LayoutSection AeroPanelGroup demo"
      to: "library AeroPanelGroup public API"
      via: "scope-DSL section(key, title){content}"
      pattern: "AeroPanelGroup"
---

<objective>
Build-order steps 7 + 8: Aero visual polish on the library component, then the showcase demo and three-theme visual sign-off. Polish includes glassPanel(8.dp) header, CaretRight 0-to-90-degree rotation, leadingIcon, headerActions (non-bubbling, visible in both states), grip dots on the divider, and the collapsible=false / resizable=false visual behaviors. Then append an AeroPanelGroup demo to LayoutSection.kt, remove the throwaway spike, and obtain three-theme visual sign-off (AeroBlue / AeroDark / Classic).

Purpose: Delivers the Win7 Aero aesthetic (PNL-14, gloss/gradient/rounded/depth per the user's locked aero-priority decision), the per-section flags (PNL-11, PNL-12), the grip visual (PNL-05), and the showcase integration with manual sign-off (PNL-17). This is the final plan; it closes the phase. Honors the user MEMORY note: visual choices lean Win7 Aero, not modern-flat.
Output: Polished AeroPanelGroup; showcase demo; spike removed; three-theme sign-off PASS.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-aeropanelgroup/13-RESEARCH.md
@.planning/phases/13-aeropanelgroup/13-CONTEXT.md
@.planning/phases/13-aeropanelgroup/13-01-SUMMARY.md
@.planning/phases/13-aeropanelgroup/13-04-SUMMARY.md

<interfaces>
glassPanel (GlassModifiers.kt 54): @Composable public fun Modifier.glassPanel(cornerRadius: Dp = 0.dp): Modifier. Single drawBehind: panelBackground + glassHighlight gradient.
AeroIcons.CaretRight (CaretRight.kt 12): public val AeroIcons.CaretRight: ImageVector (extension property).
Caret rotation (AeroAccordion.kt 198-202, 241): animateFloatAsState(if (expanded) 90f else 0f, tween(160, FastOutSlowInEasing)) + graphicsLayer { rotationZ = caretRotation }; Icon tint explicit.
SplitPaneDivider grip (AeroSplitPane.kt 216-230, vertical orientation): Row of 3 Box(size 3.dp) dots separated by Box(width 4.dp), color labelText.
LayoutSection.kt: existing demos use AeroCard wrappers, AeroTheme.colors, section composables.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aero visual polish — glassPanel header, CaretRight rotation, leadingIcon, headerActions, grip dots, flags</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroAccordion.kt (lines 198-259 AccordionSectionRow: caretRotation, glassPanel(8.dp), clip(RoundedCornerShape(8.dp)) before clickable, AeroIcons.CaretRight + graphicsLayer rotationZ, explicit Icon tint)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSplitPane.kt (lines 216-230 vertical-orientation 3-dot grip Row)
    - library/src/main/kotlin/com/mordred/aero/theme/GlassModifiers.kt (glassPanel signature)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt (current state from 13-04)
    - .planning/phases/13-aeropanelgroup/13-CONTEXT.md (header interaction: whole Row clickable to toggle; headerActions separate non-bubbling slot, visible both states; 36dp fixed)
    - C:/Users/1/.claude/projects/c--1A-WORK-ui-lib/memory/MEMORY.md (Aero-aesthetic alignment: lean Win7 Aero gloss/gradient/rounded/depth, not modern-flat)
  </read_first>
  <action>
    Polish the header Row in `AeroPanelGroup.kt`:
    - Surface: `Modifier.fillMaxWidth().height(HEADER_HEIGHT).glassPanel(cornerRadius = 8.dp).clip(RoundedCornerShape(8.dp)).clickable { onToggle(section.key) }.padding(horizontal = 12.dp)` (clip before clickable — F-ACCORDION-HOVER precedent). The whole Row click toggles (PNL-02).
    - Chevron: only when `section.collapsible` (PNL-11 — collapsible=false hides chevron). `val caretRotation by animateFloatAsState(targetValue = if (isExpanded(section.key)) 90f else 0f, animationSpec = tween(durationMillis = 160, easing = FastOutSlowInEasing), label = "caret_" + section.key)`; `Icon(imageVector = AeroIcons.CaretRight, contentDescription = null, tint = AeroTheme.colors.onSurface, modifier = Modifier.size(18.dp).graphicsLayer { rotationZ = caretRotation })`.
    - leadingIcon: if `section.leadingIcon != null`, render `Icon(section.leadingIcon, contentDescription = null, tint = AeroTheme.colors.onSurface, ...)` (explicit tint — v1.1 rule) between caret and title.
    - Title `Text(section.title, color = AeroTheme.colors.onSurface)` with `Modifier.weight(1f)` so headerActions sit at the far right.
    - headerActions: if `section.headerActions != null`, render in a SEPARATE trailing `Row` whose clicks do NOT bubble to the header toggle. Achieve non-bubbling by NOT putting the toggle clickable on this sub-Row (the toggle clickable is on the header Row; give the actions Row its own interaction so action onClicks are handled there — wrap actions in a Box/Row that consumes its own clicks). headerActions render in BOTH collapsed and expanded states (they live in the always-present header strip — PNL-14 / VS Code model).
    - Divider grip dots (PNL-05): in `PanelGroupDivider`, add the vertical-orientation 3-dot grip `Row` (3 x Box(size 3.dp) separated by Box(width 4.dp), color `AeroTheme.colors.labelText`) centered over the 1dp line, mirroring SplitPaneDivider. When `enabled == false` (resizable=false, PNL-12) render a static 1dp line with NO grip dots and NO aeroDragSplitter.
    - Confirm collapsible=false sections still participate in resize (PNL-11 — they have height, dividers to expanded neighbors still draw) and resizable=false strips grip + disables drag (PNL-12).
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:compileKotlin -q 2>&1 | tail -6 && echo "glass=$(grep -c 'glassPanel(cornerRadius = 8.dp)' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) caret=$(grep -c 'AeroIcons.CaretRight' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) rot=$(grep -c 'rotationZ' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `:library:compileKotlin` exits 0.
    - File contains `glassPanel(cornerRadius = 8.dp)`, `AeroIcons.CaretRight`, `graphicsLayer`, `rotationZ`, `clip(RoundedCornerShape(8.dp))`.
    - Chevron render is guarded by `section.collapsible` (PNL-11).
    - Grip-dot path guarded by divider `enabled`; `enabled == false` renders no grip + no aeroDragSplitter (PNL-12).
    - leadingIcon and headerActions render with explicit tint where Icons are used (no implicit tint — v1.1 rule).
    - `grep -c "remember(totalPx)"` and `grep -c "animateContentSize"` and `grep -c "detectDragGestures"` each return 0 (anti-patterns preserved absent).
  </acceptance_criteria>
  <done>Win7 Aero header (glassPanel + caret 0-90 + leadingIcon + non-bubbling headerActions), grip dots, collapsible/resizable flags all present; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Append AeroPanelGroup demo to LayoutSection + remove throwaway spike</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt (existing demo structure, imports, AeroCard wrappers, bounded-height parent pattern — AeroPanelGroup needs a bounded parent height since it fillMaxSize within constraints)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt (public API to call)
    - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt (spike wiring added in 13-01 to remove)
    - showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt (throwaway to delete)
    - .planning/phases/13-aeropanelgroup/13-01-SUMMARY.md (note marking spike + wiring as throwaway)
  </read_first>
  <action>
    Append (append-only — do not modify existing demos) an `AeroPanelGroup` demo block to `LayoutSection.kt`. The demo MUST exercise: 3+ sections via `section(key, title) { content }`; at least one `leadingIcon`; at least one `headerActions` slot (e.g. a small icon button) demonstrating non-bubbling click; one `collapsible = false` section (PNL-11); one `resizable = false` boundary (PNL-12); a section whose content overflows so clipping is visible (PNL-15 / clip behavior). Wrap the group in a bounded-height parent (e.g. `Box(Modifier.height(360.dp))`) because AeroPanelGroup fills its constraints (same lesson as F-WIZARD bounded-parent fix). Add required imports for the new public API.

    Remove the throwaway spike: delete `showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt` and remove its registration/nav wiring from `ShowcaseApp.kt` (added in 13-01). Confirm no other section references it.
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :showcase:compileKotlin -q 2>&1 | tail -6 && echo "demo=$(grep -c 'AeroPanelGroup' showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt) spike_file=$(ls showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt 2>/dev/null | wc -l) spike_ref=$(grep -rc 'PanelGroupSpikeSection' showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `:showcase:compileKotlin` exits 0.
    - `LayoutSection.kt` contains `AeroPanelGroup` and uses `section(` with leadingIcon, headerActions, `collapsible = false`, `resizable = false`.
    - `PanelGroupSpikeSection.kt` no longer exists (ls count 0) and `ShowcaseApp.kt` has zero `PanelGroupSpikeSection` references.
    - Existing LayoutSection demos (SplitPane/Accordion/Sidebar/Wizard) unchanged (append-only).
  </acceptance_criteria>
  <done>Showcase has a full AeroPanelGroup demo; throwaway spike + wiring removed; showcase compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Three-theme visual sign-off (AeroBlue / AeroDark / Classic)</name>
  <action>Pause for human verification. The executor presents the running showcase AeroPanelGroup demo; the human performs the three-theme checklist below and signs off. Record per-theme result in the SUMMARY.</action>
  <what-built>Win7 Aero AeroPanelGroup demo in the showcase LayoutSection: glassPanel(8.dp) headers, CaretRight rotating 0-to-90 on expand, leadingIcon + headerActions slots, grip-dot dividers between expanded neighbors, collapsible=false and resizable=false sections.</what-built>
  <how-to-verify>
    1. Run the showcase: `./gradlew :showcase:run`. Open the Layout section, find the AeroPanelGroup demo.
    2. For EACH of the three themes (switch via the theme switcher): AeroBlue, AeroDark, Classic — confirm:
       a. Header strips show Aero gloss/gradient/rounded depth (glassPanel), NOT a flat modern surface (MEMORY: aero-aesthetic priority).
       b. CaretRight rotates smoothly 0-to-90 degrees on expand and back on collapse.
       c. leadingIcon and headerActions render with correct tint; clicking a headerAction does NOT toggle the section; headerActions are visible when the section is collapsed too.
       d. The divider between two expanded sections shows 3 grip dots + 1dp line and a resize cursor on hover; drag resizes both sections instantly.
       e. The collapsible=false section has NO chevron and does not collapse on click, but still resizes.
       f. The resizable=false boundary has NO grip dots and no drag.
       g. Collapse a section: neighbors absorb the freed height; re-expand restores prior size.
    3. Resize the window: section proportions hold (no reset).
  </how-to-verify>
  <resume-signal>Type "approved" if all three themes pass the seven checks (PNL-14, PNL-17 sign-off complete). Otherwise list the failing theme + check; fixes are made before the phase closes.</resume-signal>
</task>

</tasks>

<verification>
- `:library:compileKotlin` and `:showcase:compileKotlin` green; `./gradlew :library:test --tests "*.PanelGroupLogicTest"` green.
- Polish tokens present; spike removed; banned tokens absent.
- Three-theme human sign-off recorded as PASS.
</verification>

<success_criteria>
- PNL-05 grip visual, PNL-11 collapsible flag, PNL-12 resizable flag, PNL-14 Aero header, PNL-17 showcase + three-theme sign-off all satisfied.
- Phase 13 success criteria 1-5 all observable; all 18 requirements delivered.
</success_criteria>

<output>
After completion, create `.planning/phases/13-aeropanelgroup/13-05-SUMMARY.md`. Record the three-theme sign-off result (per theme), confirm spike removal, and note any visual fixes applied during sign-off (no iteration-narrative code comments — MEMORY rule).
</output>
