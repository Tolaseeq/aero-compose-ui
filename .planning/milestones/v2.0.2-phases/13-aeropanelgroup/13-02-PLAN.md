---
phase: 13-aeropanelgroup
plan: 02
type: tdd
wave: 2
depends_on: ["13-01"]
files_modified:
  - library/src/test/kotlin/com/mordred/aero/components/layout/PanelGroupLogicTest.kt
  - library/src/main/kotlin/com/mordred/aero/components/layout/internal/panelgroup/PanelDistribution.kt
autonomous: true
requirements: [PNL-02, PNL-03, PNL-04, PNL-06, PNL-10, PNL-15, PNL-16]
must_haves:
  truths:
    - "All pure-logic functions run GREEN under kotlin.test with no Compose runtime"
    - "N-section divider clamp never throws IllegalArgumentException on inverted range"
    - "Collapsing a section redistributes its px to neighbors conserving total px"
    - "Re-expanding restores lastExpandedFraction without exceeding currentAvailableForExpanded after a window shrink"
    - "activeDividerCount for pattern E/E/C/E equals 1 (zipWithNext, not expandedCount-1)"
    - "All-collapsed yields zero available height for expanded sections"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/layout/internal/panelgroup/PanelDistribution.kt"
      provides: "Pure-JVM distribution + clamp logic, no Compose imports"
      min_lines: 60
      contains: "internal fun clampPanelDividerPx"
    - path: "library/src/test/kotlin/com/mordred/aero/components/layout/PanelGroupLogicTest.kt"
      provides: "TDD pure-logic suite covering PNL-02/03/04/06/10/15 + PNL-PITFALL-02/04/05/06/07"
      contains: "import kotlin.test.Test"
  key_links:
    - from: "PanelGroupLogicTest.kt"
      to: "PanelDistribution.kt pure functions"
      via: "direct function call + assertEquals (no Compose runtime)"
      pattern: "PanelDistribution"
---

<objective>
Build-order step 2: the pure-logic foundation via TDD (RED to GREEN BEFORE any Compose code). Create `PanelDistribution.kt` (no Compose imports) and `PanelGroupLogicTest.kt`. The functions cover distribution math, N-section divider clamp, share-transfer on collapse/expand, available-px computation, active-divider count, and lastExpandedFraction restore.

Purpose: This is the SECOND hard gate. Per 13-VALIDATION.md (Nyquist Dimension 8) every named test below gates the Compose layout/drag/animation work in plans 13-03/04/05. All tests must be GREEN before the layout skeleton (plan 13-03) begins. The N-section clamp defuses PNL-PITFALL-04 (cascading clamp crash, same class as the v2.0.1 PITFALL-B IllegalArgumentException); share-transfer defuses PNL-PITFALL-05 (rounding gap); fraction restore defuses PNL-PITFALL-06 (lastExpandedPx overflow); zipWithNext count defuses PNL-PITFALL-07; float drift handled by last-section remainder (PNL-PITFALL-02).
Output: `PanelDistribution.kt` + `PanelGroupLogicTest.kt` with the full named test suite GREEN.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-aeropanelgroup/13-RESEARCH.md
@.planning/phases/13-aeropanelgroup/13-VALIDATION.md
@.planning/phases/13-aeropanelgroup/13-01-SUMMARY.md

<interfaces>
<!-- Reuse verbatim — clampPanelDividerPx wraps the SAME idea (safeMax guard). SplitClamp.kt lines 21-24. -->
internal fun clampDividerPx(currentPx: Float, deltaPx: Float, minFirstPx: Float, maxPx: Float): Float {
    val safeMax = maxPx.coerceAtLeast(minFirstPx)   // PITFALL-B guard
    return (currentPx + deltaPx).coerceIn(minFirstPx, safeMax)
}

<!-- Test template — SplitClampTest.kt / AccordionToggleTest.kt: kotlin.test.Test + pure-fn assertEquals. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write PanelGroupLogicTest.kt with all named tests failing first</name>
  <read_first>
    - library/src/test/kotlin/com/mordred/aero/components/layout/SplitClampTest.kt (exact structure: package, kotlin.test.Test, assertEquals tolerance, clampInvertedRangeDoesNotThrow template)
    - library/src/test/kotlin/com/mordred/aero/components/layout/AccordionToggleTest.kt (pure-fn call structure)
    - .planning/phases/13-aeropanelgroup/13-VALIDATION.md (Per-Task Verification Map — the exact test names that must exist)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Common Pitfalls section — PNL-PITFALL-04/05/06/07 fix formulas; Pure/Unit-Testable Logic table)
  </read_first>
  <behavior>
    The suite must contain these EXACTLY-NAMED tests (wired to 13-VALIDATION.md), each a pure-fn call + assertEquals:
    - `distributePxWindowResizePreservesRatios` (PNL-04): 3 equal expanded sections; distribute at totalPx=900 then totalPx=600; assert each render height ratio preserved (each ~1/3 of availableForExpanded) within 0.5f.
    - `shareTransferOnCollapseConservesPx` (PNL-02/03): middle section of 3 collapses; assert Σ of new expanded sizePx == sum of the two survivors' original sizePx + collapsed section's released share (conservation), last neighbor absorbs rounding remainder (PNL-PITFALL-05).
    - `clampPanelDividerPxInvertedRangeNoThrow` (PNL-10/PNL-PITFALL-04): three expanded sections each minSizePx=60, totalBudget=100 (below Σmin) — call must NOT throw IllegalArgumentException and returns a finite value (mirror SplitClampTest.clampInvertedRangeDoesNotThrow).
    - `activeDividerCountEECEGivesOne` (PNL-06/PNL-PITFALL-07): expanded=[true,true,false,true] → assert activeDividerCount == 1 (zipWithNext, NOT expandedCount-1 which would give 2).
    - `restoreAfterShrinkDoesNotExceedAvailable` (PNL-03/PNL-PITFALL-06): collapse captures lastExpandedFraction; window shrinks; expand restores `lastExpandedFraction * currentAvailableForExpanded` — assert restored px <= currentAvailableForExpanded (no overflow).
    - `allCollapsedAvailableIsZero` (PNL-15): all sections collapsed → assert availableForExpanded == 0f (or <= 0f) and every expanded-section share is 0.
    Add supporting tests for: first-collapses (right neighbor gains all), last-collapses (left neighbor gains all), single-expanded fills availableForExpanded.
  </behavior>
  <action>
    Create `library/src/test/kotlin/com/mordred/aero/components/layout/PanelGroupLogicTest.kt`, package `com.mordred.aero.components.layout`, `import kotlin.test.Test`, `import kotlin.test.assertEquals` (+ `assertTrue`). Write the named tests above calling functions that do NOT yet exist (or are stubs) so the suite is RED. Use `assertEquals(expected, actual, 0.5f)` float tolerance like SplitClampTest. Commit RED: `test(13-02): add failing PanelGroupLogicTest suite`.
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:test --tests "*.PanelGroupLogicTest" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `PanelGroupLogicTest.kt` exists, package `com.mordred.aero.components.layout`, contains `import kotlin.test.Test`.
    - All six named tests present verbatim: `distributePxWindowResizePreservesRatios`, `shareTransferOnCollapseConservesPx`, `clampPanelDividerPxInvertedRangeNoThrow`, `activeDividerCountEECEGivesOne`, `restoreAfterShrinkDoesNotExceedAvailable`, `allCollapsedAvailableIsZero` (grep each name → match).
    - Test run is RED (fails to compile or assertions fail) at this commit — gate is the GREEN in Task 2.
  </acceptance_criteria>
  <done>RED suite committed; all six 13-VALIDATION.md test names present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement PanelDistribution.kt pure functions until suite passes</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/internal/splitpane/SplitClamp.kt (safeMax guard to replicate in clampPanelDividerPx)
    - library/src/test/kotlin/com/mordred/aero/components/layout/PanelGroupLogicTest.kt (the RED suite just written — the contract)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Common Pitfalls fix formulas: clampPanelDividerPx signature lines ~499-507; computeAvailablePx zipWithNext; lastExpandedFraction restore)
  </read_first>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/layout/internal/panelgroup/PanelDistribution.kt`, package `com.mordred.aero.components.layout.internal.panelgroup`. NO Compose imports (plain JVM). Implement the following `internal fun`s with these EXACT contracts:

    - `clampPanelDividerPx(aboveSizePx: Float, deltaPx: Float, minAbovePx: Float, minBelowPx: Float, totalBudgetPx: Float): Float` — `val maxAbovePx = (totalBudgetPx - minBelowPx).coerceAtLeast(minAbovePx); return (aboveSizePx + deltaPx).coerceIn(minAbovePx, maxAbovePx)`. The `coerceAtLeast` is the PITFALL-B/PNL-PITFALL-04 inverted-range guard — REQUIRED.
    - `computeAvailablePx(totalPx: Float, expanded: BooleanArray, headerPx: Float, dividerPx: Float): Float` — `val collapsed = expanded.count { !it }; val activeDividers = expanded.toList().zipWithNext().count { (a, b) -> a && b }; return (totalPx - collapsed * headerPx - activeDividers * dividerPx).coerceAtLeast(0f)`.
    - `activeDividerCount(expanded: BooleanArray): Int = expanded.toList().zipWithNext().count { (a, b) -> a && b }` (PNL-PITFALL-07).
    - `distributePx(sizePx: FloatArray, expanded: BooleanArray, totalPx: Float, headerPx: Float, dividerPx: Float): FloatArray` — returns render heights: collapsed→headerPx; expanded→`(sizePx[i] / expandedSizeSum) * computeAvailablePx(...)`; single-expanded→full availableForExpanded; all-collapsed→headerPx for every section (and availableForExpanded==0). Use last-EXPANDED-section remainder to absorb float rounding so Σ expanded heights == availableForExpanded exactly (PNL-PITFALL-02).
    - `shareTransferOnCollapse(sizePx: FloatArray, expanded: BooleanArray, index: Int): FloatArray` — distribute the collapsing section's `sizePx[index]` across the OTHER currently-expanded sections proportionally; last remaining expanded neighbor absorbs the rounding remainder so total is conserved (PNL-PITFALL-05). Return new array.
    - `shareTransferOnExpand(sizePx: FloatArray, expanded: BooleanArray, index: Int, restorePx: Float): FloatArray` — take `restorePx` from the other expanded sections proportionally, clamped so no donor drops below a floor; assign restored share to `index`. Return new array.
    - `lastExpandedFraction(sizePx_i: Float, availableForExpanded: Float): Float = if (availableForExpanded <= 0f) 0f else sizePx_i / availableForExpanded` and a `restoreFromFraction(fraction: Float, currentAvailableForExpanded: Float): Float = fraction * currentAvailableForExpanded` (PNL-PITFALL-06 — store fraction, restore as fraction of CURRENT available; result never exceeds currentAvailableForExpanded for fraction<=1).

    Iterate until `./gradlew :library:test --tests "*.PanelGroupLogicTest"` is fully GREEN. Commit GREEN: `feat(13-02): implement PanelDistribution pure logic`.
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:test --tests "*.PanelGroupLogicTest" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `PanelDistribution.kt` exists in package `com.mordred.aero.components.layout.internal.panelgroup`, contains `internal fun clampPanelDividerPx`, and has NO `import androidx.compose` lines (grep count 0).
    - Each named test passes individually: `./gradlew :library:test --tests "*.PanelGroupLogicTest.clampPanelDividerPxInvertedRangeNoThrow"` exits 0; same for `shareTransferOnCollapseConservesPx`, `distributePxWindowResizePreservesRatios`, `activeDividerCountEECEGivesOne`, `restoreAfterShrinkDoesNotExceedAvailable`, `allCollapsedAvailableIsZero`.
    - Full `./gradlew :library:test --tests "*.PanelGroupLogicTest"` exits 0 (all GREEN).
    - `clampPanelDividerPx` body contains `coerceAtLeast` (inverted-range guard present).
  </acceptance_criteria>
  <done>Full PanelGroupLogicTest suite GREEN; pure logic has zero Compose imports; second hard gate satisfied.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:test --tests "*.PanelGroupLogicTest"` exits 0.
- `PanelDistribution.kt` has no Compose imports.
- All six 13-VALIDATION.md named tests green individually.
</verification>

<success_criteria>
- PNL-16: pure-logic functions covered by GREEN unit tests with no Compose runtime.
- PNL-PITFALL-04/05/06/07 + PITFALL-A/B classes defused at the pure-logic layer.
- Compose work (plans 13-03/04/05) is now unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/13-aeropanelgroup/13-02-SUMMARY.md`. Record the final pure-fn signatures (so plans 13-03/04/05 call them verbatim) and confirm GREEN status of each named test.
</output>
