---
phase: 13.1-aeropanelgroup-horizontal-orientation-variant
verified: 2026-06-23T08:00:00Z
status: passed
score: 12/12 must-haves verified
re_verification: false
human_verification:
  - test: "Run showcase and open Layout section under all three themes (AeroBlue / AeroDark / Classic)"
    expected: "Both vertical and horizontal AeroPanelGroup demos render correctly with Win7 Aero gloss/gradient/rounded/depth aesthetic; horizontal column titles read bottom-to-top; chevrons animate correctly; drag dividers show correct cursors and resize behavior"
    why_human: "Three-theme visual sign-off was already recorded as APPROVED in STATE.md line 200 and 13.1-03-SUMMARY.md — listed here for completeness only, not as a blocking gap"
---

# Phase 13.1: AeroPanelGroup Horizontal Orientation Variant — Verification Report

**Phase Goal:** Deliver a horizontal orientation variant of AeroPanelGroup (PNL-HORIZ-01) — N side-by-side columns, vertical dividers, drag resizes WIDTH — implemented by extracting the shipped vertical AeroPanelGroup into an orientation-parameterized internal core (AeroPanelGroupImpl) WITHOUT regressing any Phase 13 vertical behavior. Public API gains only an additive `orientation: Orientation = Orientation.Vertical` param. Three-theme showcase sign-off (AeroBlue/AeroDark/Classic) on both vertical and horizontal demos.

**Verified:** 2026-06-23
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Public AeroPanelGroup exposes `orientation: Orientation = Orientation.Vertical` with zero change to existing callers | VERIFIED | AeroPanelGroup.kt line 230: `orientation: Orientation = Orientation.Vertical,` |
| 2 | Vertical layout/state/drag body lives in `internal fun AeroPanelGroupImpl(orientation, ...)` | VERIFIED | AeroPanelGroup.kt line 262: `internal fun AeroPanelGroupImpl(`; public wrapper at line 240 calls it with `orientation = orientation` |
| 3 | Horizontal branch reads totalPx from `constraints.maxWidth` (not maxHeight) | VERIFIED | AeroPanelGroup.kt line 340: `val totalPx = if (isHorizontal) constraints.maxWidth.toFloat() else constraints.maxHeight.toFloat()` |
| 4 | Horizontal branch renders N side-by-side columns via Row container | VERIFIED | AeroPanelGroup.kt line 461: `Row(modifier = Modifier.fillMaxSize())`; vertical branch at line 608: `Column(modifier = Modifier.fillMaxSize())` — both present |
| 5 | Horizontal header strip is 36dp wide, full height, with rotated bottom-to-top title | VERIFIED | AeroPanelGroup.kt line 487: `.width(HEADER_HEIGHT)`; line 533: `.rotate(-90f)`; line 529: `softWrap = false`; requiredWidth(maxHeight) pattern at lines 516-534 |
| 6 | Horizontal chevron animates 0deg (expanded >) to 180deg (collapsed <) | VERIFIED | AeroPanelGroup.kt lines 441-446: `if (expandedState.getOrElse(i) { false }) 0f else 180f` for isHorizontal branch |
| 7 | Vertical divider drag between expanded columns uses aeroDragSplitter(Orientation.Horizontal) | VERIFIED | AeroPanelGroup.kt line 589: `orientation = Orientation.Horizontal` at PanelGroupDivider call site in horizontal branch |
| 8 | PanelGroupDivider is orientation-aware with both grip-dot branches | VERIFIED | AeroPanelGroup.kt lines 787-816: Column of 3 dots (horizontal) and Row of 3 dots (vertical) branches present |
| 9 | All 12 PanelGroupLogicTest tests GREEN; PanelDistribution.kt untouched | VERIFIED | `./gradlew :library:test --tests "*.PanelGroupLogicTest"` BUILD SUCCESSFUL; git log shows no Phase 13.1 commits to PanelDistribution.kt or PanelGroupLogicTest.kt |
| 10 | `:library:compileKotlin` and `:showcase:compileKotlin` GREEN | VERIFIED | Both `./gradlew :library:compileKotlin` and `./gradlew :showcase:compileKotlin` exit 0 with no errors |
| 11 | LayoutSection.kt has horizontal AeroPanelGroup demo(s) with correct call and vertical demo unchanged | VERIFIED | LayoutSection.kt lines 253-302 (3-col Explorer/Editor/Properties) and 304-389 (4-col Navigator/Sources/Activity/Alerts); both use `orientation = Orientation.Horizontal`; vertical demo at lines 186-248 untouched; `import androidx.compose.foundation.gestures.Orientation` at line 3 |
| 12 | Three-theme visual sign-off APPROVED on both vertical and horizontal demos | VERIFIED (human-recorded) | STATE.md line 200: "Three-theme sign-off APPROVED — AeroBlue / AeroDark / Classic — on both vertical (regression) and horizontal (PNL-HORIZ-01) demos; PNL-17 Aero fidelity confirmed across both orientations"; 13.1-03-SUMMARY.md records identical outcome with defect fixes (GAP-1 rotated titles, GAP-2 column distribution) committed prior to sign-off |

**Score:** 12/12 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|---------|--------|---------|
| `library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt` | Public AeroPanelGroup with orientation default param + internal AeroPanelGroupImpl core + orientation-aware PanelGroupDivider; horizontal Row branch with maxWidth axis, rotated header strip, Orientation.Horizontal divider; KDoc with @param orientation + PNL-HORIZ-01 | VERIFIED | 819-line file with all required constructs present and substantive; no stubs; no TODOs |
| `showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt` | Horizontal demo(s) present; vertical demo unchanged; Orientation import present | VERIFIED | Two horizontal demos appended (lines 250-389); vertical demo at lines 182-248 untouched; import at line 3 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| AeroPanelGroup (public wrapper) | AeroPanelGroupImpl (internal core) | `AeroPanelGroupImpl(` with `orientation = orientation` | VERIFIED | AeroPanelGroup.kt lines 240-248: complete forwarding call with all 7 params including `orientation = orientation` |
| AeroPanelGroupImpl horizontal branch | constraints.maxWidth | `if (isHorizontal) constraints.maxWidth.toFloat()` | VERIFIED | AeroPanelGroup.kt line 340 — axis switch conditional present |
| horizontal vertical-divider | aeroDragSplitter | `orientation = Orientation.Horizontal` at PanelGroupDivider call | VERIFIED | AeroPanelGroup.kt line 589 — Orientation.Horizontal passed; aeroDragSplitter at lines 767-773 receives it |
| horizontal header strip title | rotate(-90f) with requiredWidth(maxHeight) | BoxWithConstraints + requiredWidth + rotate | VERIFIED | AeroPanelGroup.kt lines 516-534: BoxWithConstraints with `val titleSpan = maxHeight`, `Modifier.requiredWidth(titleSpan).rotate(-90f)` |
| LayoutSection.kt horizontal demo | AeroPanelGroup(orientation = Orientation.Horizontal) | `orientation = Orientation.Horizontal` in showcase call | VERIFIED | LayoutSection.kt lines 257 and 312 — two demos both pass Orientation.Horizontal |
| AeroPanelGroupImpl | PanelGroupDivider | `PanelGroupDivider(orientation = ...)` from both branches | VERIFIED | Horizontal call at line 588, vertical call at line 702 — both pass orientation param |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|------------|------------|-------------|--------|---------|
| PNL-HORIZ-01 | 13.1-02, 13.1-03 | Horizontal orientation: N side-by-side columns, vertical dividers, drag resizes WIDTH | SATISFIED | Full horizontal branch in AeroPanelGroupImpl; two showcase demos; sign-off APPROVED |
| PNL-01 | 13.1-01 | scope-DSL: N vertical sections via `section(key, title) { content }` | SATISFIED (regression) | AeroPanelGroupScope.section() at line 116 unchanged; 12 logic tests GREEN |
| PNL-02 | 13.1-01 | Collapse section to ~36dp strip; freed height redistributed | SATISFIED (regression) | onToggle() with shareTransferOnCollapse; distributePx; 12 tests GREEN |
| PNL-03 | 13.1-01 | Re-expand restores prior size via lastExpandedFraction | SATISFIED (regression) | restoreFromFraction + shareTransferOnExpand; test coverage |
| PNL-04 | 13.1-01 | Fraction-based size state survives window resize | SATISFIED (regression) | sizePx as abstract weights; no totalPx remember-key (PITFALL-A comment at line 284) |
| PNL-05 | 13.1-01 | Drag divider between adjacent expanded sections with cursor and grip | SATISFIED (regression) | aeroDragSplitter + grip-dot branches in PanelGroupDivider |
| PNL-06 | 13.1-01 | Grip only between two adjacent EXPANDED sections | SATISFIED (regression) | `isExpandedNow && i < sections.lastIndex && nextExpanded` condition both branches |
| PNL-07 | 13.1-01 | Collapse/expand 200ms animation; drag writes directly (no tween lag) | SATISFIED (regression) | animationSpec = if (isDragging) snap() else tween(200ms) at line 430 |
| PNL-08 | 13.1-01 | Hybrid controlled/uncontrolled expansion; do not collapse to one branch | SATISFIED (regression) | Dual branch at onToggle(); "Do not collapse to one branch" comments preserved (lines 159, 277, 325, 353, 460, 607) |
| PNL-09 | 13.1-01 | onLayoutChange fires at drag-end and toggle only (not per frame) | SATISFIED (regression) | onLayoutChange invocations at lines 388, 597, 714 — all at drag-end or toggle end |
| PNL-10 | 13.1-01 | Per-section minSize; clampPanelDividerPx with pairwise minima | SATISFIED (regression) | clampPanelDividerPx call at line 417; minAbovePx + minBelowPx at lines 410, 413 |
| PNL-11 | 13.1-01 | collapsible=false: no chevron, no collapse | SATISFIED (regression) | `if (section.collapsible)` guards on chevron and toggle in both branches |
| PNL-12 | 13.1-01 | resizable=false: divider rendered without grip, drag disabled | SATISFIED (regression) | `dragEnabled = section.resizable && sections[i+1].resizable`; passed to PanelGroupDivider enabled param |
| PNL-13 | 13.1-01 | Section identity by explicit `key` | SATISFIED (regression) | `key(section.key)` at lines 463 and 610 in render loops |
| PNL-14 | 13.1-01 | glassPanel header with CaretRight, leadingIcon, headerActions; Win7 Aero | SATISFIED (regression) | glassPanel() calls in both branches; headerActions non-bubbling (separate Row from toggle clickable) |
| PNL-15 | 13.1-01 | Edge cases: all collapsed = header stack; one expanded = fills availableForExpanded | SATISFIED (regression) | distributePx + computeAvailablePx handle this; weight(1f) on last expanded; test coverage |
| PNL-16 | 13.1-01 | Pure logic covered by unit tests (TDD) | SATISFIED (regression) | 12 PanelGroupLogicTest tests GREEN; PanelGroupLogicTest.kt unchanged in Phase 13.1 |
| PNL-17 | 13.1-03 | Showcase demo; three-theme visual sign-off PASSED | SATISFIED | Two horizontal demos in LayoutSection.kt; vertical demo unchanged; sign-off APPROVED on all three themes |
| PNL-18 | 13.1-01 | KDoc with REQ-ID and PITFALL references | SATISFIED | KDoc at lines 145-226 contains @param orientation, PNL-HORIZ-01 (3 occurrences), 6+ PNL-08 references, PITFALL-A, PITFALL-B, PNL-04..PNL-14 traceability |

**Note on REQUIREMENTS.md traceability table:** The table at lines 72-89 maps PNL-01..PNL-18 to Phase 13 only; PNL-HORIZ-01 appears in the "AeroPanelGroup extensions" section without a Phase 13 row (the table predates Phase 13.1). This is a documentation gap in REQUIREMENTS.md, not a code gap — PNL-HORIZ-01 is fully implemented. ROADMAP.md line 137 correctly maps PNL-HORIZ-01 to Phase 13.1.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| LayoutSection.kt | 159 | `placeholder = "Session name"` | Info | Not a stub — this is the AeroTextField placeholder UI string for the wizard step, predating Phase 13.1 entirely |

No blockers or warnings found. The sole grep match was a legitimate UI placeholder string argument, not a code stub.

---

### Human Verification

The three-theme visual sign-off was performed by the user during the Phase 13.1-03 checkpoint and recorded as APPROVED in STATE.md and 13.1-03-SUMMARY.md. No additional human verification is required for this phase.

For completeness, the sign-off checklist verified:

**Vertical demo (regression — APPROVED):** Three stacked sections; glassPanel header gloss; chevron collapses/expands with 200ms animation at 0/90 degrees; drag divider between expanded sections with N/S cursor and grip dots; collapsible=false hides chevron; resizable=false omits grip; leadingIcon + headerActions render.

**Horizontal demo (PNL-HORIZ-01 — APPROVED):** Three and four side-by-side columns; ~36dp-wide full-height vertical header strip with glassPanel gloss; rotated bottom-to-top title (BoxWithConstraints + requiredWidth(maxHeight) + rotate(-90f)); chevron at 0deg (expanded >) animates to 180deg (collapsed <); drag vertical divider resizes column WIDTH with E/W cursor and Column grip dots; collapsible=true and resizable=true exercised in 4-column demo; rotated title does not leak into content area (clipToBounds); Win7 Aero aesthetic confirmed across AeroBlue/AeroDark/Classic.

Two defects were found and fixed before sign-off approval: GAP-1 (rotated titles clipped/mis-centered — fixed by requiredWidth approach, commit ec6be6f) and GAP-2 (column width distribution — fixed by removing weight(1f) from outer section Row, commit 410fcaf).

---

### Gaps Summary

No gaps. All 12 must-haves are verified. All requirement IDs declared across the three plans are accounted for. Automated build and test gates are GREEN. Human three-theme sign-off is APPROVED and recorded.

---

_Verified: 2026-06-23_
_Verifier: Claude (gsd-verifier)_
