---
phase: 02-atomic-components
plan: "06"
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt
autonomous: false
requirements: [BTN-01, BTN-02, BTN-03, BTN-04, INP-01, INP-02, INP-03, INP-04, INP-05, INP-06, SEL-01, SEL-02, SEL-03, SEL-04, SEL-05, DRP-01, DRP-02, RNG-01, RNG-02, LST-01, LST-02]
must_haves:
  truths:
    - "ShowcaseApp renders all six new sections (Buttons, Input, Selection, Dropdown, Range, Lists) instead of the corresponding PlaceholderSection calls"
    - "ThemeSwitcher uses AeroSegmentedControl (SEL-05) instead of three Material3 Buttons — proves SEL-05 is publicly exported and works for theme switching"
    - "Visual checkpoint: human verifies all 21 atomic components render correctly across AeroBlue / AeroDark / Classic themes"
    - "All 21 Phase 2 requirement IDs are now visible and interactive in the running showcase"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt"
      provides: "ShowcaseApp now imports and invokes ButtonsSection, InputSection, SelectionSection, DropdownSection, RangeSection, ListSection"
      contains: "ButtonsSection()"
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt"
      provides: "ThemeSwitcher rebuilt on AeroSegmentedControl"
      contains: "AeroSegmentedControl"
  key_links:
    - from: "showcase/.../ShowcaseApp.kt"
      to: "all 6 section composables"
      via: "import com.mordred.showcase.sections.{ButtonsSection,InputSection,SelectionSection,DropdownSection,RangeSection,ListSection}"
      pattern: "import com\\.mordred\\.showcase\\.sections"
    - from: "showcase/.../ThemeSwitcher.kt"
      to: "AeroSegmentedControl from :library"
      via: "import com.mordred.aero.components.selection.AeroSegmentedControl"
      pattern: "AeroSegmentedControl"
---

<objective>
Wire all 6 Phase 2 sections (created in plans 02-01..02-05) into ShowcaseApp.kt, replacing the five `PlaceholderSection(category=...)` calls. Rebuild ThemeSwitcher.kt on top of `AeroSegmentedControl` (SEL-05) to prove the segmented control is publicly exported and functional. Run a visual checkpoint to confirm all 21 atomic components render correctly across all three themes.

Purpose: Close Phase 2 with a fully-interactive showcase. The visual checkpoint is the goal-backward verification for ALL 21 Phase 2 requirement IDs — every BTN/INP/SEL/DRP/RNG/LST id is satisfied only when its component is visibly correct in the running app.
Output: Updated ShowcaseApp.kt + ThemeSwitcher.kt, plus visual checkpoint approval.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-atomic-components/02-CONTEXT.md
@.planning/phases/02-atomic-components/02-01-PLAN.md
@.planning/phases/02-atomic-components/02-02-PLAN.md
@.planning/phases/02-atomic-components/02-03-PLAN.md
@.planning/phases/02-atomic-components/02-04-PLAN.md
@.planning/phases/02-atomic-components/02-05-PLAN.md

@showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
@showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt
@showcase/src/main/kotlin/com/mordred/showcase/sections/PlaceholderSection.kt
@showcase/src/main/kotlin/com/mordred/showcase/sections/FoundationSection.kt

<interfaces>
Current ShowcaseApp.kt structure (BEFORE this plan):
```kotlin
Column(verticalArrangement = Arrangement.spacedBy(8.dp)) {
    PlaceholderSection(category = "Buttons")
    PlaceholderSection(category = "Input")
    PlaceholderSection(category = "Selection")
    PlaceholderSection(category = "Dropdown")
    PlaceholderSection(category = "Range & Progress")
}
```

(Note: original ShowcaseApp does NOT have a PlaceholderSection for "Lists & Data Display" because Phase 1 had only 5 placeholder rows. Phase 2 adds Lists as a new section.)

Current ThemeSwitcher.kt (BEFORE):
```kotlin
val themes = listOf("AeroBlue" to AeroBlue, "AeroDark" to AeroDark, "Classic" to Classic)
Row { themes.forEach { (name, scheme) ->
    Button(onClick = { onSelect(scheme) },
        colors = ButtonDefaults.buttonColors(
            containerColor = if (active) colors.primary else colors.surface,
            contentColor = if (active) colors.onPrimary else colors.onSurface
        )) { Text(name) }
}}
```

AeroSegmentedControl signature (Plan 02-03):
```kotlin
@Composable
public fun <T> AeroSegmentedControl(
    options: List<T>,
    selected: T,
    onSelect: (T) -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
    optionLabel: (T) -> String = { it.toString() }
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite ThemeSwitcher.kt with AeroSegmentedControl</name>
  <files>
    showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt
  </files>
  <read_first>
    showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt
    library/src/main/kotlin/com/mordred/aero/components/selection/AeroSegmentedControl.kt
    library/src/main/kotlin/com/mordred/aero/theme/AeroColorScheme.kt
  </read_first>
  <action>
Overwrite `showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt` with the following content. Drop all imports from `androidx.compose.material3.{Button, ButtonDefaults}` (no longer needed). Keep package and `@Composable fun ThemeSwitcher` signature unchanged so ShowcaseApp.kt callsite is preserved.

```kotlin
package com.mordred.showcase.sections

import androidx.compose.runtime.Composable
import androidx.compose.runtime.remember
import com.mordred.aero.theme.AeroColorScheme
import com.mordred.aero.components.selection.AeroSegmentedControl

@Composable
fun ThemeSwitcher(
    current: AeroColorScheme,
    onSelect: (AeroColorScheme) -> Unit
) {
    val themes = remember {
        listOf(
            ThemeOption("AeroBlue", AeroColorScheme.AeroBlue),
            ThemeOption("AeroDark", AeroColorScheme.AeroDark),
            ThemeOption("Classic", AeroColorScheme.Classic)
        )
    }
    val selected = themes.first { it.scheme == current }
    AeroSegmentedControl(
        options = themes,
        selected = selected,
        onSelect = { onSelect(it.scheme) },
        optionLabel = { it.name }
    )
}

private data class ThemeOption(val name: String, val scheme: AeroColorScheme)
```

Why a wrapper data class? `AeroColorScheme` instances do not have human-readable names; `optionLabel` needs to render "AeroBlue"/"AeroDark"/"Classic". Wrapping in `ThemeOption(name, scheme)` keeps the AeroSegmentedControl `<T>` generic clean.

Verification: `./gradlew :showcase:compileKotlin` exits 0.
  </action>
  <verify>
    <automated>./gradlew :showcase:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - File `showcase/src/main/kotlin/com/mordred/showcase/sections/ThemeSwitcher.kt` exists
    - File contains exact substring `import com.mordred.aero.components.selection.AeroSegmentedControl`
    - File contains exact substring `AeroSegmentedControl(`
    - File contains exact substring `private data class ThemeOption`
    - File does NOT contain any of: `androidx.compose.material3.Button`, `ButtonDefaults`, `import androidx.compose.foundation.layout.Row` (unless retained for inner layout — should NOT be needed)
    - File does NOT contain `Color(0x` literals
    - `./gradlew :showcase:compileKotlin` exits 0
  </acceptance_criteria>
  <done>
    ThemeSwitcher rebuilt on AeroSegmentedControl. Selecting a theme option calls onSelect with the correct AeroColorScheme. Compile gate green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire all 6 sections into ShowcaseApp.kt</name>
  <files>
    showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
  </files>
  <read_first>
    showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/InputSection.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/SelectionSection.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/DropdownSection.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/RangeSection.kt
    showcase/src/main/kotlin/com/mordred/showcase/sections/ListSection.kt
  </read_first>
  <action>
Overwrite `showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt` to:
1. Replace the existing `Column { PlaceholderSection(...) ×5 }` block with explicit calls to the six new sections.
2. ADD a "Lists & Data Display" section (Phase 1 had only 5 placeholders; Phase 2 introduces the sixth category).
3. Keep all existing imports for theme + Surface + Foundation section. Drop unused `PlaceholderSection` import.

Final content:

```kotlin
package com.mordred.showcase

import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.rememberScrollState
import androidx.compose.foundation.verticalScroll
import androidx.compose.material3.Surface
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.mordred.aero.theme.AeroColorScheme
import com.mordred.aero.theme.AeroTheme
import com.mordred.showcase.sections.ButtonsSection
import com.mordred.showcase.sections.DropdownSection
import com.mordred.showcase.sections.FoundationSection
import com.mordred.showcase.sections.InputSection
import com.mordred.showcase.sections.ListSection
import com.mordred.showcase.sections.RangeSection
import com.mordred.showcase.sections.SelectionSection
import com.mordred.showcase.sections.ThemeSwitcher

@Composable
fun ShowcaseApp() {
    var currentScheme by remember { mutableStateOf(AeroColorScheme.AeroBlue) }

    AeroTheme(colorScheme = currentScheme) {
        val colors = AeroTheme.colors
        val typography = AeroTheme.typography
        Surface(
            color = colors.background,
            modifier = Modifier.fillMaxSize()
        ) {
            Column(
                modifier = Modifier
                    .fillMaxSize()
                    .verticalScroll(rememberScrollState())
                    .padding(48.dp),
                verticalArrangement = Arrangement.spacedBy(32.dp)
            ) {
                ThemeSwitcher(
                    current = currentScheme,
                    onSelect = { currentScheme = it }
                )

                Column(verticalArrangement = Arrangement.spacedBy(16.dp)) {
                    Text(
                        text = "Foundation",
                        color = colors.onBackground,
                        style = typography.title
                    )
                    FoundationSection()
                }

                ButtonsSection()
                InputSection()
                SelectionSection()
                DropdownSection()
                RangeSection()
                ListSection()

                Spacer(Modifier.height(24.dp))
            }
        }
    }
}
```

Note: Each section composable already renders its own title `Text("Buttons", ...)` etc., so no extra Column heading wrapper is needed (the FoundationSection still uses the explicit Column+Text wrapper because FoundationSection.kt did not include its own title in Phase 1).

Verification: run BOTH `./gradlew :showcase:compileKotlin` AND `./gradlew :library:test` (sanity check no Phase 1 test regression).
  </action>
  <verify>
    <automated>./gradlew :library:test :showcase:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - File `showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt` contains all six exact substrings: `ButtonsSection()`, `InputSection()`, `SelectionSection()`, `DropdownSection()`, `RangeSection()`, `ListSection()`
    - File contains all six matching imports for the section composables
    - File does NOT contain `PlaceholderSection` (no calls AND no import)
    - File contains `ThemeSwitcher(current = currentScheme, onSelect = { currentScheme = it })` invocation
    - `./gradlew :library:test` exits 0 (existing 19 Phase 1 tests still pass)
    - `./gradlew :showcase:compileKotlin` exits 0
  </acceptance_criteria>
  <done>
    ShowcaseApp.kt now invokes all six Phase 2 section composables in place of the previous 5 placeholders. PlaceholderSection import removed. Compile + Phase 1 tests both green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual checkpoint — verify all 21 Phase 2 components render and interact correctly</name>
  <files>
    (none — visual verification only)
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/02-atomic-components/02-CONTEXT.md
  </read_first>
  <action>
This is a manual visual verification checkpoint. The executor MUST run `./gradlew :showcase:run` to launch the showcase and then observe the behaviors described in <how-to-verify> below across all three themes (AeroBlue, AeroDark, Classic). DO NOT modify any files in this task. Wait for user approval (resume-signal) before marking complete.
  </action>
  <verify>
    <automated>./gradlew :showcase:run</automated>
  </verify>
  <what-built>
After Tasks 1-2 of this plan, plus the upstream Wave 1 plans (02-01..02-05):
- 21 atomic library components in 5 packages: buttons (4), input (6), selection (5), dropdown (2), range (2), list (2)
- 6 new showcase sections + ThemeSwitcher rebuilt on AeroSegmentedControl
- ShowcaseApp.kt invokes all 6 sections; 0 PlaceholderSection calls remain in place of Phase 2 categories
- Compile gates green: `./gradlew :library:compileKotlin`, `./gradlew :library:test`, `./gradlew :showcase:compileKotlin`
  </what-built>
  <how-to-verify>
1. From `c:\1A_WORK\ui_lib`, run: `./gradlew :showcase:run` and wait for the window to appear.
2. **Theme switcher (SEL-05 + meta-test)**: confirm the theme switcher at the top is now an Aero-styled segmented control (NOT three M3 buttons). Click each segment — background, text colors, and accent should update across the entire showcase. Selected segment should have primary-colored fill+border.
3. **Foundation row (Phase 1 regression)**: three glass demo boxes still render correctly under the Foundation heading.
4. **Buttons section (BTN-01..04)**:
   - AeroButton row: hover the "Save" button — see the white overlay (buttonHover token). Click+hold — see scale-down to 0.97. Tab into it — see 2.dp accent border. The "Disabled" button should be 40% transparent and not respond to hover/click.
   - AeroOutlinedButton row: same state matrix.
   - AeroIconButton row: 3 square buttons with ▲ ▼ × glyphs — hover shows buttonHover overlay; the third is disabled (40% alpha).
   - AeroToolbar row: 4 icon buttons with a divider in the middle, all on a glass background.
5. **Input section (INP-01..06)**:
   - AeroTextField: type text — focus border thickens to 2dp accent over 150ms; placeholder visible when empty; trailing icon slot is null here.
   - AeroTextArea: multi-line; type past visible area — vertical scroll appears.
   - AeroPasswordField: type — characters render as dots; click 👁/🙈 toggle to reveal/mask.
   - AeroNumberSpinner: click ▲ — value increments by 5; click ▼ — decrements by 5; bounds 0..100 enforced (cannot go above 100 or below 0).
   - AeroSearchField: 🔍 icon at left; type — clear × appears at right; click × — text clears, × disappears.
   - AeroFilePicker: click "Обзор" — native OS file dialog opens; pick any file — path appears in the field.
6. **Selection section (SEL-01..05)**:
   - AeroCheckbox: click — toggles ✓/empty. "Disabled" item shows ✓ at 40% alpha and does not respond to clicks. Tri-state cycles Off → On → Indeterminate (–) → Off.
   - AeroRadioGroup: 3 options vertically — selecting one deselects the others; dot scales in over 150ms.
   - AeroSwitch: thumb slides L↔R over 150ms; track flips borderDefault↔primary.
   - AeroChip: click "Filter" — flips between selected (primary border + primary text) and unselected (default border + labelText).
   - AeroSegmentedControl: 3-option Day/Week/Month — exactly one is highlighted; clicking another shifts highlight.
7. **Dropdown section (DRP-01..02)**:
   - AeroDropdown: click trigger — popup opens directly below with country list. Click an option — popup closes, "= Russia" (or similar) appears next to it. Open again — focus border thickens; press Escape — popup closes. Reopen, press ↓↓ then Enter — second option selected.
   - AeroComboBox: type "uni" — popup filters to "United States", "United Kingdom"; click one — text field updates and popup closes. Type something not in list (e.g. "qwerty") — popup hides because filter is empty; the typed text is preserved.
   - Test popup-edge clamping: scroll the showcase down so AeroDropdown is near the bottom of the visible area; open the popup — it should flip ABOVE the trigger because there is no room below.
8. **Range section (RNG-01..02)**:
   - AeroSlider: drag thumb — tooltip with "%.2f" value appears above the slider during drag, disappears on release. "= 0.50" label updates live.
   - AeroProgressBar (det): shows ~35% fill with "35%" label aligned end.
   - AeroProgressBar (ind): a 30%-wide highlight bar slides L→R over 1500ms, repeating.
9. **List section (LST-01..02)**:
   - AeroListItem: 3 rows "Inbox", "Sent", "Drafts". Hover — buttonHover overlay applied. Click — selected gets primary tint (alpha 0.2). Each item has a numeric badge on the right.
   - AeroBadge row: 3 badges including a red error badge.
10. **Theme cross-check**: switch to AeroDark — confirm all 21 components re-render with darker palette correctly. Switch to Classic — confirm flat (non-translucent) Aero look applies to all 21 components. Switch back to AeroBlue.
11. **No regressions**: foundation section (3 glass demo boxes) still renders correctly under all three themes.

Pass criteria: every numbered step above produces the described behavior in all three themes.
  </how-to-verify>
  <acceptance_criteria>
    - All 11 verification steps observable in running showcase
    - All 21 components visible and interactive in at least one theme
    - Theme switcher is an AeroSegmentedControl (not Material3 Button row)
    - All three themes render correctly without runtime crash
    - User explicitly approves with "approved"
  </acceptance_criteria>
  <resume-signal>Type "approved" to continue, or describe specific issues (component name + theme + observed vs expected).</resume-signal>
  <done>
    User-approved visual checkpoint. All 21 Phase 2 requirement IDs (BTN-01..04, INP-01..06, SEL-01..05, DRP-01..02, RNG-01..02, LST-01..02) confirmed satisfied through observable showcase behavior.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :library:compileKotlin` exits 0
- `./gradlew :library:test` exits 0 (Phase 1 regression check — 19 tests still pass)
- `./gradlew :showcase:compileKotlin` exits 0
- `./gradlew :showcase:run` launches without crash
- ShowcaseApp.kt: 0 occurrences of `PlaceholderSection` (string match), 6 occurrences of section composable invocations
- ThemeSwitcher.kt: contains `AeroSegmentedControl`, no `material3.Button` references
- Visual checkpoint approved by user
</verification>

<success_criteria>
- All 21 Phase 2 requirement IDs visible and interactive in showcase across AeroBlue / AeroDark / Classic
- AeroSegmentedControl proven publicly exported and functional via ThemeSwitcher swap
- No Phase 1 test regression
- Phase 2 deliverable complete: a developer can `./gradlew :showcase:run` and see every Phase 2 component live
</success_criteria>

<output>
Create `.planning/phases/02-atomic-components/02-06-SUMMARY.md` covering:
- ShowcaseApp.kt + ThemeSwitcher.kt diffs
- All 21 Phase 2 requirement IDs satisfied (verified in checkpoint)
- ThemeSwitcher → AeroSegmentedControl swap notes
- Visual checkpoint approval timestamp + any user-reported issues + their resolutions
- Phase 2 complete declaration: ready for Phase 3
</output>
