---
phase: 11-showcase-v2-0-visual-sign-off
plan: 11
type: execute
wave: 3
depends_on: [06, 07, 08, 09, 10]
files_modified:
  - .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md
autonomous: false
requirements: [SHW-10]
gap_closure: true

must_haves:
  truths:
    - "Full project build is green (./gradlew :library:test :showcase:compileKotlin) after all gap fixes"
    - "W11-01 grep gate (transparent = true) returns 0 hits across all touched v2.0 files"
    - "W11-02 grep gate (AeroScrollArea in datatable package) returns 0 hits"
    - "Every defect's checklist row is re-tested eyes-on and all 48 cells (16 items x 3 themes) read PASS"
    - "Phase 11 / v2.0 milestone gate (SHW-10) passes — no FAIL remains"
  artifacts:
    - path: ".planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md"
      provides: "Re-run 16x3 sign-off record — every cell PASS after gap closure"
      contains: "transparent = true"
  key_links:
    - from: "11-SIGNOFF.md re-test rows"
      to: "the 16 gap defects (F2..F-WIZARD)"
      via: "each previously-FAIL row re-verified against its fix"
      pattern: "PASS"
---

<objective>
Re-run the formal v2.0 milestone sign-off gate after gap closure. Build the project green, run the two
automated grep gates (W11-01 / W11-02), then drive the human-verified 14-item visual/interactive checklist
across all three themes (AeroBlue / AeroDark / Classic), specifically RE-TESTING each defect's checklist
row (item 3 resize bounds, 5 DatePicker popup right-align, 6 range partial-state, 7 ColorPicker hue/round-
trip, 8 RangeSlider thumbs, 9/F11 accordion divider, 10 SplitPane drag, 12/F-WIZARD wizard, 13 disabled
cells, 14 drag-on-first-pixel). Update 11-SIGNOFF.md so all 48 cells read PASS. Phase 11 is NOT complete
until every cell is PASS (SHW-10).

Purpose: this is the closing gate of v2.0. The previous sign-off (11-05) FAILED on 16 defects; this plan
confirms every one is resolved and produces the durable all-PASS sign-off artifact.
Output: 11-SIGNOFF.md with all 48 cells PASS.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-CONTEXT.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The 16-item checklist verbatim + per-item verify steps live in 11-RESEARCH.md "The 16-Item Checklist". -->
<!-- Grep gates (Bash + PowerShell) live in 11-RESEARCH.md "Grep Gates". -->
<!-- Defect -> checklist row map (which gap fix to re-verify per row): -->

  Item 1 (virtualization)            -> unchanged; re-confirm still PASS
  Item 2 (selection after sort)      -> unchanged; re-confirm PASS (PITFALL-10 four-state in AeroDark)
  Item 3 (column resize bounds)      -> F-RESIZE (11-07) + drag (11-06): right-bound + left min + 1:1 drag
  Item 4 (TreeView lazy once)        -> F5 whole-row toggle (11-07) must NOT break once-only onExpand
  Item 5 (DatePicker popup position) -> F8 compact trigger (11-10) unblocks; re-test right-align ~1024dp
  Item 6 (range partial state "—")   -> F14 (11-08) same-month + PITFALL-06 partial-state still "—"
  Item 7 (ColorPicker round-trip)    -> F12+F10 (11-09): hue slider discoverable, #FF0000 round-trip
  Item 8 (RangeSlider thumbs)        -> F9 (11-06): both thumbs hold independent values; overlap reachable
  Item 9 (Accordion single mode)     -> behaviour unchanged; F11 (11-09) divider now fits rounded bg
  Item 10 (SplitPane clamp)          -> F3+F15 (11-06): no ghosting, 1:1 drag, clamp >=48dp
  Item 11 (Sidebar reflow)           -> unchanged; re-confirm PASS
  Item 12 (Wizard validation)        -> F-WIZARD (11-10): interactive; type field, Next enables, advance
  Item 13 (AeroDark disabled cells)  -> F13 (11-10): disabled-date demo now exists; cells readable in AeroDark
  Item 14 (drag on first pixel)      -> F15 (11-06): HSV square + RangeSlider + DataTable splitter 1:1
  Items 15,16 (grep gates)           -> automated, theme-independent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + grep gates, then reset 11-SIGNOFF.md for re-verification</name>
  <read_first>
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md (existing FAILED table + 16 findings)
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md ("Grep Gates" exact commands; "The 16-Item Checklist" verbatim items + verify steps)
  </read_first>
  <files>.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md</files>
  <action>
    1. Build the project green (all gap fixes must compile + tests pass):
       ```bash
       cd C:/1A_WORK/ui_lib && ./gradlew :library:test :showcase:compileKotlin -x generateIcons
       ```
       Must exit 0. Any failure is a BLOCKER — report and stop.

    2. Run the W11-01 grep gate (item 15) in the project root:
       ```bash
       grep -rn "transparent = true" \
         library/src/main/kotlin/com/mordred/aero/components/pickers/ \
         library/src/main/kotlin/com/mordred/aero/components/datatable/ \
         library/src/main/kotlin/com/mordred/aero/components/layout/ \
         library/src/main/kotlin/com/mordred/aero/components/range/ \
         library/src/main/kotlin/com/mordred/aero/components/internal/drag/ \
         showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt \
         showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt \
         showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt
       ```
       Expected 0 matches (BLOCKER otherwise).

    3. Run the W11-02 grep gate (item 16):
       ```bash
       grep -rn "AeroScrollArea" library/src/main/kotlin/com/mordred/aero/components/datatable/
       ```
       Expected 0 matches.

    4. Rewrite the 11-SIGNOFF.md sign-off TABLE: reset items 1-14 to PENDING across all three theme
       columns (preserve the verbatim checklist item text + the existing "Findings" section as historical
       record, but add a clear "Re-verification 2026-..." sub-header). Pre-fill items 15 and 16 PASS
       (grep results from steps 2-3, theme-independent). Add a "Re-test target" note per previously-FAIL
       row pointing at the gap plan that fixed it (use the Defect->row map in <interfaces>). Update the
       frontmatter `gate_status:` to `RE-VERIFYING` and keep the launch command `./gradlew :showcase:run`.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:test :showcase:compileKotlin -x generateIcons -q && ! grep -rq "transparent = true" library/src/main/kotlin/com/mordred/aero/components/pickers/ library/src/main/kotlin/com/mordred/aero/components/datatable/ library/src/main/kotlin/com/mordred/aero/components/layout/ library/src/main/kotlin/com/mordred/aero/components/range/ library/src/main/kotlin/com/mordred/aero/components/internal/drag/ showcase/src/main/kotlin/com/mordred/showcase/sections/ && ! grep -rq "AeroScrollArea" library/src/main/kotlin/com/mordred/aero/components/datatable/</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :library:test :showcase:compileKotlin -x generateIcons` exits 0
    - W11-01 grep returns 0 hits across the listed library dirs (incl. internal/drag) + the three showcase sections
    - W11-02 grep returns 0 hits in the datatable package
    - 11-SIGNOFF.md items 1-14 reset to PENDING (3 themes each); items 15/16 PASS; previously-FAIL rows annotated with the fixing gap plan
    - `grep -c PENDING 11-SIGNOFF.md` returns 42 (14 items x 3 themes) before Task 2
  </acceptance_criteria>
  <done>Project green; both grep gates 0 hits; 11-SIGNOFF.md reset for re-verification (items 15/16 PASS, 1-14 PENDING).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify all 16 defects x 3 themes — every cell PASS (v2.0 milestone gate)</name>
  <files>.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md</files>
  <read_first>
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md (reset scaffold from Task 1 + historical Findings)
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md ("The 16-Item Checklist" per-item verify steps)
  </read_first>
  <action>
    Human-verify checkpoint. Launch `./gradlew :showcase:run`, then walk the human through the 14 visual/
    interactive items across all three themes (ThemeSwitcher: AeroBlue, AeroDark, Classic), recording
    PASS/FAIL for each of the 42 cells in 11-SIGNOFF.md. Pay special attention to the rows that were FAIL/
    BLOCKED last time (3, 5, 6, 7, 8, 10, 12, 13, 14) — the per-item re-test steps are in the how-to-verify
    block. AeroDark is the lowest-contrast theme (item 13 disabled cells, item 2 four-state rows). If any
    cell is FAIL, the corresponding gap fix is incomplete — record it and report for another gap-closure
    pass (do NOT mark Phase 11 complete with any FAIL).
  </action>
  <what-built>
    All gap fixes (11-06 drag root cause; 11-07 DataTable/TreeView F2/F4/F5/F-RESIZE; 11-08 pickers F6/F14;
    11-09 ColorPicker F12/F10 + Accordion F11; 11-10 showcase F8/F7/F13/F6/F-WIZARD) are landed and the
    project builds green. Both grep gates passed in Task 1. 11-SIGNOFF.md is reset with items 1-14 PENDING.
  </what-built>
  <how-to-verify>
    Launch: `./gradlew :showcase:run`. For EACH theme (AeroBlue, AeroDark, Classic) verify all 14 items.

    DataSection:
    1. Virtualization — ~10-15 rows visible (not 100); scroll renders new rows.
    2. Selection after sort — sort "AOS Date" asc, select SAT-050, sort desc; SAT-050 keeps highlight. In
       AeroDark confirm four row states distinguishable (PITFALL-10).
    3. (F-RESIZE/F3/F15) Column resize — drag "Name" splitter RIGHT: column grows but the table stays on
       screen (does NOT leave the window) and other columns shrink to their min (none vanish); drag LEFT:
       "Name" stops at a readable min (120dp) and stays legible. No ghosting/doubling during drag; the
       column edge tracks the cursor 1:1 (no lag).
    4. (F5) TreeView — click anywhere on a ground-station ROW (not just the chevron): it expands. Scroll
       it off-screen and back; console shows only ONE "onExpand fired for: X" line for that node.
    5. (F8) DatePicker popup — resize window to ~1024dp; the compact DatePicker trigger sits left with its
       value Text beside it; open the calendar near the right half — it right-aligns and does not clip.

    PickersSection:
    6. (F14) DateRangePicker — open the range picker; select a range WITHIN the same month (e.g. 07.06 then
       17.06 in the June grid) — it commits; the value Text shows the DD.MM.YYYY range. Clicking only one
       date then dismissing leaves the value Text at "—" (partial state not leaked, PITFALL-06).
    7. (F12/F10) ColorPicker — open AeroColorPickerButton: a vertical HUE slider is visible right of the
       HSV square; the popup is a content-width Aero glass panel (not full-window); the "Original"/"Current"
       swatch captions are visible. Set #FF0000, drag saturation to 50% then back to 100% — HEX reads
       #FF0000 (no drift).
    8. (F9) RangeSlider — drag the start thumb; the end thumb HOLDS its value (does not reset). Drag thumbs
       to overlap, release, drag again — both remain individually reachable.
    13. (F13/PITFALL-09) AeroDark disabled cells — in AeroDark open the "DatePicker (min/max)" demo; dates
        outside June 2026 are disabled and clearly readable as grey (labelText), not invisible. Same on the
        "RangePicker (min/max)" demo.

    LayoutSection:
    9. (F11) Accordion single mode — open section A then B; A closes. The inter-section divider fits inside
       the rounded section background (does not poke past the rounded corners).
    10. (F3/F15) SplitPane — drag the divider (horizontal + vertical demos): no ghosting/doubling; the
        divider tracks the cursor 1:1 (no lag); both panes stay >= 48dp at the edges.
    11. Sidebar reflow — Toggle mode Expanded->Collapsed->Hidden; adjacent content reflows to fill space.
    12. (F-WIZARD) Wizard — type a name into step-1's field; Next becomes enabled; click Next -> step 2;
        Back is enabled on step 2 and returns to step 1 with the typed name preserved. Next is blocked when
        step-1 is blank.

    Drag (cross-section):
    14. (F15) HSV square (ColorPicker), RangeSlider thumbs, DataTable column splitter ALL respond on the
        first mouse movement and track the cursor 1:1 (no slop, no slow-follow).

    Record PASS/FAIL for each of the 42 cells. All 48 cells (incl. items 15/16) must read PASS.
  </how-to-verify>
  <verify>
    <automated>test "$(grep -c PENDING .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md)" -eq 0 && test "$(grep -c FAIL .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 11-SIGNOFF.md has all 48 cells filled (16 items x 3 themes), none PENDING
    - Zero FAIL cells remain (grep -c FAIL returns 0)
    - Frontmatter `gate_status:` updated to PASSED
    - Each previously-FAIL row (3,5,6,7,8,9,10,12,13,14) explicitly re-verified PASS
  </acceptance_criteria>
  <done>All 48 cells PASS; no PENDING, no FAIL; gate_status: PASSED; Phase 11 / v2.0 milestone gate satisfied.</done>
  <resume-signal>Type "approved" if all 48 cells PASS, or describe which items/themes FAILED (routes to another gap pass).</resume-signal>
</task>

</tasks>

<verification>
- `./gradlew :library:test :showcase:compileKotlin -x generateIcons` exits 0.
- W11-01 grep: `transparent = true` = 0 hits (incl. internal/drag dir).
- W11-02 grep: `AeroScrollArea` in datatable package = 0 hits.
- 11-SIGNOFF.md: all 48 cells PASS; no PENDING, no FAIL; gate_status PASSED.
</verification>

<success_criteria>
- SHW-10 satisfied: the full 16-item checklist passes eyes-on in all three themes after gap closure; both grep gates clean; 11-SIGNOFF.md is the durable all-PASS milestone-gate artifact.
- All 16 defects (F2..F-WIZARD) re-verified resolved. Phase 11 complete only when every one of the 48 cells reads PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/11-showcase-v2-0-visual-sign-off/11-11-SUMMARY.md` summarizing the re-sign-off result, linking 11-SIGNOFF.md, and confirming v2.0 milestone gate PASS (or listing any residual FAIL for a further gap pass).
</output>
