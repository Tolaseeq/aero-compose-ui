---
phase: 11-showcase-v2-0-visual-sign-off
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt
  - library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt
autonomous: true
requirements: [SHW-07, SHW-08, SHW-09]

must_haves:
  truths:
    - "ShowcaseApp.kt calls DataSection(), PickersSection(), LayoutSection() — exactly three new calls"
    - "ShowcaseApp.kt no longer calls Phase7ScratchSection() and no longer imports it"
    - "Phase7ScratchSection.kt (:showcase) and AeroPhase7Scratch.kt (:library) are deleted"
    - "Full build compiles after wiring + scratch removal"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt"
      provides: "Root wiring with three new section calls, scratch call removed"
      contains: "DataSection()"
  key_links:
    - from: "ShowcaseApp.kt"
      to: "DataSection / PickersSection / LayoutSection"
      via: "direct composable calls + imports"
      pattern: "DataSection\\(\\)"
    - from: "ShowcaseApp.kt"
      to: "scratch removal"
      via: "Phase7ScratchSection call + import deleted"
      pattern: "Phase7ScratchSection"
---

<objective>
Wire the three new sections into `ShowcaseApp.kt` (exactly three new calls: `DataSection()`, `PickersSection()`, `LayoutSection()`) and remove the Phase 7 scratch artifacts: delete `Phase7ScratchSection.kt` (:showcase) and `AeroPhase7Scratch.kt` (:library), and remove the `Phase7ScratchSection()` call + its import from `ShowcaseApp.kt` (SC5 of the phase; also serves SHW-07/08/09 by making the sections live).

Purpose: This makes all three new sections visible in the running showcase and cleans the v2.0 JAR of temporary scratch code before sign-off. Scratch removal MUST happen before the W11-01 grep gate (Plan 05) to avoid false positives from scratch demo code.
Output: Updated ShowcaseApp.kt; two deleted scratch files.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-CONTEXT.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md

<interfaces>
<!-- Current ShowcaseApp.kt state (confirmed): -->
<!-- line 32: import com.mordred.showcase.sections.Phase7ScratchSection  (REMOVE) -->
<!-- line 96: Phase7ScratchSection()  (REMOVE) -->
<!-- Existing calls run in this order (lines 83-94): IconsSection, ButtonsSection, InputSection, SelectionSection, DropdownSection, RangeSection, ListSection, ContainersSection, OverlaysSection(toastState), NavigationSection -->
<!-- New section composables have NO parameters: DataSection(), PickersSection(), LayoutSection() -->

Scratch deletion targets (confirmed via Glob):
- showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt  → delete
- library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt           → delete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire three sections + remove scratch call/import in ShowcaseApp.kt</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt (current state — imports block + Column body)
  </read_first>
  <files>showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt</files>
  <action>
    Edit `ShowcaseApp.kt`:

    1. REMOVE the scratch import (currently line 32):
       `import com.mordred.showcase.sections.Phase7ScratchSection`
    2. ADD three new imports alongside the other section imports (keep alphabetical grouping with the existing `com.mordred.showcase.sections.*` imports):
       ```kotlin
       import com.mordred.showcase.sections.DataSection
       import com.mordred.showcase.sections.LayoutSection
       import com.mordred.showcase.sections.PickersSection
       ```
    3. REMOVE the scratch call (currently line 96):
       `Phase7ScratchSection()`
    4. ADD the three new calls in the Column body where the scratch call was — after `NavigationSection()` (line 94) and before `Spacer(Modifier.height(24.dp))`:
       ```kotlin
       DataSection()
       PickersSection()
       LayoutSection()
       ```

    Result: exactly three new section calls added, exactly one call (Phase7ScratchSection) removed, exactly one import removed, three imports added. Do NOT change any other section call, the toastState wiring, the AeroToastHost mount, or the verticalScroll structure.
  </action>
  <verify>
    <automated>grep -c "Phase7ScratchSection" showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase7ScratchSection" showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt` returns 0 (call and import both gone)
    - `grep -n "DataSection()" ShowcaseApp.kt` returns 1 hit
    - `grep -n "PickersSection()" ShowcaseApp.kt` returns 1 hit
    - `grep -n "LayoutSection()" ShowcaseApp.kt` returns 1 hit
    - `grep -c "import com.mordred.showcase.sections.DataSection\|import com.mordred.showcase.sections.PickersSection\|import com.mordred.showcase.sections.LayoutSection" ShowcaseApp.kt` returns 3
    - `grep -n "OverlaysSection(toastState" ShowcaseApp.kt` returns 1 (existing wiring untouched)
  </acceptance_criteria>
  <done>ShowcaseApp.kt calls DataSection/PickersSection/LayoutSection and no longer references Phase7ScratchSection (call + import removed).</done>
</task>

<task type="auto">
  <name>Task 2: Delete scratch files and verify full compile</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt (confirm it is the scratch wrapper before deleting)
    - library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt (confirm it is the scratch aggregator before deleting)
  </read_first>
  <files>
    showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt,
    library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt
  </files>
  <action>
    Delete both scratch files:
    - `rm showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt`
    - `rm library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt`

    Before deleting, confirm via read_first that no OTHER :showcase or :library file still references `AeroPhase7Scratch` or `Phase7ScratchSection` (grep the whole repo). The only references should be the (now-removed) ShowcaseApp.kt import/call from Task 1. If any other reference exists, STOP and report — do not delete blindly.

    After deletion, run a full compile of both modules to confirm nothing else depended on the scratch code:
    `./gradlew :library:compileKotlin :showcase:compileKotlin -x generateIcons`
  </action>
  <verify>
    <automated>./gradlew :library:compileKotlin :showcase:compileKotlin -x generateIcons</automated>
  </verify>
  <acceptance_criteria>
    - File `showcase/src/main/kotlin/com/mordred/showcase/sections/Phase7ScratchSection.kt` no longer exists
    - File `library/src/main/kotlin/com/mordred/aero/scratch/AeroPhase7Scratch.kt` no longer exists
    - `grep -rn "AeroPhase7Scratch" library/src showcase/src` returns 0 hits
    - `grep -rn "Phase7ScratchSection" showcase/src` returns 0 hits
    - `./gradlew :library:compileKotlin :showcase:compileKotlin -x generateIcons` exits 0
  </acceptance_criteria>
  <done>Both scratch files deleted; no dangling references remain; both modules compile clean.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:compileKotlin :showcase:compileKotlin -x generateIcons` is green.
- ShowcaseApp.kt has exactly three new section calls and zero Phase7ScratchSection references.
- Both scratch files deleted; repo-wide grep for scratch symbols returns 0.
</verification>

<success_criteria>
- SC5 satisfied: ShowcaseApp.kt receives exactly three new calls; scratch removed.
- All three new sections live in the running showcase; v2.0 JAR free of scratch code.
- Full build green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-showcase-v2-0-visual-sign-off/11-04-SUMMARY.md`
</output>
