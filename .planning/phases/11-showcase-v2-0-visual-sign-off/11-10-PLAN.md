---
phase: 11-showcase-v2-0-visual-sign-off
plan: 10
type: execute
wave: 2
depends_on: [06, 07, 08, 09]
files_modified:
  - showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt
  - showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt
autonomous: true
requirements: [SHW-07, SHW-08, SHW-09, SHW-10]
gap_closure: true

must_haves:
  truths:
    - "Each picker trigger in PickersSection is compact/bounded width with its value-preview Text beside it (RangeRow style), not stretched full-window (F8)"
    - "PickersSection has a DatePicker AND a DateRangePicker demo configured WITH disabled dates (min/max), so AeroDark disabled-cell readability is verifiable (F13)"
    - "The TimePicker and DateTimePicker demos enable showSeconds (HH:MM:SS) (F7)"
    - "Dates render DD.MM.YYYY in the DataTable AOS column and picker value previews (F6 showcase side)"
    - "The StepperWizard demo is interactive: the user can type the step-1 field, Next enables, and navigation works (F-WIZARD)"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt"
      provides: "Compact trigger widths, disabled-date demos, showSeconds, DD.MM.YYYY value previews"
      contains: "showSeconds = true"
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt"
      provides: "DD.MM.YYYY AOS date cell"
      contains: "%02d.%02d.%04d"
  key_links:
    - from: "PickersSection picker triggers"
      to: "Modifier.width(...) compact bound"
      via: "trigger width fixed so value Text sits to the right (RangeRow)"
      pattern: "Modifier.width"
    - from: "LayoutSection wizard step 1"
      to: "AeroStepperWizard canProceed/onValidate"
      via: "field input drives canProceed; Next enables when non-blank"
      pattern: "canProceed"
---

<objective>
Apply the showcase-only gap fixes (no library edits in this plan): F8 (give picker triggers a compact
bounded width so the value-preview Text sits to the right, RangeRow style — restores checklist item 5
verifiability), F13 (add DatePicker + DateRangePicker demos configured WITH disabled dates so AeroDark
disabled-cell readability — item 13 — is verifiable, exercising PITFALL-09), F7 (enable
`showSeconds = true` on the TimePicker + DateTimePicker demos — the library param already exists), the
F6 showcase side (DD.MM.YYYY in the DataTable AOS column + picker value previews), and F-WIZARD
(make the StepperWizard demo interactive — diagnose why Back/Next were both disabled).

Purpose: this plan consumes the library fixes from 11-06..11-09 and wires the showcase so the
re-sign-off (11-11) can verify every checklist row eyes-on. Per 11-CONTEXT.md gap_closure, F8 is
SHOWCASE-ONLY and F7 is already implemented in the library (just enable it here).
Output: a showcase that exercises every fixed defect across all three themes.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-CONTEXT.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md

<interfaces>
<!-- Verified against source 2026-06-18: -->

PickersSection.kt uses a private RangeRow (label 160dp + content Row). Picker triggers currently call
AeroDatePicker/etc. with NO modifier, so AeroTextField takes parent constraints and stretches full-width
(F8) — squeezing the value Text into a column. Fix: pass `modifier = Modifier.width(220.dp)` (compact)
to each picker trigger so the value Text sits beside it.

AeroTimePicker(value, onValueChange, ..., showSeconds: Boolean = false) and
AeroDateTimePicker(..., showSeconds: Boolean = false) — params ALREADY EXIST (library). F7 = pass
showSeconds = true in the demos.

AeroDatePicker(..., minDate: LocalDate?, maxDate: LocalDate?, selectableDates: (LocalDate)->Boolean) and
AeroDateRangePicker(same) — pass minDate/maxDate to create disabled dates (F13). Disabled cells use the
labelText token in AeroDark (PITFALL-09) — readability verified eyes-on in 11-11.

DataSection.kt AOS cell currently `Text(s.aosDate.toString(), ...)` (ISO). F6 showcase: render DD.MM.YYYY.
Value previews in PickersSection currently use raw `.toString()`; per F6 gap decision, the value previews
should also show DD.MM.YYYY for dates (the range preview "start → end" in DD.MM.YYYY). The library default
formatter already became DD.MM.YYYY in 11-08, so the trigger field shows DD.MM.YYYY automatically; the
value Text below must match.

LayoutSection.kt wizard demo (verified): Back `enabled = currentStepInt > 0` (disabled on step 0 — correct),
Next `enabled = steps[currentStepInt].canProceed` where step-1 `canProceed = wizardName.isNotBlank()`
(disabled until the user types — correct). F-WIZARD ("both disabled, non-interactive") therefore means the
user could not TYPE into the step-1 AeroTextField — investigate: the wizard keeps inactive steps at
`Modifier.size(0.dp)` but the ACTIVE step is `Modifier.fillMaxWidth().weight(1f, fill=false)` inside an
AeroCard inside a verticalScroll Column with NO bounded height → the weight(1f) step content area may
collapse to 0 height, hiding/blocking the field. Likely fix: give the wizard/card a bounded height (Box
with fixed height) or remove the weight collapse by wrapping step content height. Diagnose and fix in the
demo; if it is a genuine library bug in AeroStepperWizard's content-area sizing, STOP and report (it would
move to a library gap task).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: F8 compact triggers + F7 showSeconds + F13 disabled-date demos + F6 value previews (PickersSection)</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt (RangeRow pattern; current full-width triggers, raw .toString() value Text)
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md (PickersSection picker row pattern; PITFALL-09 disabled cells)
  </read_first>
  <files>showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt</files>
  <action>
    1. F8 — compact trigger width. Pass `modifier = Modifier.width(220.dp)` to AeroDatePicker,
       AeroTimePicker, AeroDateTimePicker, AeroDateRangePicker so each trigger is bounded and the value
       Text sits to its right (RangeRow already lays out content horizontally). AeroColorPickerButton is a
       32dp swatch (already compact) — leave it. AeroRangeSlider already uses Modifier.width(240.dp).
    2. F7 — enable seconds: `AeroTimePicker(value = timeValue, onValueChange = { timeValue = it }, showSeconds = true, modifier = Modifier.width(220.dp))`
       and `AeroDateTimePicker(..., showSeconds = true, modifier = Modifier.width(220.dp))`.
    3. F13 — add disabled-date demos. Add two new RangeRow entries (after the existing range picker row):
       ```kotlin
       val minD = LocalDate(2026, 6, 1)
       val maxD = LocalDate(2026, 6, 30)   // only June 2026 selectable; everything else disabled
       var boundedDate by remember { mutableStateOf<LocalDate?>(null) }
       RangeRow(label = "DatePicker (min/max)") {
           AeroDatePicker(value = boundedDate, onValueChange = { boundedDate = it }, minDate = minD, maxDate = maxD, modifier = Modifier.width(220.dp))
           Text(boundedDate?.let { "%02d.%02d.%04d".format(it.dayOfMonth, it.monthNumber, it.year) } ?: "—", color = colors.labelText, style = typography.bodyMedium)
       }
       var bStart by remember { mutableStateOf<LocalDate?>(null) }
       var bEnd by remember { mutableStateOf<LocalDate?>(null) }
       RangeRow(label = "RangePicker (min/max)") {
           AeroDateRangePicker(startValue = bStart, endValue = bEnd, onRangeSelect = { s, e -> bStart = s; bEnd = e }, minDate = minD, maxDate = maxD, modifier = Modifier.width(220.dp))
           Text(if (bStart != null && bEnd != null) "${"%02d.%02d.%04d".format(bStart!!.dayOfMonth, bStart!!.monthNumber, bStart!!.year)} → ${"%02d.%02d.%04d".format(bEnd!!.dayOfMonth, bEnd!!.monthNumber, bEnd!!.year)}" else "—", color = colors.labelText, style = typography.bodyMedium)
       }
       ```
       This creates visible disabled cells (out-of-June dates) for item 13 / PITFALL-09.
    4. F6 — value previews in DD.MM.YYYY. Update the existing date/range value Texts to render DD.MM.YYYY
       (use the same `"%02d.%02d.%04d".format(d.dayOfMonth, d.monthNumber, d.year)` form). For
       DateRangePicker keep the committed-only display ("—" until onRangeSelect fires — PITFALL-06).
       For TimePicker/DateTimePicker value Text, the raw `.toString()` is acceptable (shows HH:MM:SS for
       UAT); keep those raw.
    Import `androidx.compose.foundation.layout.width` and `androidx.compose.ui.unit.dp` if missing.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && grep -q "showSeconds = true" showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt && grep -q "minDate = minD" showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt && grep -q "%02d.%02d.%04d" showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt && ./gradlew :showcase:compileKotlin -x generateIcons -q</automated>
  </verify>
  <acceptance_criteria>
    - All four date/time picker triggers pass `Modifier.width(220.dp)` (grep `Modifier.width(220.dp)` >= 4 hits, allowing the two new disabled-date rows)
    - `showSeconds = true` appears for both AeroTimePicker and AeroDateTimePicker (grep -c >= 2)
    - Two disabled-date demos exist with `minDate = minD` + `maxDate = maxD` (DatePicker + DateRangePicker)
    - Date value previews use the `%02d.%02d.%04d` DD.MM.YYYY form
    - `./gradlew :showcase:compileKotlin -x generateIcons` exits 0
  </acceptance_criteria>
  <done>Compact triggers + showSeconds + two disabled-date demos + DD.MM.YYYY previews wired; showcase compiles.</done>
</task>

<task type="auto">
  <name>Task 2: F6 DataTable AOS date cell DD.MM.YYYY (DataSection)</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt (AOS Date column cell uses s.aosDate.toString())
  </read_first>
  <files>showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt</files>
  <action>
    Change the AOS Date column cell from ISO to DD.MM.YYYY (F6: dates render DD.MM.YYYY everywhere,
    including the table). Replace:
    ```kotlin
    cell = { s -> Text(s.aosDate.toString(), color = colors.onSurface) }
    ```
    with:
    ```kotlin
    cell = { s -> Text("%02d.%02d.%04d".format(s.aosDate.dayOfMonth, s.aosDate.monthNumber, s.aosDate.year), color = colors.onSurface) }
    ```
    Optionally bump the "Name" column minWidth to keep it readable on left-shrink (F-RESIZE left side
    is a showcase config concern): change the Name column to
    `AeroTableColumn<SatSession>(header = "Name", width = AeroColumnWidth.Weight(2f), minWidth = 120.dp, sortKey = { it.name }, cell = ...)`.
    Keep `key = { it.id }` (PITFALL-04). Do not otherwise restructure DataSection.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && grep -q "%02d.%02d.%04d" showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt && ! grep -q "s.aosDate.toString()" showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt && ./gradlew :showcase:compileKotlin -x generateIcons -q</automated>
  </verify>
  <acceptance_criteria>
    - AOS Date cell renders DD.MM.YYYY via `%02d.%02d.%04d` (grep matches); the old `s.aosDate.toString()` cell is gone
    - Name column has a `minWidth = 120.dp` (readable on left-shrink, F-RESIZE)
    - `key = { it.id }` unchanged
    - `./gradlew :showcase:compileKotlin -x generateIcons` exits 0
  </acceptance_criteria>
  <done>AOS date column shows DD.MM.YYYY; Name minWidth 120dp; showcase compiles.</done>
</task>

<task type="auto">
  <name>Task 3: F-WIZARD — make the StepperWizard demo interactive</name>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt (wizard demo: AeroCard wrapping AeroStepperWizard, step content area)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroStepperWizard.kt (content-area sizing: active step weight(1f, fill=false); Back enabled = step>0; Next enabled = canProceed)
  </read_first>
  <files>showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt</files>
  <action>
    F-WIZARD: in the running demo Back + Next were both disabled / the wizard was non-interactive. Back is
    correctly disabled on step 0; Next is correctly disabled until `wizardName.isNotBlank()`. The actionable
    bug is that the user could not TYPE into the step-1 field (so canProceed never flips). The likely cause
    is the wizard's step content area collapsing to 0 height inside the verticalScroll Column / AeroCard.

    Fix in the demo:
    1. Give the wizard a bounded height so its content area can lay out: wrap the AeroStepperWizard in a
       fixed-height container, e.g. replace the bare `AeroCard { AeroStepperWizard(...) }` with:
       ```kotlin
       AeroCard(modifier = Modifier.fillMaxWidth()) {
           Box(Modifier.fillMaxWidth().height(200.dp)) {
               AeroStepperWizard( ...same steps... , onFinish = {})
           }
       }
       ```
       (200.dp gives the step content + buttons room; bounded height prevents the weight(1f, fill=false)
       step area from collapsing inside the scroll Column.)
    2. Confirm the step-1 field is the AeroTextField bound to `wizardName` with
       `canProceed = wizardName.isNotBlank()` and `onValidate = { wizardName.isNotBlank() }` (keep as-is).
       After the height fix, typing into the field should enable Next.
    3. Test by reasoning: with the bounded box, the AeroTextField is visible and focusable; typing sets
       `wizardName`, recomposition flips `canProceed = true`, Next enables; clicking Next advances to step 2;
       Back enables on step 2. Document this flow in the SUMMARY.

    If, after giving the wizard a bounded height, the field is STILL non-interactive (e.g. the AeroTextField
    inside the wizard's collapsed inactive-step boxes is intercepting focus, or weight(1f,fill=false) still
    yields 0 height), that indicates a library sizing bug in AeroStepperWizard — STOP and report it as a
    library gap (do not hack around it in the demo). Note: this is the one F-WIZARD escalation path.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && grep -q "AeroStepperWizard" showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt && grep -q "height(200.dp)" showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt && ./gradlew :showcase:compileKotlin -x generateIcons -q</automated>
  </verify>
  <acceptance_criteria>
    - The wizard demo wraps `AeroStepperWizard` in a bounded-height container (`height(200.dp)` present in LayoutSection.kt)
    - Step-1 `canProceed = wizardName.isNotBlank()` + `onValidate = { wizardName.isNotBlank() }` preserved
    - `./gradlew :showcase:compileKotlin -x generateIcons` exits 0
    - SUMMARY documents the diagnosed cause (content-area collapse) and confirms the type→enable→advance flow, OR escalates a library bug if the bounded-height fix did not make it interactive
  </acceptance_criteria>
  <done>Wizard demo has bounded height so step-1 field is interactive; Next enables on input; or library bug escalated.</done>
</task>

</tasks>

<verification>
- `./gradlew :showcase:compileKotlin -x generateIcons` exits 0.
- F8: picker triggers compact (Modifier.width(220.dp)); value Text beside them.
- F7: showSeconds=true on TimePicker + DateTimePicker demos.
- F13: DatePicker + DateRangePicker demos with min/max (disabled dates) present.
- F6: DataTable AOS column + date value previews render DD.MM.YYYY.
- F-WIZARD: wizard demo bounded height → interactive (or library bug escalated).
</verification>

<success_criteria>
- The showcase exercises every fixed defect so the re-sign-off (11-11) can verify items 3, 5, 6, 7, 13, 14 and the DD.MM.YYYY expectation eyes-on across all three themes.
- No library source edited in this plan (F8/F7/F13/F6-showcase/F-WIZARD are showcase-only here; F7 library param already existed).
</success_criteria>

<output>
After completion, create `.planning/phases/11-showcase-v2-0-visual-sign-off/11-10-SUMMARY.md` listing each showcase change (F8/F7/F13/F6/F-WIZARD), and the F-WIZARD diagnosis outcome.
</output>
