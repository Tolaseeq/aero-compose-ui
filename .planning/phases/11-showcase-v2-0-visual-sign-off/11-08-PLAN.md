---
phase: 11-showcase-v2-0-visual-sign-off
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt
autonomous: true
requirements: [SHW-08, SHW-10]
gap_closure: true

must_haves:
  truths:
    - "A selected date renders as DD.MM.YYYY (e.g. 07.06.2025) in every picker trigger by default (F6)"
    - "AeroDateTimePicker renders its date part as DD.MM.YYYY by default; AeroDateRangePicker renders both endpoints as DD.MM.YYYY (F6)"
    - "AeroDateRangePicker can select a range whose start and end are in the SAME visible month (e.g. 07.06–17.06), both endpoints clickable in either visible month (F14)"
    - "onRangeSelect still fires exactly once per completed range and never on a start-only click (PITFALL-06 intact)"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt"
      provides: "DD.MM.YYYY default formatter"
      contains: "formatAeroDate"
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt"
      provides: "Same-month range selection (relaxed state machine) + DD.MM.YYYY default"
      contains: "formatAeroDate"
  key_links:
    - from: "AeroDateRangePicker onDayClick"
      to: "nextRangeState commit guard"
      via: "single onRangeSelect call site (PITFALL-06)"
      pattern: "onRangeSelect"
---

<objective>
Apply the two locked picker decisions from 11-CONTEXT.md gap_closure: F6 (date format DD.MM.YYYY by default everywhere a full date is rendered in a picker) and F14 (AeroDateRangePicker must allow selecting a range within the SAME month — both endpoints clickable in either of the two visible months — while keeping the dual-month view and the PITFALL-06 single-`onRangeSelect` guarantee).

Purpose: F6 is a 🟣 design change (default may change visible behavior — explicitly accepted); F14 is a 🟣 design change relaxing the Phase 8 state machine. Both are additive (no broken public signature). Unblocks sign-off items 6 (range partial state) and the DD.MM.YYYY expectation across the pickers.
Output: pickers default to DD.MM.YYYY; same-month ranges selectable; PITFALL-06 intact.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-CONTEXT.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md

<interfaces>
<!-- Verified against source 2026-06-18: -->

All three date pickers currently default `formatter: (LocalDate)->String = { it.toString() }` (ISO):
  AeroDatePicker.kt:63, AeroDateRangePicker.kt:115, AeroDateTimePicker.kt:76 (LocalDateTime).
kotlinx.datetime LocalDate exposes `.dayOfMonth` (Int), `.monthNumber` (Int 1-12), `.year` (Int).
A DD.MM.YYYY string = `"%02d.%02d.%04d".format(date.dayOfMonth, date.monthNumber, date.year)`.

AeroDateRangePicker state machine (F14 target):
- `AeroDateRangeState = Idle | SelectingEnd(start) | Selected(start,end)` and pure `nextRangeState`
  already order start<=end on commit regardless of click order — so SAME-MONTH selection ALREADY works
  at the state-machine level (clicking 07.06 then 17.06 in the left month commits Selected(07.06,17.06)).
- The real F14 blocker is the MONTH NAVIGATION coupling, not the state machine: `leftMonth` drives both
  months and `rightMonth = leftMonth + 1`. The grids highlight via hlStart/hlEnd, and both `onDateSelected`
  route to the same `onDayClick`. Confirm by reading whether BOTH visible months actually accept clicks
  on every cell (they call the same onDayClick — they should). If they already do, F14 is satisfied by the
  state machine + showcase exercising it; the fix is verifying nothing forces "two adjacent months only"
  and that a click in the SAME month for both endpoints commits. Do NOT add a single-month fallback; keep
  the <560dp vertical stacking threshold (NEW-PICK-03) unchanged.
- CRITICAL: keep the SOLE `onRangeSelect(` call site guarded by the non-null commit from nextRangeState
  (PITFALL-06). Do not introduce a second call site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F6 — DD.MM.YYYY default formatter across the three date pickers</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt (formatter default at :63)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt (formatter default at :76, LocalDateTime)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt (formatter default at :115)
  </read_first>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt, library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt, library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt</files>
  <behavior>
    - formatAeroDate(LocalDate(2025, 6, 7)) == "07.06.2025"
    - formatAeroDate(LocalDate(2026, 12, 31)) == "31.12.2026"
    - AeroDateTimePicker default still renders the time part; only the date part switches to DD.MM.YYYY
  </behavior>
  <action>
    Add one shared internal helper (put it in AeroDatePicker.kt next to the existing `dateIsDisabled`/
    `todayLocalDate` helpers so all three pickers in the package can reference it):
    ```kotlin
    /** DD.MM.YYYY full-date format (F6 default). Calendar nav header stays month-name + year. */
    internal fun formatAeroDate(date: LocalDate): String =
        "%02d.%02d.%04d".format(date.dayOfMonth, date.monthNumber, date.year)
    ```
    Then change the three defaults:
    1. AeroDatePicker.kt: `formatter: (LocalDate) -> String = { formatAeroDate(it) }`
    2. AeroDateRangePicker.kt: `formatter: (LocalDate) -> String = { formatAeroDate(it) }`
    3. AeroDateTimePicker.kt: `formatter: (LocalDateTime) -> String = { ldt -> "${formatAeroDate(ldt.date)} ${"%02d:%02d".format(ldt.hour, ldt.minute)}" }`
       (date as DD.MM.YYYY + HH:MM; this is the trigger display only — the Apply-gate emit behavior is
       unchanged). If showSeconds-aware display is desired, keep it HH:MM here — the value Text in the
       showcase shows the raw `.toString()` for UAT.
    Update each `@param formatter` KDoc line from "(default ISO ...)" to "(default DD.MM.YYYY)".
    These are DEFAULT changes only — callers passing their own formatter are unaffected (additive,
    no signature break).

    Add a unit test for `formatAeroDate` (pure function) in
    library/src/test/kotlin/com/mordred/aero/components/pickers/ (create
    `AeroDateFormatTest.kt` if no suitable file exists) asserting the two <behavior> cases.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && grep -q "fun formatAeroDate" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt && grep -q "formatAeroDate(it)" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt && ./gradlew :library:test --tests "com.mordred.aero.components.pickers.*" -x generateIcons -q</automated>
  </verify>
  <acceptance_criteria>
    - `formatAeroDate` exists and returns DD.MM.YYYY (test green for 07.06.2025 and 31.12.2026)
    - AeroDatePicker.kt + AeroDateRangePicker.kt formatter default now calls `formatAeroDate(it)`
    - AeroDateTimePicker.kt date part rendered via `formatAeroDate(ldt.date)`
    - `grep -c "it.toString()" ` on the three picker files no longer matches a `formatter:` default line (the ISO default is gone)
    - All three public signatures unchanged (formatter param still present with a default)
    - `./gradlew :library:test --tests "com.mordred.aero.components.pickers.*"` exits 0
  </acceptance_criteria>
  <done>Shared formatAeroDate helper added; three pickers default to DD.MM.YYYY; pickers tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F14 — same-month range selection (verify/relax state machine, keep PITFALL-06)</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt (sealed AeroDateRangeState + nextRangeState + onDayClick single call site)
    - .planning/research/PITFALLS.md (PITFALL-06 — onRangeSelect once per completed range, never on start-only)
  </read_first>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt</files>
  <behavior>
    - nextRangeState(Idle, 2025-06-07) == (SelectingEnd(2025-06-07), null)            // start click: no emit
    - nextRangeState(SelectingEnd(2025-06-07), 2025-06-17) == (Selected(07,17), (07,17)) // SAME-month end: commit once
    - nextRangeState(SelectingEnd(2025-06-17), 2025-06-07) == (Selected(07,17), (07,17)) // reverse-order same-month: ordered + commit
  </behavior>
  <action>
    F14 is about same-month range selection in the dual-month view. The pure `nextRangeState` already
    orders start<=end and commits on the second click regardless of which visible month each click lands
    in — so SAME-month selection works at the logic level. Do the following:

    1. CONFIRM both visible months route every day click to the same `onDayClick` (the left grid's
       `onDateSelected = onDayClick` and the right grid's `onDateSelected = onDayClick`). They do today —
       keep it. This is what makes both endpoints clickable in EITHER visible month (F14 requirement).
    2. Ensure clicking the SAME month for both endpoints is not blocked by month navigation: nothing in
       the current code prevents it (both endpoints can be in `leftMonth`). Leave `leftMonth` driving both
       months and `rightMonth = leftMonth + 1` (no single-month fallback; <560dp stacking unchanged).
    3. Keep the SOLE `onRangeSelect(commit.first, commit.second)` call site guarded by `if (commit != null)`
       inside `onDayClick` (PITFALL-06). Do NOT add a second emit site.
    4. Add/extend a pure unit test for `nextRangeState` proving the three <behavior> cases, especially the
       same-month commit and the reverse-order ordering. Find the existing range-state test file under
       library/src/test/kotlin/com/mordred/aero/components/pickers/ (e.g. an AeroDateRangePicker*Test.kt
       from Phase 8); add the same-month assertions there. If none exists, create
       `AeroDateRangeStateTest.kt` testing `nextRangeState` directly (pure, no Compose).

    If — and only if — reading the code reveals an actual block on same-month selection (e.g. a guard
    that rejects `clicked` outside `rightMonth`, or month auto-advance on first click), remove that guard
    so both endpoints are clickable in either visible month. Document what you found in the SUMMARY.
    Do NOT change the public `AeroDateRangePicker` signature.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && test "$(grep -c "onRangeSelect(" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateRangePicker.kt)" -eq 1 && ./gradlew :library:test --tests "com.mordred.aero.components.pickers.*" -x generateIcons -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onRangeSelect(" AeroDateRangePicker.kt` returns EXACTLY 1 (PITFALL-06; the verify asserts `-eq 1` and fails on 0 or >=2). The sole match is the guarded invocation `onRangeSelect(...)`; the param declaration line `onRangeSelect:` has no trailing `(` so it does not count. A second call site (count 2) FAILS the build.
    - A pure `nextRangeState` test asserts the same-month commit `(SelectingEnd(07-06) , 17-06) -> Selected(07,17), (07,17)` and the reverse-order ordering
    - Both calendar grids still route `onDateSelected = onDayClick` (grep shows two `onDateSelected = onDayClick`)
    - `<560.dp` stacking threshold unchanged (grep `560.dp` still present)
    - `AeroDateRangePicker` public signature unchanged
    - `./gradlew :library:test --tests "com.mordred.aero.components.pickers.*"` exits 0
  </acceptance_criteria>
  <done>Same-month range selection verified/enabled in the dual-month view; single onRangeSelect guard intact; tests green.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:test --tests "com.mordred.aero.components.pickers.*"` green (formatAeroDate + nextRangeState same-month).
- `./gradlew :library:compileKotlin -x generateIcons` exits 0.
- F6: three pickers default to DD.MM.YYYY (date part).
- F14: same-month range selectable; dual-month view kept; <560dp stacking kept.
- PITFALL-06: exactly one onRangeSelect invocation, guarded by non-null commit.
- No public signature changed.
</verification>

<success_criteria>
- F6 resolved: DD.MM.YYYY is the default full-date rendering across the pickers (calendar nav header unchanged).
- F14 resolved: a same-month range (e.g. 07.06–17.06) is selectable with both endpoints clickable in either visible month.
- DataTable AOS-date column + showcase value-Text DD.MM.YYYY display is handled in 11-10 (showcase).
</success_criteria>

<output>
After completion, create `.planning/phases/11-showcase-v2-0-visual-sign-off/11-08-SUMMARY.md` documenting the formatAeroDate helper, the three default changes, and the F14 finding (state machine already allowed same-month, or a guard removed), plus which sign-off rows it affects (6 + the DD.MM.YYYY expectation).
</output>
