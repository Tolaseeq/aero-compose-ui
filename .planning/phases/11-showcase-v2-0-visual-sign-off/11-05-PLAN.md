---
phase: 11-showcase-v2-0-visual-sign-off
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md
autonomous: false
requirements: [SHW-10]

must_haves:
  truths:
    - "W11-01 grep gate (transparent = true) returns 0 hits across all new/touched v2.0 files"
    - "W11-02 grep gate (AeroScrollArea in datatable package) returns 0 hits"
    - "11-SIGNOFF.md records all 16 checklist items x 3 themes (48 cells) with PASS/FAIL"
    - "All 48 sign-off cells are PASS before Phase 11 is marked complete (formal v2.0 milestone gate)"
  artifacts:
    - path: ".planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md"
      provides: "Formal 16-item x 3-theme sign-off record — the v2.0 milestone gate artifact"
      contains: "transparent = true"
  key_links:
    - from: "11-SIGNOFF.md"
      to: "16-item checklist (PITFALLS.md)"
      via: "one row per checklist item x three theme columns"
      pattern: "AeroBlue"
---

<objective>
Run the formal v2.0 milestone sign-off gate: execute the two automated grep gates (W11-01 `transparent = true` = 0; W11-02 `AeroScrollArea` in datatable package = 0), then drive the human-verified 14-item visual/interactive checklist across all three themes (AeroBlue / AeroDark / Classic), recording all 16 items x 3 themes (48 cells) in `11-SIGNOFF.md`. Phase 11 is NOT complete until every cell is PASS (SHW-10).

Purpose: The 16-item "looks done but isn't" checklist from PITFALLS.md is the formal gate for the entire v2.0 milestone. This plan produces the durable sign-off artifact and blocks completion on any FAIL.
Output: `11-SIGNOFF.md` with all 48 cells PASS.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-CONTEXT.md
@.planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- 11-RESEARCH.md section "The 16-Item Checklist" has the verbatim items + per-item verification steps + relevant section. -->
<!-- 11-RESEARCH.md section "Grep Gates" has the exact grep commands (Bash + PowerShell). -->

The 16 items map to sections as follows (from research):
- Items 1,2,3,4 -> DataSection (virtualization, selection-after-sort, column resize, TreeView lazy callback)
- Items 5,6,7,13 -> PickersSection (DatePicker popup position, DateRangePicker partial state, ColorPicker round-trip, AeroDark disabled cells)
- Item 8 -> RangeSection (RangeSlider thumb overlap)
- Items 9,10,11,12 -> LayoutSection (Accordion single-mode, SplitPane clamp, Sidebar reflow, Wizard validation)
- Item 14 -> drag response across DataSection + PickersSection + RangeSection
- Items 15,16 -> automated grep gates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated grep gates (W11-01, W11-02) and scaffold 11-SIGNOFF.md</name>
  <read_first>
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md (section "Grep Gates" exact commands; section "The 16-Item Checklist" verbatim item text + verify steps)
  </read_first>
  <files>.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md</files>
  <action>
    1. Run the W11-01 grep gate (item 15) in the project root. The scratch files are deleted by Plan 04, so this should be clean:
    ```bash
    grep -rn "transparent = true" \
      library/src/main/kotlin/com/mordred/aero/components/pickers/ \
      library/src/main/kotlin/com/mordred/aero/components/datatable/ \
      library/src/main/kotlin/com/mordred/aero/components/layout/ \
      library/src/main/kotlin/com/mordred/aero/components/range/ \
      showcase/src/main/kotlin/com/mordred/showcase/sections/DataSection.kt \
      showcase/src/main/kotlin/com/mordred/showcase/sections/PickersSection.kt \
      showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt
    ```
    Expected: 0 matches. Any match is a BLOCKER — record FAIL and report (do not proceed to visual sign-off).

    2. Run the W11-02 grep gate (item 16):
    ```bash
    grep -rn "AeroScrollArea" library/src/main/kotlin/com/mordred/aero/components/datatable/
    ```
    Expected: 0 matches. Any match is a BLOCKER.

    3. Create `11-SIGNOFF.md` with a results table: one row per checklist item (1-16), three theme columns (AeroBlue / AeroDark / Classic), plus a "How verified" column. Reproduce the verbatim item text from 11-RESEARCH.md section "The 16-Item Checklist". Pre-fill items 15 and 16 with the grep-gate results from steps 1-2 (PASS across all three theme columns since grep is theme-independent — annotate "automated, theme-independent"). Leave items 1-14 cells as `PENDING` (filled by the human-verify checkpoint in Task 2).

    Header of the file must include: phase id, date, the launch command `./gradlew :showcase:run`, and a note that Phase 11 is NOT complete until all 48 cells are PASS.
  </action>
  <verify>
    <automated>! grep -rq "transparent = true" library/src/main/kotlin/com/mordred/aero/components/pickers/ library/src/main/kotlin/com/mordred/aero/components/datatable/ library/src/main/kotlin/com/mordred/aero/components/layout/ library/src/main/kotlin/com/mordred/aero/components/range/ showcase/src/main/kotlin/com/mordred/showcase/sections/ && ! grep -rq "AeroScrollArea" library/src/main/kotlin/com/mordred/aero/components/datatable/</automated>
  </verify>
  <acceptance_criteria>
    - W11-01 gate: `grep -rn "transparent = true"` across the listed picker/datatable/layout/range library dirs + the three new showcase section files returns 0 hits
    - W11-02 gate: `grep -rn "AeroScrollArea" library/src/main/kotlin/com/mordred/aero/components/datatable/` returns 0 hits
    - File `.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md` exists
    - `grep -c "AeroBlue" 11-SIGNOFF.md` returns at least 1 (theme columns present)
    - 11-SIGNOFF.md contains 16 checklist-item rows (one per item 1-16)
    - Items 15 and 16 rows show PASS (grep gates passed); items 1-14 show PENDING
  </acceptance_criteria>
  <done>Both grep gates pass (0 hits each); 11-SIGNOFF.md scaffolded with all 16 item rows x 3 theme columns; items 15/16 PASS, items 1-14 PENDING.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Three-theme visual/interactive sign-off (14 items x 3 themes)</name>
  <files>.planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md</files>
  <read_first>
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md (scaffold from Task 1)
    - .planning/phases/11-showcase-v2-0-visual-sign-off/11-RESEARCH.md (section "The 16-Item Checklist" — per-item verification steps)
  </read_first>
  <action>
    This is a human-verify checkpoint. Launch `./gradlew :showcase:run`, then walk the human through the 14 visual/interactive checklist items (items 1-14) across all three themes using the ThemeSwitcher (AeroBlue, AeroDark, Classic), recording PASS/FAIL for each of the 48 cells in 11-SIGNOFF.md. The detailed per-item verification steps are in the how-to-verify block below. Do NOT edit any component source from this plan — Phase 11 is additive-only; record any FAIL and report it for a gap-closure decision.
  </action>
  <what-built>
    All three new showcase sections (DataSection, PickersSection, LayoutSection) plus the extended RangeSection are live in `ShowcaseApp.kt`; scratch removed; both grep gates (items 15, 16) passed automatically in Task 1. The 11-SIGNOFF.md scaffold is ready with items 1-14 marked PENDING.
  </what-built>
  <how-to-verify>
    Launch the showcase: `./gradlew :showcase:run`

    For EACH of the three themes (use the ThemeSwitcher at the top of the showcase: AeroBlue, then AeroDark, then Classic), verify all 14 visual/interactive items. Record PASS/FAIL per item per theme in 11-SIGNOFF.md. AeroDark is the lowest-contrast theme — pay closest attention there (item 13, and four-state row colors in item 2).

    DataSection:
    1. Virtualization — table shows ~10-15 rows visible (not all 100); scroll renders new rows.
    2. Selection after sort — click "AOS Date" header to sort asc, click row SAT-050, click "AOS Date" again (desc); SAT-050 keeps its highlight at the new position. (In AeroDark also confirm all four row states normal/hover/selected/selected+hover are distinguishable — PITFALL-10.)
    3. Column resize — drag the "Name" column splitter right past "NORAD ID"; other columns shrink but none vanish; drag left until a column hits its 40dp minimum.
    4. TreeView lazy callback — expand a ground-station node, scroll it off screen, scroll back; console shows only ONE "onExpand fired for: X" line for that node.

    PickersSection:
    5. DatePicker popup position — resize window to ~1024dp wide; open the DatePicker (right half); calendar right-aligns and does not clip.
    6. DateRangePicker partial state — open the range picker, click one date, dismiss; the value Text still shows the placeholder ("—"), not a committed range.
    7. ColorPicker round-trip — open AeroColorPickerButton, set to #FF0000, drag saturation to 50% then back to 100%; HEX value Text reads #FF0000 (no drift).
    13. AeroDark disabled cells — in AeroDark, open DatePicker/DateRangePicker to a month with disabled (out-of-range) dates; disabled cells are visible (grey via labelText token), not invisible.

    RangeSection:
    8. RangeSlider thumb overlap — drag the start thumb to meet the end thumb, release, drag again; both thumbs remain individually reachable.

    LayoutSection:
    9. Accordion single mode — in the single-mode column, open section A, then open section B; A closes.
    10. SplitPane clamp — drag the divider to each edge (horizontal and vertical demos); both panes stay >= 48dp.
    11. Sidebar adjacent reflow — click "Toggle mode"; sidebar animates Expanded->Collapsed->Hidden; adjacent content box expands to fill reclaimed space.
    12. StepperWizard validation — on step 1 (Identifier), Tab through the field without clicking Next (no validation feedback); then click Next with a blank field — Next is blocked.

    Drag response (all drag components, across sections):
    14. HSV square (ColorPicker), RangeSlider thumbs, and DataTable column splitter all respond on the FIRST mouse movement (no ~18px slop delay).

    Update 11-SIGNOFF.md: mark each of the 48 cells PASS or FAIL with a short note. If any cell is FAIL, that is a component-level bug (NOT fixable here — Phase 11 is additive only); record it and report so a gap-closure decision can be made.
  </how-to-verify>
  <verify>
    <automated>test "$(grep -c PENDING .planning/phases/11-showcase-v2-0-visual-sign-off/11-SIGNOFF.md)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 11-SIGNOFF.md has all 48 cells filled (16 items x 3 themes), none PENDING
    - Every cell is PASS (the formal gate); any FAIL blocks Phase 11 completion and is reported for gap-closure
  </acceptance_criteria>
  <done>All 48 cells in 11-SIGNOFF.md are filled (no PENDING) and every cell is PASS.</done>
  <resume-signal>Type "approved" if all 48 cells PASS, or describe which items/themes FAILED.</resume-signal>
</task>

</tasks>

<verification>
- W11-01 grep gate: `transparent = true` = 0 hits across all new/touched v2.0 files.
- W11-02 grep gate: `AeroScrollArea` in datatable package = 0 hits.
- 11-SIGNOFF.md: all 16 items x 3 themes recorded; all 48 cells PASS.
</verification>

<success_criteria>
- SHW-10 satisfied: full 16-item checklist passes eyes-on in all three themes; both grep gates clean; 11-SIGNOFF.md is the durable milestone-gate artifact.
- Phase 11 is complete only when every one of the 48 cells reads PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/11-showcase-v2-0-visual-sign-off/11-05-SUMMARY.md` summarizing the sign-off result and linking 11-SIGNOFF.md.
</output>
