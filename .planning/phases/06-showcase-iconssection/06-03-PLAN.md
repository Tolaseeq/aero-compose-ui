---
phase: 06-showcase-iconssection
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - .planning/phases/06-showcase-iconssection/06-VERIFICATION.md
autonomous: false
requirements: [SHW-04, SHW-05, SHW-06]
must_haves:
  truths:
    - "User has launched `./gradlew :showcase:run` and visually inspected the showcase in AeroBlue, AeroDark, and Classic themes"
    - "All eight checkpoint items below pass eyes-on for each theme (and the AeroNumberSpinner regression check passes once on AeroDark)"
    - "User has typed `approved` (or filed a deviation) and the verdict is recorded in `06-VERIFICATION.md`"
    - "Approval closes Phase 6 AND closes the v1.1 milestone visual sign-off"
  artifacts:
    - path: ".planning/phases/06-showcase-iconssection/06-VERIFICATION.md"
      provides: "Three-theme checkpoint record with per-item observations and final verdict"
      contains: "approved"
  key_links:
    - from: "Plan 01 IconsSection deliverable"
      to: "this checkpoint"
      via: "the running showcase loads IconsSection between Foundation and Buttons sections"
      pattern: "n/a — visual gate"
    - from: "Plan 02 ButtonsSection migration"
      to: "this checkpoint"
      via: "the running showcase shows three Phosphor caret/X glyphs in the AeroIconButton row instead of text"
      pattern: "n/a — visual gate"
---

<objective>
Run the formal three-theme visual checkpoint that signs off Phase 6 AND the entire v1.1 Icon System milestone. The checkpoint is eyes-on inspection of the running showcase in AeroBlue, AeroDark, and Classic themes against a fixed list of checkpoint items.

Purpose: SHW-04 / SHW-05 / SHW-06 each have a manual visual component (per VALIDATION.md "Manual-Only Verifications"); CONTEXT.md locks this as a single ceremony at the end of Phase 6. No grep, no compile gate replaces this — the user must observe the running app.
Output: `06-VERIFICATION.md` updated with per-item observations and final verdict (`approved` or deviation list).
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-showcase-iconssection/06-CONTEXT.md
@.planning/phases/06-showcase-iconssection/06-VALIDATION.md
@.planning/phases/06-showcase-iconssection/06-01-PLAN.md
@.planning/phases/06-showcase-iconssection/06-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm Plan 01 + Plan 02 grep gates green and run compile + library tests</name>
  <files>.planning/phases/06-showcase-iconssection/06-VERIFICATION.md</files>

  <read_first>
    - .planning/phases/06-showcase-iconssection/06-VALIDATION.md (section "Per-Task Verification Map" + "Sampling Rate" → "Phase gate")
    - .planning/phases/06-showcase-iconssection/06-01-PLAN.md (acceptance_criteria for Tasks 1+2)
    - .planning/phases/06-showcase-iconssection/06-02-PLAN.md (acceptance_criteria for Task 1)
  </read_first>

  <action>
Run the full automated gate before invoking the manual checkpoint. This catches regressions cheaply.

**Step 1 — Run the SHW-04 / SHW-05 grep suite against IconsSection.kt:**

```bash
F=showcase/src/main/kotlin/com/mordred/showcase/sections/IconsSection.kt
grep -c "IconEntry(" "$F"                                         # must be 138
grep -E "GridCells.Adaptive\(80.dp\)" "$F"                         # must hit
grep -E "\.height\(400.dp\)" "$F"                                  # must hit
grep -E "Modifier.size\(24.dp\)" "$F"                              # must hit
grep "tint = colors.onSurface" "$F"                                # must hit
grep -E "AeroSearchField\(" "$F"                                   # must hit
grep "Search icons" "$F"                                           # must hit
grep "ignoreCase = true" "$F"                                      # must hit
grep "No icons match" "$F"                                         # must hit
grep "Modifier.glassSurface(cornerRadius = 6.dp)" "$F"             # must hit
grep "rememberCoroutineScope" "$F"                                 # must hit
grep "LocalClipboardManager" "$F"                                  # must hit
```

**Step 2 — Run the SHW-04 wiring grep against ShowcaseApp.kt:**

```bash
G=showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt
grep -n "import com.mordred.showcase.sections.IconsSection" "$G"   # must hit
grep -n "IconsSection(toastState = toastState)" "$G"               # must hit
awk '/FoundationSection\(\)/{f=NR} /IconsSection\(toastState/{i=NR} /ButtonsSection\(\)/{b=NR} END{print (f>0 && i>f && b>i) ? "OK" : "FAIL"}' "$G"   # must print OK
```

**Step 3 — Run the SHW-06 grep suite against ButtonsSection.kt:**

```bash
grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/                        # must return 0 hits
H=showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt
grep -E "Icon\(AeroIcons\.(CaretUp|CaretDown|X)" "$H"              # must return 3 hits
grep -E 'Text\("(B|I|U|S)"' "$H"                                   # must return 4 hits (locked exclusion)
```

**Step 4 — Compile + test gate:**

```bash
./gradlew :showcase:compileKotlin   # must exit 0
./gradlew :library:test             # must exit 0 (regression sanity; no library changes)
```

**Step 5 — Initialize `06-VERIFICATION.md`** with frontmatter and a "Pre-flight" section recording the results of Steps 1-4. Use this skeleton (do NOT add iteration-narrative comments):

```markdown
---
phase: 06-showcase-iconssection
verified: <ISO date>
verdict: pending
checkpoint_type: three-theme-visual
v1_1_milestone_signoff: pending
---

# Phase 6 — Verification

## Pre-flight (automated)

- [ ] IconsSection.kt grep suite: <PASS / FAIL with offending command>
- [ ] ShowcaseApp.kt wiring grep: <PASS / FAIL>
- [ ] ButtonsSection.kt SHW-06 grep: <PASS / FAIL>
- [ ] `./gradlew :showcase:compileKotlin`: <PASS / FAIL>
- [ ] `./gradlew :library:test`: <PASS / FAIL>

## Three-theme visual checkpoint

(Filled by Task 2.)
```

If any pre-flight step fails, do NOT proceed to the manual checkpoint. File a deviation referencing the failing command and stop the plan.
  </action>

  <verify>
    <automated>cd C:/1A_WORK/ui_lib &amp;&amp; ./gradlew :showcase:compileKotlin &amp;&amp; ./gradlew :library:test &amp;&amp; test "$(grep -rcn 'Text("▲\|▼\|×\|✕")' showcase/src/ | wc -l)" -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - All twelve grep commands in Step 1 hit (or return the expected count for `IconEntry(` = 138).
    - The `awk` check in Step 2 prints `OK`.
    - `grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/` returns 0 hits.
    - `grep -E "Icon\(AeroIcons\.(CaretUp|CaretDown|X)" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 3 hits.
    - `./gradlew :showcase:compileKotlin` exits 0.
    - `./gradlew :library:test` exits 0.
    - `06-VERIFICATION.md` exists with the Pre-flight section filled in and every checkbox marked PASS.
  </acceptance_criteria>

  <done>
    All automated gates green; `06-VERIFICATION.md` initialized with a PASS Pre-flight section; the plan is cleared to invoke the manual checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Three-theme visual checkpoint — AeroBlue + AeroDark + Classic (v1.1 milestone sign-off gate)</name>

  <what-built>
    Phase 6 has landed two showcase changes (Plan 01 + Plan 02) and the showcase compiles cleanly:

    1. **`IconsSection`** — new section between `FoundationSection` and `ButtonsSection`, displaying all 138 `AeroIcons.*` constants in a `LazyVerticalGrid(GridCells.Adaptive(80.dp))` of 80dp-wide × 88dp-tall cells inside a 400dp-bounded scroll viewport. Each cell shows a 24dp Phosphor glyph with `tint = colors.onSurface` and a single-line label below. An `AeroSearchField` at the top filters by case-insensitive substring on the PascalCase identifier. An empty filter result shows `No icons match '<query>'`. Clicking a cell copies `AeroIcons.<Name>` to the clipboard and shows a toast `Copied AeroIcons.<Name>`.

    2. **`ButtonsSection` migration** — the AeroIconButton row (BTN-03) now uses `Icon(AeroIcons.CaretUp / CaretDown / X)` at 14dp instead of `Text("▲" / "▼" / "×")`. The AeroToolbar B/I/U/S letter row is intentionally unchanged (locked v1.1 exclusion).

    The compile + library tests + grep gates (recorded in `06-VERIFICATION.md` Pre-flight) all passed before this checkpoint was invoked.
  </what-built>

  <how-to-verify>
    Launch the showcase and inspect each of the three themes against the eight checkpoint items below. Single ceremony — toggle the `ThemeSwitcher` to walk all three.

    **Launch:**

    ```bash
    cd C:/1A_WORK/ui_lib
    ./gradlew :showcase:run
    ```

    The showcase opens at the AeroBlue theme (default).

    ---

    **Theme 1 of 3 — AeroBlue (default-on-launch):**

    Scroll to `Icons` (the new section, between Foundation and Buttons). Confirm:

    1. **All 138 cells visible after scrolling the bounded grid.** The grid scrolls internally inside its 400dp viewport; you should see multiple full rows of 80dp-wide cells with no obvious gaps. Inline match-count below the search field reads `138 of 138` when no query is entered.
    2. **Spot-check six representative icons render acceptably:** `X`, `CaretDown`, `MagnifyingGlass`, `FrameCorners`, `Warning`, `Square`. Each must show a recognizable Phosphor Regular glyph at 24dp with rounded stroke caps (not a question-mark, not a missing-glyph rectangle). FrameCorners and Square are the visually-thinnest in the set; confirm their stroke is visible against the glassy AeroBlue background.
    3. **Search behavior — `caret` filters to 4 entries.** Type `caret` into the search field. The grid should re-render to show exactly `CaretDown`, `CaretLeft`, `CaretRight`, `CaretUp` (alphabetical, four cells). Match-count reads `4 of 138`.
    4. **Clear the search field.** Use the `AeroSearchField`'s built-in clear button (the `X` glyph on the right side of the field). The grid restores to all 138 entries; match-count reads `138 of 138`.
    5. **Empty state — `xyzzy` shows the not-found message.** Type `xyzzy`. The grid is replaced by a centered `Text("No icons match 'xyzzy'")` in body typography. Match-count reads `0 of 138`. Clear again.
    6. **Click-to-copy — click any icon cell** (try `MagnifyingGlass` for legibility). A toast appears at the bottom-right reading `Copied AeroIcons.MagnifyingGlass`. Open any text editor (e.g., Notepad) and paste — the literal text `AeroIcons.MagnifyingGlass` should appear.
    7. **`ButtonsSection` AeroIconButton row** — scroll up to the Buttons section. The third row (label "AeroIconButton") shows three buttons containing real Phosphor glyphs: an upward caret, a downward caret, and an X (the third button is also disabled). NO `▲`, `▼`, or `×` text characters anywhere.
    8. **`AeroToolbar` row preserved** — the fourth row (label "AeroToolbar") still shows letters `B I U S` (locked exclusion confirmed).

    ---

    **Theme 2 of 3 — AeroDark:**

    Click the `ThemeSwitcher` segmented control at the top of the showcase to switch to **AeroDark**. Confirm:

    1. **All 138 cells remain visible — no black-on-black, no invisible glyphs.** `colors.onSurface` resolves to a light-on-dark token in AeroDark; every icon should read clearly.
    2. **Re-spot-check the six representative icons** (`X`, `CaretDown`, `MagnifyingGlass`, `FrameCorners`, `Warning`, `Square`). Pay particular attention to `FrameCorners` and `Square` — these are the thin-stroke chrome icons RESEARCH.md flagged for visual-weight concern at 24dp on AeroDark glass. They must read clearly without straining; if either visibly disappears or becomes a faint outline you can barely make out, file a deviation per gsd-executor protocol.
    3. **AeroNumberSpinner regression spot-check (carry-forward from Phase 5, NOT a re-decision):** scroll to `InputSection` → find the `AeroNumberSpinner` demo → set/observe its disabled state → confirm the up/down caret icons remain visible at the 12dp render in the 14dp slot. Phase 5 already approved this; this is a final regression confirmation as part of the v1.1 milestone gate.
    4. **`ButtonsSection` row glyphs remain visible on AeroDark** — three Phosphor caret/X icons read clearly in the AeroIconButton row.

    ---

    **Theme 3 of 3 — Classic:**

    Click `ThemeSwitcher` again to switch to **Classic**. Confirm:

    1. **All 138 cells render with correct tint and Phosphor stroke contrast at 24dp.** Spot-check the same six representative icons (`X`, `CaretDown`, `MagnifyingGlass`, `FrameCorners`, `Warning`, `Square`).
    2. **`ButtonsSection` row glyphs remain visible on Classic** — three Phosphor caret/X icons read clearly.

    Briefly re-trigger one search query (`caret` → 4 results) and one click-to-copy in Classic to confirm the interactive paths work in this theme as well.

    ---

    **Recording the verdict:**

    Open `.planning/phases/06-showcase-iconssection/06-VERIFICATION.md` and append a `## Three-theme visual checkpoint` section with one subsection per theme (`### AeroBlue`, `### AeroDark`, `### Classic`). For each theme, list the eight (AeroBlue) / four (AeroDark — with the AeroNumberSpinner spot-check) / two (Classic) items above and mark each PASS / FAIL with a one-line note. Then update the frontmatter:

    - On full pass: set `verdict: approved` and `v1_1_milestone_signoff: approved`. Add a final line `## Verdict\n\napproved`.
    - On any FAIL: set `verdict: deviation`. Document the failing item, the theme, and the observed behavior. Do NOT mark `v1_1_milestone_signoff: approved`.

    Comment hygiene: do NOT add iteration-narrative notes ("after fix", "round 2"). Only describe observed state.
  </how-to-verify>

  <resume-signal>
    Type `approved` to close Phase 6 + the v1.1 milestone visual sign-off, or describe the regression for deviation handling.
  </resume-signal>

  <files>.planning/phases/06-showcase-iconssection/06-VERIFICATION.md</files>

  <action>
This task is a `checkpoint:human-verify` gate; the user (not Claude) performs the visual inspection. The detailed protocol is in `<what-built>` and `<how-to-verify>` above.

Claude's role at this task: launch the showcase via `./gradlew :showcase:run` (background), then BLOCK and wait for the user to type `approved` or describe a regression. Claude transcribes the user's per-item observations into `06-VERIFICATION.md` under a `## Three-theme visual checkpoint` section with three subsections (`### AeroBlue`, `### AeroDark`, `### Classic`), then finalizes the frontmatter:
- On user `approved`: set `verdict: approved` and `v1_1_milestone_signoff: approved`.
- On a deviation report: set `verdict: deviation`; record the failing theme + item + observed behavior; do NOT set `v1_1_milestone_signoff: approved`.

Comment hygiene: do NOT add iteration-narrative notes ("after fix", "round 2") to `06-VERIFICATION.md` — only describe observed state.
  </action>

  <verify>
    <automated>cd C:/1A_WORK/ui_lib &amp;&amp; grep -E "^verdict: approved" .planning/phases/06-showcase-iconssection/06-VERIFICATION.md &amp;&amp; grep -E "^v1_1_milestone_signoff: approved" .planning/phases/06-showcase-iconssection/06-VERIFICATION.md</automated>
  </verify>

  <done>
    User has typed `approved` (or filed a deviation) inline. `06-VERIFICATION.md` frontmatter `verdict` is set to `approved` (success path) or `deviation` (gap path); on the success path `v1_1_milestone_signoff: approved` is also set. The `## Three-theme visual checkpoint` section contains per-theme observations covering all checkpoint items.
  </done>
</task>

</tasks>

<verification>
The plan succeeds if:
- Pre-flight Task 1 produced an all-PASS section in `06-VERIFICATION.md`.
- The user has typed `approved` (or filed a deviation) at Task 2, and `06-VERIFICATION.md` has been updated with per-theme observations and a final verdict.
- `./gradlew :showcase:compileKotlin` and `./gradlew :library:test` both exit 0 at the moment of sign-off.
</verification>

<success_criteria>
- `06-VERIFICATION.md` exists with frontmatter `verdict: approved` and `v1_1_milestone_signoff: approved`, AND
- All three theme subsections record PASS for every checkpoint item.

If the user files a deviation instead, the plan reaches its checkpoint but does not satisfy success criteria — gap closure (Plan 06-04 onward) is then created via `/gsd:plan-phase 6 --gaps`.
</success_criteria>

<output>
After approval, create `.planning/phases/06-showcase-iconssection/06-03-SUMMARY.md` and the phase-level `.planning/phases/06-showcase-iconssection/06-SUMMARY.md` per the standard summary template. Mark Phase 6 + v1.1 milestone complete in STATE.md, ROADMAP.md, and REQUIREMENTS.md (SHW-04, SHW-05, SHW-06 → Complete).
</output>
