---
phase: 06-showcase-iconssection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt
autonomous: true
requirements: [SHW-06]
must_haves:
  truths:
    - "ButtonsSection's AeroIconButton row renders three real Phosphor `Icon(AeroIcons.*)` glyphs (CaretUp, CaretDown, X) instead of `Text(\"▲\"/\"▼\"/\"×\")`"
    - "No text glyphs `▲ ▼ × ✕` remain anywhere under `showcase/src/`"
    - "Each migrated Icon call uses `tint = colors.onSurface` and `Modifier.size(14.dp)` matching the Phase 5 affordance-icon convention"
    - "AeroToolbar `B I U S` letter glyphs (lines 58-62) are untouched (locked v1.1 exclusion)"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt"
      provides: "ButtonsSection with AeroIconButton row migrated to AeroIcons.{CaretUp, CaretDown, X}"
      contains: "Icon(AeroIcons.CaretUp"
  key_links:
    - from: "showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt AeroIconButton content"
      to: "com.mordred.aero.icons.AeroIcons + internal extension properties"
      via: "Icon(AeroIcons.{CaretUp|CaretDown|X}, contentDescription = null, tint = colors.onSurface, modifier = Modifier.size(14.dp))"
      pattern: "Icon\\(AeroIcons\\.(CaretUp|CaretDown|X)"
---

<objective>
Migrate the three text-glyph mockups in `ButtonsSection.kt`'s AeroIconButton row (`▲ ▼ ×` at lines 50-52) to real `Icon(AeroIcons.{CaretUp, CaretDown, X})` calls — a mechanical edit that closes SHW-06 and removes the last text-glyph stragglers from showcase source.

Purpose: SHW-06 requires `grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/` to return 0 hits. Currently returns 3.
Output: One file modified, three line edits + import additions; ~6 lines of net change.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-showcase-iconssection/06-CONTEXT.md
@.planning/phases/06-showcase-iconssection/06-RESEARCH.md
@.planning/phases/06-showcase-iconssection/06-VALIDATION.md

<interfaces>
<!-- Public APIs the executor will consume from :library. Verified by RESEARCH.md sources. -->

From com.mordred.aero.icons.AeroIcons + extension property files:
```kotlin
public val AeroIcons.CaretUp: ImageVector       // file-scope extension property in internal/CaretUp.kt
public val AeroIcons.CaretDown: ImageVector     // file-scope extension property in internal/CaretDown.kt
public val AeroIcons.X: ImageVector             // file-scope extension property in internal/X.kt
// IMPORTANT: each requires its own `import com.mordred.aero.icons.internal.<Name>` alongside
//            `import com.mordred.aero.icons.AeroIcons` — Phase 5 lock (STATE.md).
```

From androidx.compose.material3:
```kotlin
@Composable
public fun Icon(
    imageVector: ImageVector,
    contentDescription: String?,
    modifier: Modifier = Modifier,
    tint: Color = LocalContentColor.current,  // ALWAYS pass explicit tint; AeroTheme does not bridge LocalContentColor
)
```

From androidx.compose.foundation.layout:
```kotlin
public fun Modifier.size(size: Dp): Modifier
```

From com.mordred.aero.components.buttons.AeroIconButton (already imported in current file):
```kotlin
@Composable
public fun AeroIconButton(
    onClick: () -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
    content: @Composable () -> Unit,
)
```
</interfaces>

<buttonssection_current_lines_48_to_53>
<!-- Current ButtonsSection.kt lines 48-53 — these are the lines you will modify. -->

```kotlin
        // Row 3: AeroIconButton (BTN-03) — three icon buttons, one disabled
        SectionRow(label = "AeroIconButton") {
            AeroIconButton(onClick = {}) { Text("▲", color = colors.onSurface) } // ▲
            AeroIconButton(onClick = {}) { Text("▼", color = colors.onSurface) } // ▼
            AeroIconButton(onClick = {}, enabled = false) { Text("×", color = colors.onSurface) } // ×
        }
```

Lines 56-64 (AeroToolbar with `B I U S` letter glyphs) are NOT touched — locked v1.1 exclusion (CONTEXT.md "ButtonsSection migration (SHW-06)" → "AeroToolbar row's text-letter glyphs (B I U S lines 58-62) are NOT migrated").
</buttonssection_current_lines_48_to_53>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ButtonsSection.kt lines 50-52 Text glyphs with Icon(AeroIcons.*) calls + add imports</name>
  <files>showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt</files>

  <read_first>
    <!-- MANDATORY: load these before editing. -->
    - showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt — current file (you will edit; need exact line content)
    - .planning/phases/06-showcase-iconssection/06-CONTEXT.md (section "ButtonsSection migration (SHW-06)") — exact replacement spec; locks the BIUS exclusion
    - .planning/phases/06-showcase-iconssection/06-RESEARCH.md (section "Pattern 2: Extension-property import requirement") — extension-property import pattern (Phase 5 lock)
    - .planning/phases/06-showcase-iconssection/06-VALIDATION.md (section "Per-Task Verification Map" rows for plan 02) — exact grep commands this task must satisfy
  </read_first>

  <action>
Edit `showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` with two surgical changes.

**Edit 1 — add four imports** to the existing import block (after line 12 `import androidx.compose.ui.unit.dp` and before line 13's `import com.mordred.aero...`). Maintain alphabetical sub-grouping of `androidx.compose.*` then `com.mordred.aero.*`:

Insert the following imports (alphabetical placement within their groups):

```kotlin
import androidx.compose.foundation.layout.size
import androidx.compose.material3.Icon
// ... existing androidx.* imports remain ...
// then in the com.mordred.aero block:
import com.mordred.aero.icons.AeroIcons
import com.mordred.aero.icons.`internal`.CaretDown
import com.mordred.aero.icons.`internal`.CaretUp
import com.mordred.aero.icons.`internal`.X
```

Concretely:
- `import androidx.compose.foundation.layout.size` goes after `import androidx.compose.foundation.layout.RowScope` (line 6) and before `import androidx.compose.foundation.layout.width` (line 7) — alphabetical: `Row`, `RowScope`, `size`, `width`.
- `import androidx.compose.material3.Icon` goes immediately above `import androidx.compose.material3.Text` (line 8).
- `import com.mordred.aero.icons.AeroIcons`, `internal.CaretDown`, `internal.CaretUp`, `internal.X` — insert as a contiguous block between the `com.mordred.aero.components.buttons.*` imports (lines 13-17) and `import com.mordred.aero.theme.AeroTheme` (line 18). Maintain alphabetical order within the inserted block (`AeroIcons` precedes `internal.CaretDown` etc., and the three internals are alphabetized: `CaretDown`, `CaretUp`, `X`).

**Edit 2 — replace the three Text glyphs at the AeroIconButton row.** Current code at lines 50-52 is:

```kotlin
        SectionRow(label = "AeroIconButton") {
            AeroIconButton(onClick = {}) { Text("▲", color = colors.onSurface) } // ▲
            AeroIconButton(onClick = {}) { Text("▼", color = colors.onSurface) } // ▼
            AeroIconButton(onClick = {}, enabled = false) { Text("×", color = colors.onSurface) } // ×
        }
```

Replace with:

```kotlin
        SectionRow(label = "AeroIconButton") {
            AeroIconButton(onClick = {}) {
                Icon(AeroIcons.CaretUp, contentDescription = null, tint = colors.onSurface, modifier = Modifier.size(14.dp))
            }
            AeroIconButton(onClick = {}) {
                Icon(AeroIcons.CaretDown, contentDescription = null, tint = colors.onSurface, modifier = Modifier.size(14.dp))
            }
            AeroIconButton(onClick = {}, enabled = false) {
                Icon(AeroIcons.X, contentDescription = null, tint = colors.onSurface, modifier = Modifier.size(14.dp))
            }
        }
```

The trailing `// ▲` `// ▼` `// ×` comments are removed — they were placeholder anchors for Phase 5's eventual migration; with the migration complete they are obsolete (and re-using them in the migrated lines would violate the no-iteration-narrative comment rule from MEMORY.md).

**Why these specific values:**
- `AeroIcons.CaretUp` / `CaretDown` / `X` (Phosphor PascalCase verbatim, locked Phase 4 decision; STATE.md `[v1.1 locked decisions]`).
- `tint = colors.onSurface` — same color as the original Text glyphs (`color = colors.onSurface` on lines 50-52); pure mechanical replacement preserves intent. Mandatory explicit tint per Phase 4/5 lock.
- `Modifier.size(14.dp)` — matches Phase 5's affordance-icon convention for in-button icons (used by `AeroToastHost` and `AeroNotificationBanner` close buttons; CONTEXT.md "Icon size inside AeroIconButton: Modifier.size(14.dp)").
- `contentDescription = null` — decorative role inside a showcase mockup; same convention as Phase 5 decorative icons (CONTEXT.md "Each Icon call passes contentDescription = null (decorative — they're inside AeroIconButton which has no semantic action label, so the decorative-null convention from Phase 5 applies)").

**Do NOT touch:**
- Lines 56-64 — the `AeroToolbar` row with `Text("B"|"I"|"U"|"S", ...)` letter glyphs. Locked v1.1 exclusion. Migration would expand SHW-06 grep scope.
- The `SectionRow` private composable at lines 68-86.
- The KDoc at lines 20-28.
- Any color tokens or layout values.

**Comment hygiene:** Do NOT add comments like "migrated from Text glyphs" or "Phase 6 SHW-06". The git commit message carries that context (per MEMORY.md `feedback_no_iteration_comments.md`).
  </action>

  <verify>
    <automated>cd C:/1A_WORK/ui_lib &amp;&amp; ./gradlew :showcase:compileKotlin</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/` returns 0 hits (was 3 before — VALIDATION.md confirms current state).
    - `grep -E "Icon\(AeroIcons\.(CaretUp|CaretDown|X)" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns exactly 3 hits.
    - `grep -c "tint = colors.onSurface" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns at least 7 (4 BIUS letters that are untouched + 3 newly migrated icons).
    - `grep -c "Modifier.size(14.dp)" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns exactly 3 (one per migrated icon).
    - `grep -E "import com.mordred.aero.icons.AeroIcons$" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - `grep "import com.mordred.aero.icons.\`internal\`.CaretUp" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - `grep "import com.mordred.aero.icons.\`internal\`.CaretDown" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - `grep "import com.mordred.aero.icons.\`internal\`.X" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - `grep "import androidx.compose.material3.Icon$" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - `grep "import androidx.compose.foundation.layout.size" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns 1 hit.
    - BIUS letters preserved: `grep -E 'Text\("(B|I|U|S)"' showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt` returns exactly 4 hits (locked exclusion confirmed).
    - `./gradlew :showcase:compileKotlin` exits 0.
  </acceptance_criteria>

  <done>
    `ButtonsSection.kt` AeroIconButton row (BTN-03) renders three real `Icon(AeroIcons.{CaretUp, CaretDown, X})` calls at 14dp with `colors.onSurface` tint; the AeroToolbar BIUS letter row remains untouched; `grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/` returns 0 hits; `:showcase:compileKotlin` succeeds.
  </done>
</task>

</tasks>

<verification>
```bash
# Primary SHW-06 grep gate:
grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/    # must return 0

# Confirm new Icon calls landed:
grep -E "Icon\(AeroIcons\.(CaretUp|CaretDown|X)" showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt   # must return 3

# Confirm BIUS exclusion respected:
grep -E 'Text\("(B|I|U|S)"' showcase/src/main/kotlin/com/mordred/showcase/sections/ButtonsSection.kt   # must return 4

# Compile gate:
./gradlew :showcase:compileKotlin
```
</verification>

<success_criteria>
- Three `Text("▲"|"▼"|"×")` sites in ButtonsSection.kt are replaced with `Icon(AeroIcons.{CaretUp,CaretDown,X}, ...)`.
- The four `Text("B"|"I"|"U"|"S")` sites in the AeroToolbar row are unchanged (locked exclusion).
- `grep -rn 'Text("▲\|▼\|×\|✕")' showcase/src/` returns 0 hits.
- `./gradlew :showcase:compileKotlin` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/06-showcase-iconssection/06-02-SUMMARY.md` per template — record before/after grep counts and confirm the BIUS row was not touched.
</output>
