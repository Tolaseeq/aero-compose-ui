---
phase: 05-component-migrations
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - library/build.gradle.kts
autonomous: true
requirements: [CLN-02, CLN-03]

must_haves:
  truths:
    - "library/build.gradle.kts no longer contains the line `implementation(compose.materialIconsExtended)`"
    - "./gradlew :library:compileKotlin succeeds without compose.materialIconsExtended on the classpath"
    - "./gradlew :library:dependencies --configuration compileClasspath shows zero `material-icons-extended` entries"
    - "grep -rn \"androidx.compose.material.icons\" library/src/ returns 0 hits across production AND test sources"
    - "All ROADMAP success-criterion text-glyph greps return 0 hits across library/src/"
    - "JAR-size baseline (post-Wave-4) and post-removal sizes are recorded; delta is in the expected ~6-8 MB range"
    - "STATE.md Performance Metrics block contains a single-line entry documenting JAR-size pre/post/delta"
    - "Phase 5 SUMMARY.md contains pre-bytes, post-bytes, delta-bytes, delta-MB"
  artifacts:
    - path: "library/build.gradle.kts"
      provides: "Library Gradle config WITHOUT compose.materialIconsExtended; compose.material3 retained"
      contains: "compose.material3"
    - path: ".planning/phases/05-component-migrations/05-SUMMARY.md"
      provides: "Phase 5 closure summary including JAR-size delta block"
      contains: "JAR size"
    - path: ".planning/STATE.md"
      provides: "Updated Performance Metrics block with JAR-size delta line"
      contains: "JAR size:"
  key_links:
    - from: "library/build.gradle.kts (removal of materialIconsExtended)"
      to: "library/src/ (zero residual material.icons references)"
      via: "grep -rn over library/src/ confirms cleanup"
      pattern: "androidx\\.compose\\.material\\.icons"
    - from: "Wave 4 test gate (Plan 04)"
      to: "Wave 5 dep removal (this plan)"
      via: "depends_on: [04]"
      pattern: "depends_on: \\[04\\]"
---

<objective>
Wave 5 of Phase 5 — final dependency removal and verification. Delete `implementation(compose.materialIconsExtended)` from `library/build.gradle.kts`, run JAR-size pre/post measurement, run final grep verifications, and update STATE.md + 05-SUMMARY.md.

Purpose: Close out Phase 5. The Material Icons Extended dependency is the largest item being shed (~6-8 MB expected). The grep suite proves no residual references in production OR test sources. JAR-size delta is recorded in STATE.md Performance Metrics.

Output: 1 modified Gradle file (CLN-02), 0 source changes, 2 documentation files (05-SUMMARY.md, STATE.md update), 1 commit for the Gradle change + the JAR-size measurement annotated in commit body.

**Wave 5 dependency:** depends_on [04] — Wave 4 must have rewritten the tests so removing materialIconsExtended doesn't break test compilation.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-component-migrations/05-CONTEXT.md
@.planning/phases/05-component-migrations/05-RESEARCH.md
@.planning/phases/05-component-migrations/05-VALIDATION.md

<interfaces>
library/build.gradle.kts — current state at line 15:
```kotlin
implementation(compose.materialIconsExtended)
```
This line is the ONLY change. Other dependencies (compose.material3, compose.desktop.common, etc.) remain.

Gradle commands:
- `./gradlew :library:jar` — produces library JAR at library/build/libs/library-*.jar
- `./gradlew :library:compileKotlin` — compile-only verification (~30s)
- `./gradlew :library:test` — full test suite (~45s)
- `./gradlew :library:dependencies --configuration compileClasspath` — emit resolved compileClasspath; pipe through `grep -i materialIcons` to confirm absence

JAR-size measurement (cross-platform):
- bash/MINGW: `ls -l library/build/libs/library-*.jar` (column 5 is bytes; first column with `-rw-`)
- PowerShell: `Get-ChildItem library/build/libs/library-*.jar | Select-Object Name, Length`
- Either works; planner picks based on shell session.

Baseline isolation rationale (CONTEXT.md):
- Take baseline AFTER Wave 4 (current tree) but BEFORE removing materialIconsExtended.
- Take post-removal AFTER CLN-02 commits.
- Delta cleanly attributes to dep removal (Waves 1-4 source changes are negligible byte-wise).

Documentation targets:
- `.planning/phases/05-component-migrations/05-SUMMARY.md` — phase summary; must contain pre-bytes, post-bytes, delta-bytes, delta-MB.
- `.planning/STATE.md` `## Performance Metrics` block — single line entry: `JAR size: pre-v1.1 = X MB, post-Phase-5 = Y MB (delta: -Z MB after materialIconsExtended removal)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture JAR-size baseline (post-Wave-4, pre-removal)</name>
  <files>(no file modifications — captures a number into the next task's commit body)</files>
  <read_first>
    - .planning/phases/05-component-migrations/05-CONTEXT.md (JAR-size measurement section: "Baseline: captured at the START of Wave 5 (after Wave 4 completes), BEFORE removing materialIconsExtended")
    - .planning/phases/05-component-migrations/05-RESEARCH.md ("Wave 5 - Dependency Removal" - JAR-size measurement subsection with exact commands)
    - library/build.gradle.kts (confirm current state: line 15 still has implementation(compose.materialIconsExtended))
  </read_first>
  <action>
**Pre-condition check:** Verify Wave 4 has landed by running:
```bash
./gradlew :library:test
```
Must exit 0. If not, STOP — Plan 04 was not completed properly.

**Build clean JAR:**
```bash
./gradlew :library:jar
```

**Measure size** (pick the appropriate command for the shell):
- bash/MINGW: `ls -l library/build/libs/library-*.jar`
- PowerShell: `Get-ChildItem library/build/libs/library-*.jar | Format-List Name,Length`

**Record the byte count.** Capture the JAR filename + exact byte count. Convert to MB (bytes / 1024 / 1024, two decimals).

**Format for next task's commit body** (do not commit yet — this is a measurement task):
```
JAR-size baseline (post-Wave-4):
  filename: library-<version>.jar
  bytes: <N>
  MB: <N/1048576 to 2 decimals>
```

Save this as a temporary artifact. The number is referenced verbatim in Task 3's commit body and Task 4's documentation update.

This task does not commit. It captures a number.
  </action>
  <verify>
    <automated>./gradlew :library:test ; ./gradlew :library:jar ; ls -l library/build/libs/library-*.jar 2>/dev/null || dir library\build\libs\library-*.jar</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :library:test` exits 0 (Wave 4 confirmed green)
    - `./gradlew :library:jar` exits 0 (clean JAR produced)
    - `library/build/libs/library-*.jar` file exists
    - Baseline byte count captured (numeric, > 0) and recorded for use in Tasks 3 + 4
  </acceptance_criteria>
  <done>Baseline JAR-size byte count captured; Wave 4 test gate confirmed green; ready for CLN-02 removal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove implementation(compose.materialIconsExtended) from library/build.gradle.kts (CLN-02)</name>
  <files>library/build.gradle.kts</files>
  <read_first>
    - library/build.gradle.kts (line 15: implementation(compose.materialIconsExtended); confirm exact text before removal; surrounding lines for context)
    - .planning/phases/05-component-migrations/05-CONTEXT.md (CLN-02 section: this is the ONLY line removed)
    - .planning/phases/05-component-migrations/05-RESEARCH.md ("CLN-02: library/build.gradle.kts" verbatim removal instruction)
  </read_first>
  <action>
**Single-line deletion.** Remove exactly this line from `library/build.gradle.kts` (line 15):

BEFORE (verbatim):
```kotlin
implementation(compose.materialIconsExtended)
```

AFTER:
(line removed entirely; no replacement)

Do NOT touch any other line:
- `compose.material3` STAYS (provides material3.Icon())
- `compose.desktop.common` STAYS (Phase 1 doctrine)
- All test dependencies STAY
- All plugin/repo configuration STAYS

Save the file.

**Verify compileKotlin succeeds without the dep:**
```bash
./gradlew :library:compileKotlin
```
Must exit 0. If it fails, the absence of the dep means some source still imports `androidx.compose.material.icons.*` — diagnose and fix that source file's import (it's a pre-existing migration miss, not a Wave-5 problem).

**Verify dep resolution:**
```bash
./gradlew :library:dependencies --configuration compileClasspath
```
Pipe through `grep -i materialIcons` (bash) or `Select-String materialIcons` (PowerShell). MUST return 0 lines.

Commit message:
```
chore(05-05-CLN-02): remove compose.materialIconsExtended from :library

JAR baseline (post-Wave-4): <N> bytes (~<N/MB> MB)
Wave 4 test gate: green
Compile-after-removal: green
```
(fill `<N>` from Task 1's captured baseline)
  </action>
  <verify>
    <automated>grep -n 'compose.materialIconsExtended' library/build.gradle.kts ; ./gradlew :library:compileKotlin ; ./gradlew :library:dependencies --configuration compileClasspath > /tmp/deps_05.txt 2>&1 ; grep -i materialIcons /tmp/deps_05.txt || echo "ok: no materialIcons lines"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'compose.materialIconsExtended' library/build.gradle.kts` returns 0 hits
    - `grep -n 'materialIconsExtended' library/build.gradle.kts` returns 0 hits (defensive variant)
    - `grep -n 'compose.material3' library/build.gradle.kts` returns >=1 hit (material3 retained)
    - `./gradlew :library:compileKotlin` exits 0
    - `./gradlew :library:dependencies --configuration compileClasspath` output piped to grep `materialIcons` returns 0 lines
    - Commit landed with message starting `chore(05-05-CLN-02):`
  </acceptance_criteria>
  <done>compose.materialIconsExtended removed from build.gradle.kts; library still compiles; classpath has zero materialIcons entries; one commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CLN-03 — final grep verification across library/src/ for material.icons + ROADMAP success-criterion text glyphs</name>
  <files>(no file modifications — pure verification)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (CLN-03 text: grep -rn "androidx.compose.material.icons" library/src/ returns 0)
    - .planning/ROADMAP.md (Phase 5 success criterion 1: text-glyph greps over library/src/ all return 0)
    - .planning/phases/05-component-migrations/05-RESEARCH.md ("Wave 5 - CLN-03 Grep Verification" - exact grep commands)
    - .planning/phases/05-component-migrations/05-VALIDATION.md (CLN-03 row: final grep gate)
  </read_first>
  <action>
Run the full grep suite and confirm every command returns 0 hits.

**Grep 1 — Material Icons import absence (CLN-03):**
```bash
grep -rn "androidx.compose.material.icons" library/src/
```
Expected: 0 hits.

**Grep 2 — ROADMAP success-criterion text glyphs (broad sweep):**
```bash
grep -rn 'Text("▲\|▼\|▶\|✕\|✓\|─\|□\|❒\|–")' library/src/
```
Expected: 0 hits.

**Grep 3 — Lowercase-x clear button (ROADMAP success criterion 1 explicit):**
```bash
grep -rn 'Text("x"' library/src/
```
Expected: 0 hits.

**Grep 4 — Stale Icons.Outlined references (defensive):**
```bash
grep -rn 'Icons.Outlined' library/src/
```
Expected: 0 hits.

**Grep 5 — Deleted Canvas composable names (defensive):**
```bash
grep -rn 'SearchIcon\|EyeOpenIcon\|EyeClosedIcon' library/src/main/
```
Expected: 0 hits in production source. (The names may legitimately appear in test files or git history; check production only.)

If any grep returns hits, STOP. Diagnose: it's either a missed migration (rare; Wave 1-3 should have caught it) or a stale reference. Fix and recompile. Do NOT mark Phase 5 complete with non-zero hits.

If all greps return 0, proceed to Task 4.

This task does not commit. It is a final verification gate before documentation tasks.
  </action>
  <verify>
    <automated>echo "=== material.icons sweep ===" ; grep -rn "androidx.compose.material.icons" library/src/ ; echo "=== text glyph sweep ===" ; grep -rn 'Text("▲\|▼\|▶\|✕\|✓\|─\|□\|❒\|–")' library/src/ ; echo "=== Text(\"x\") sweep ===" ; grep -rn 'Text("x"' library/src/ ; echo "=== Icons.Outlined sweep ===" ; grep -rn 'Icons.Outlined' library/src/ ; echo "=== Canvas-fn names sweep ===" ; grep -rn 'SearchIcon\|EyeOpenIcon\|EyeClosedIcon' library/src/main/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "androidx.compose.material.icons" library/src/` returns 0 hits
    - `grep -rn 'Text("▲\|▼\|▶\|✕\|✓\|─\|□\|❒\|–")' library/src/` returns 0 hits
    - `grep -rn 'Text("x"' library/src/` returns 0 hits
    - `grep -rn 'Icons.Outlined' library/src/` returns 0 hits
    - `grep -rn 'SearchIcon\|EyeOpenIcon\|EyeClosedIcon' library/src/main/` returns 0 hits
    - `./gradlew :library:test` (re-run after CLN-02 removal in Task 2) exits 0
  </acceptance_criteria>
  <done>All 5 grep sweeps return 0 hits; full test suite still green after dep removal; CLN-03 satisfied; ready for documentation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Capture post-removal JAR size, write 05-SUMMARY.md, update STATE.md Performance Metrics</name>
  <files>
    - .planning/phases/05-component-migrations/05-SUMMARY.md
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/05-component-migrations/05-CONTEXT.md (JAR-size measurement section: "Where the numbers live: 1. Phase 5 SUMMARY.md - pre-bytes, post-bytes, delta-bytes, delta-MB. Mandatory. 2. STATE.md Performance Metrics block - single line. Cross-reference to SUMMARY.")
    - .planning/STATE.md (current Performance Metrics block; identify insertion point for the new JAR-size line)
    - C:/Users/1/.claude/get-shit-done/templates/summary.md (the SUMMARY.md template the executor follows)
  </read_first>
  <action>
**Step 1 - Capture post-removal JAR size:**
```bash
./gradlew :library:jar
ls -l library/build/libs/library-*.jar    # bash/MINGW
# OR
Get-ChildItem library/build/libs/library-*.jar | Format-List Name,Length    # PowerShell
```

Record post-removal byte count. Compute:
- delta_bytes = baseline_bytes - post_bytes
- delta_MB = delta_bytes / 1048576 (2 decimals)

**Step 2 - Write `.planning/phases/05-component-migrations/05-SUMMARY.md`** (using the project's summary.md template structure). Required content:

- Phase scope summary (5 waves, 14 requirements, all complete)
- Plan-by-plan completion table (Plans 01-05)
- All 14 requirements (MIG-01..11, CLN-01..03) with status checked
- All 5 ROADMAP success criteria with evidence (grep results from Task 3, JAR-size delta from this task)
- **JAR-size delta block** (mandatory; values from Task 1 baseline + this task post-removal):
  ```
  ## JAR size delta

  | Metric | Value |
  |--------|-------|
  | Pre-removal (post-Wave-4) | <N1> bytes (~<N1/MB> MB) |
  | Post-removal (post-CLN-02) | <N2> bytes (~<N2/MB> MB) |
  | Delta | -<N1-N2> bytes (~-<delta_MB> MB) |
  | Expected delta (per ROADMAP) | ~6-8 MB |
  | Within expected range? | yes / no — explain if no |
  ```
- AeroNumberSpinner visual checkpoint outcome (approved at 12dp default OR escalation step recorded)
- Commits landed (one per plan + atomic commits within Plan 01)
- Open issues / next phase notes (Phase 6 unblocked)

**Step 3 - Update `.planning/STATE.md`** Performance Metrics block. Add a single line in the existing `## Performance Metrics` section (under v1.1 metrics):
```
JAR size: pre-v1.1 = <N1/MB> MB, post-Phase-5 = <N2/MB> MB (delta: -<delta_MB> MB after materialIconsExtended removal). See .planning/phases/05-component-migrations/05-SUMMARY.md.
```

Do NOT add the JAR-size delta to PROJECT.md (CONTEXT.md explicitly rejected that — PROJECT.md is for decisions, not metrics).

Also update STATE.md frontmatter:
- `stopped_at: "Phase 5 complete"`
- `last_activity: "2026-04-29 — Phase 5 complete: 14 requirements migrated, materialIconsExtended removed, JAR delta -<delta_MB> MB"`
- `progress.completed_phases: 5`
- `progress.percent` recomputed (5/6 phases)

Update STATE.md `## Current Position` to reflect Phase 5 closure and Phase 6 readiness.

Commit message: `docs(05-05): record Phase 5 closure — JAR delta -<delta_MB> MB; STATE updated`
  </action>
  <verify>
    <automated>./gradlew :library:jar ; ls -l library/build/libs/library-*.jar 2>/dev/null || dir library\build\libs\library-*.jar ; test -f .planning/phases/05-component-migrations/05-SUMMARY.md && echo SUMMARY_EXISTS ; grep -n 'JAR size' .planning/STATE.md ; grep -n 'JAR size' .planning/phases/05-component-migrations/05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :library:jar` exits 0; library JAR exists in library/build/libs/
    - `.planning/phases/05-component-migrations/05-SUMMARY.md` exists
    - `grep -n 'JAR size' .planning/phases/05-component-migrations/05-SUMMARY.md` returns >=1 hit (delta block present)
    - `grep -n 'pre-removal\|Pre-removal\|baseline\|Baseline' .planning/phases/05-component-migrations/05-SUMMARY.md` returns >=1 hit
    - `grep -n 'post-removal\|Post-removal' .planning/phases/05-component-migrations/05-SUMMARY.md` returns >=1 hit
    - `grep -n 'JAR size' .planning/STATE.md` returns >=1 hit (Performance Metrics line added)
    - `grep -n 'materialIconsExtended' .planning/STATE.md` returns >=1 hit (cross-reference to what was removed)
    - STATE.md frontmatter `stopped_at` references Phase 5 complete
    - Commit landed with message starting `docs(05-05):`
  </acceptance_criteria>
  <done>05-SUMMARY.md captures all 14 requirements, 5 success criteria, JAR-size delta block; STATE.md Performance Metrics updated; STATE.md position reflects Phase 5 closure; commit landed.</done>
</task>

</tasks>

<verification>
After all 4 tasks:
- library/build.gradle.kts has 0 materialIconsExtended references
- :library:compileKotlin and :library:test both exit 0
- :library:dependencies --configuration compileClasspath shows 0 materialIcons entries
- All 5 CLN-03 grep sweeps return 0 hits
- 05-SUMMARY.md exists with JAR-size delta block (pre/post/delta in bytes + MB)
- STATE.md Performance Metrics has a JAR-size line
- 2 commits landed in this plan: chore(05-05-CLN-02) and docs(05-05)
</verification>

<success_criteria>
- compose.materialIconsExtended fully gone from build.gradle.kts (CLN-02)
- library/src/ has 0 androidx.compose.material.icons references (CLN-03)
- All 5 ROADMAP Phase 5 success criteria green:
  1. Text-glyph greps zero across library/src/
  2. AeroCheckbox/TitleBar etc. render Icon(AeroIcons.*) (verified by Wave 1-3 plans)
  3. SearchIcon/EyeOpenIcon/EyeClosedIcon Canvas composables deleted (verified by Wave 2)
  4. compileKotlin green WITHOUT materialIconsExtended; classpath verifies absence
  5. JAR-size delta documented; AeroNumberSpinner visually confirmed in three themes (Wave 1)
- 05-SUMMARY.md committed with full closure (14 requirements, 5 criteria, JAR delta)
- STATE.md updated to reflect Phase 5 complete (Phase 6 next)
- 2 commits landed: chore(05-05-CLN-02), docs(05-05)
</success_criteria>

<output>
05-SUMMARY.md is itself part of this plan's output (Task 4 creates it). After all 4 tasks complete, the phase is closed and Phase 6 (showcase IconsSection) is unblocked.

Final tree state at completion:
- .planning/phases/05-component-migrations/05-01-PLAN.md..05-05-PLAN.md (5 plans)
- .planning/phases/05-component-migrations/05-01-SUMMARY.md..05-04-SUMMARY.md (4 plan summaries — 05-01 through 05-04)
- .planning/phases/05-component-migrations/05-SUMMARY.md (phase summary, this plan's deliverable)
- .planning/STATE.md (updated frontmatter + Performance Metrics + Current Position)
- ROADMAP.md Phase 5 row marked Complete (handled at orchestrator level after this plan lands; planner does not pre-modify it here)
</output>
