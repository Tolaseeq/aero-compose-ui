---
phase: 05-component-migrations
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt
  - library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt
autonomous: true
requirements: [CLN-01]

must_haves:
  truths:
    - "AeroAlertKindTest asserts AeroAlertKind.Info.icon == AeroIcons.Info (and corresponding for Warning/Error/Question)"
    - "AeroBannerKindTest asserts AeroBannerKind.Info.icon == AeroIcons.Info (and corresponding for Warning/Error/Success)"
    - "Neither test file imports anything from androidx.compose.material.icons"
    - "./gradlew :library:test --tests \"*AeroAlertKindTest\" --tests \"*AeroBannerKindTest\" passes"
    - "./gradlew :library:test passes (full suite green) — gate for Wave 5"
  artifacts:
    - path: "library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt"
      provides: "JUnit 5 tests asserting AeroAlertKind.icon returns AeroIcons.* instances"
      contains: "AeroIcons.Info"
    - path: "library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt"
      provides: "JUnit 5 tests asserting AeroBannerKind.icon returns AeroIcons.* instances"
      contains: "AeroIcons.Info"
  key_links:
    - from: "AeroAlertKindTest.kt"
      to: "AeroAlertKind.icon (Wave 1 MIG-10 deliverable)"
      via: "assertEquals(AeroIcons.Info, AeroAlertKind.Info.icon)"
      pattern: "assertEquals\\(AeroIcons\\."
    - from: "AeroBannerKindTest.kt"
      to: "AeroBannerKind.icon (Wave 1 MIG-11 deliverable)"
      via: "assertEquals(AeroIcons.Info, AeroBannerKind.Info.icon)"
      pattern: "assertEquals\\(AeroIcons\\."
---

<objective>
Wave 4 of Phase 5 — rewrite the two enum-mapping tests so they assert against `AeroIcons.*` instances instead of `Icons.Outlined.*`. After this plan, **the gate for Wave 5 is open**: dep removal can proceed without breaking test compilation.

Purpose: This plan is **the gate** for `compose.materialIconsExtended` removal. If these tests still imported Material Icons after Wave 5 dropped the dep, `:library:test` would fail to compile. By rewriting tests now (after MIG-10 + MIG-11 already migrated the production enums in Wave 1), we guarantee Wave 5 can land cleanly.

Output: 2 modified test files; full `:library:test` green; one commit per file (2 commits).

**Wave 4 dependency:** depends_on [01, 02, 03] because MIG-10 (Plan 01) and MIG-11 (Plan 01) must land before assertions like `assertEquals(AeroIcons.Info, AeroAlertKind.Info.icon)` can pass — the right-hand side of the assertion is the migrated production code. (Plans 02 and 03 are not strict dependencies for compilation but are listed for ordering safety so the full test suite is exercised against the complete Wave 1-3 product.)
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-component-migrations/05-CONTEXT.md
@.planning/phases/05-component-migrations/05-RESEARCH.md
@.planning/phases/05-component-migrations/05-VALIDATION.md

<interfaces>
JUnit 5 (Jupiter) - already wired:
```kotlin
import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
```

Production enums after Wave 1 (MIG-10 + MIG-11 already landed):
```kotlin
// AeroAlertKind.icon getter returns:
//   Info     -> AeroIcons.Info
//   Warning  -> AeroIcons.Warning
//   Error    -> AeroIcons.XCircle
//   Question -> AeroIcons.Question

// AeroBannerKind.icon getter returns:
//   Info     -> AeroIcons.Info
//   Warning  -> AeroIcons.Warning
//   Error    -> AeroIcons.XCircle
//   Success  -> AeroIcons.CheckCircle
```

Instance equality rationale (from RESEARCH.md): AeroIcons.* uses lazy backing-property pattern; on every access the same cached instance is returned. `assertEquals(AeroIcons.Info, kind.icon)` compares the SAME cached ImageVector from both sides — assertion is stable.

Existing test pattern preserved: 4 @Test methods per file (one per enum value), plus `definesExactlyFourKinds()` test which is unchanged (only checks enum names).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite AeroAlertKindTest.kt to assert AeroIcons.* instances (CLN-01a)</name>
  <files>library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt</files>
  <read_first>
    - library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt (lines 3-7 imports, lines 21-36 four @Test methods - current state)
    - library/src/main/kotlin/com/mordred/aero/components/overlay/AeroAlertKind.kt (post-MIG-10 state to confirm what the tests are now asserting against - Info -> AeroIcons.Info, Warning -> AeroIcons.Warning, Error -> AeroIcons.XCircle, Question -> AeroIcons.Question)
    - .planning/phases/05-component-migrations/05-CONTEXT.md (CLN-01 test rewrite section: instance match assertions; do NOT add defensive grep checks - that's CLN-03's job)
    - .planning/phases/05-component-migrations/05-RESEARCH.md ("AeroAlertKindTest.kt rewrite" - full BEFORE/AFTER mapping + instance equality rationale)
  </read_first>
  <action>
**Imports** - REMOVE all 4 Material Icons imports from this test file:
```
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.Error
import androidx.compose.material.icons.outlined.HelpOutline
import androidx.compose.material.icons.outlined.Info
import androidx.compose.material.icons.outlined.Warning
```

**Imports** - ADD:
```
import com.mordred.aero.icons.AeroIcons
```

Keep the existing JUnit / kotlin.test imports (Test, assertEquals, etc.) - do not touch.

**Per @Test method changes** - Replace each `Icons.Outlined.*` reference with the corresponding `AeroIcons.*`. Keep the @Test method names and structure identical; only the assertion's expected value changes.

| Test method (preserve name) | BEFORE expected | AFTER expected |
|-----------------------------|-----------------|----------------|
| `infoMapsToInfoIcon` | `Icons.Outlined.Info` | `AeroIcons.Info` |
| `warningMapsToWarningIcon` | `Icons.Outlined.Warning` | `AeroIcons.Warning` |
| `errorMapsToErrorIcon` (existing name) | `Icons.Outlined.Error` | `AeroIcons.XCircle` |
| `questionMapsToHelpIcon` (existing name) | `Icons.Outlined.HelpOutline` | `AeroIcons.Question` |

Verbatim BEFORE/AFTER for one method (the others follow the same template):

BEFORE:
```kotlin
@Test
fun infoMapsToInfoIcon() {
    assertEquals(Icons.Outlined.Info, AeroAlertKind.Info.icon)
}
```

AFTER:
```kotlin
@Test
fun infoMapsToInfoIcon() {
    assertEquals(AeroIcons.Info, AeroAlertKind.Info.icon)
}
```

The `definesExactlyFourKinds()` test (or whatever the count-check method is named in the file) only checks `AeroAlertKind.values().size == 4` and enum names — DO NOT modify it.

Do NOT add any extra "guard" test for absence of Material Icons imports — that's CLN-03's job (Plan 05 grep over library/src/).

Commit message: `test(05-04-CLN-01a): rewrite AeroAlertKindTest to assert AeroIcons.* instances`
  </action>
  <verify>
    <automated>grep -n 'Icons.Outlined' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt ; grep -n 'androidx.compose.material.icons' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt ; grep -nE 'assertEquals\(AeroIcons\.(Info|Warning|XCircle|Question)' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt ; ./gradlew :library:test --tests "*AeroAlertKindTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Icons.Outlined' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns 0 hits
    - `grep -n 'androidx.compose.material.icons' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns 0 hits
    - `grep -n 'assertEquals(AeroIcons.Info' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.Warning' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.XCircle' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.Question' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroAlertKindTest.kt` returns >=1 hit
    - `./gradlew :library:test --tests "*AeroAlertKindTest"` exits 0
  </acceptance_criteria>
  <done>AeroAlertKindTest imports zero material.icons; 4 assertions reference AeroIcons.Info/Warning/XCircle/Question; test class passes with `:library:test --tests "*AeroAlertKindTest"`; 1 commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite AeroBannerKindTest.kt to assert AeroIcons.* instances (CLN-01b)</name>
  <files>library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt</files>
  <read_first>
    - library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt (lines 3-7 imports, lines 21-36 four @Test methods - current state)
    - library/src/main/kotlin/com/mordred/aero/components/overlay/AeroBannerKind.kt (post-MIG-11 state - Info -> AeroIcons.Info, Warning -> AeroIcons.Warning, Error -> AeroIcons.XCircle, Success -> AeroIcons.CheckCircle)
    - .planning/phases/05-component-migrations/05-CONTEXT.md (CLN-01 test rewrite section)
    - .planning/phases/05-component-migrations/05-RESEARCH.md ("AeroBannerKindTest.kt rewrite" - full BEFORE/AFTER mapping)
  </read_first>
  <action>
Same template as Task 1, applied to the Banner test file.

**Imports** - REMOVE all 4 Material Icons imports:
```
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.outlined.CheckCircle
import androidx.compose.material.icons.outlined.Error
import androidx.compose.material.icons.outlined.Info
import androidx.compose.material.icons.outlined.Warning
```

**Imports** - ADD:
```
import com.mordred.aero.icons.AeroIcons
```

**Per @Test method changes:**

| Test method (preserve name) | BEFORE expected | AFTER expected |
|-----------------------------|-----------------|----------------|
| `infoMapsToInfoIcon` | `Icons.Outlined.Info` | `AeroIcons.Info` |
| `warningMapsToWarningIcon` | `Icons.Outlined.Warning` | `AeroIcons.Warning` |
| `errorMapsToErrorIcon` | `Icons.Outlined.Error` | `AeroIcons.XCircle` |
| `successMapsToCheckCircleIcon` | `Icons.Outlined.CheckCircle` | `AeroIcons.CheckCircle` |

The four-kinds count test (whatever its name) is unchanged.

Commit message: `test(05-04-CLN-01b): rewrite AeroBannerKindTest to assert AeroIcons.* instances`
  </action>
  <verify>
    <automated>grep -n 'Icons.Outlined' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt ; grep -n 'androidx.compose.material.icons' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt ; grep -nE 'assertEquals\(AeroIcons\.(Info|Warning|XCircle|CheckCircle)' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt ; ./gradlew :library:test --tests "*AeroBannerKindTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Icons.Outlined' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns 0 hits
    - `grep -n 'androidx.compose.material.icons' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns 0 hits
    - `grep -n 'assertEquals(AeroIcons.Info' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.Warning' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.XCircle' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns >=1 hit
    - `grep -n 'assertEquals(AeroIcons.CheckCircle' library/src/test/kotlin/com/mordred/aero/components/overlay/AeroBannerKindTest.kt` returns >=1 hit
    - `./gradlew :library:test --tests "*AeroBannerKindTest"` exits 0
  </acceptance_criteria>
  <done>AeroBannerKindTest imports zero material.icons; 4 assertions reference AeroIcons.Info/Warning/XCircle/CheckCircle; test class passes; 1 commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full :library:test suite — gate for Wave 5</name>
  <files>(no file modifications — this is a verification gate)</files>
  <read_first>
    - .planning/phases/05-component-migrations/05-VALIDATION.md (per-task verification map - confirms full-suite-green is the Wave 4 exit criterion before Wave 5 begins)
    - .planning/phases/05-component-migrations/05-RESEARCH.md (Pitfall 4: Wave 5 attempted before Wave 4 test compilation - this gate is what prevents that)
  </read_first>
  <action>
Run the full test suite to confirm:
1. Both rewritten tests pass
2. No regression in any other test (Phase 1-3 unit tests still green)
3. Test classpath does not depend on test-side `androidx.compose.material.icons` imports

```bash
./gradlew :library:test
```

If any test fails, do NOT proceed to Plan 05. Diagnose and fix in this same plan.

If the suite passes, write the gate-passed status to the plan summary.

This task does not produce a commit (it's a verification step). Document outcome in 05-04-SUMMARY.md.
  </action>
  <verify>
    <automated>./gradlew :library:test ; grep -rn "androidx.compose.material.icons" library/src/test/</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :library:test` exits 0 (full suite green)
    - `grep -rn "androidx.compose.material.icons" library/src/test/` returns 0 hits across the entire test source tree
    - Test report at `library/build/reports/tests/test/index.html` exists and shows 0 failures
  </acceptance_criteria>
  <done>Full :library:test suite passes; 0 material.icons imports anywhere in library/src/test/; gate for Wave 5 (Plan 05) is now open.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- 2 test files committed
- `./gradlew :library:test` exits 0
- `grep -rn "androidx.compose.material.icons" library/src/test/` returns 0
- Wave 5 gate open
</verification>

<success_criteria>
- AeroAlertKindTest and AeroBannerKindTest reference only AeroIcons (not material.icons)
- 4 @Test methods per file preserve existing names; only expected-value changed
- definesExactlyFourKinds (or equivalent count test) unchanged
- :library:test full suite green
- 2 commits landed: test(05-04-CLN-01a) and test(05-04-CLN-01b)
- No defensive material-icons-absence test added (separation of concerns: CLN-03 handles that via grep in Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-migrations/05-04-SUMMARY.md` capturing:
- Per-test-file grep results
- Full :library:test exit code + duration
- Confirmation of "Wave 5 gate open" status
- Commit list (2 commits)
- Note: any flaky test runs encountered (none expected)
</output>
