---
phase: 14-panelgroup-recompose-fix-v2-0-3-release
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - build.gradle.kts
  - .planning/MILESTONES.md
  - .planning/RETROSPECTIVE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "build.gradle.kts root version is 2.0.3 (was 2.0.2)."
    - "A v2.0.3 git tag is created on a commit that includes the fix (plan 01) and the repro (plan 02), and pushed to Tolaseeq/aero-compose-ui."
    - "JitPack resolves com.github.Tolaseeq:aero-compose-ui:2.0.3."
    - "MILESTONES.md, RETROSPECTIVE.md, PROJECT.md Key Decisions, ROADMAP.md, and STATE.md reflect v2.0.3 shipped / Phase 14 done."
  artifacts:
    - path: "build.gradle.kts"
      provides: "v2.0.3 version declaration"
      contains: "version = \"2.0.3\""
    - path: ".planning/PROJECT.md"
      provides: "v2.0.3 Key Decisions row (recompose-during-drag fix)"
      contains: "2.0.3"
  key_links:
    - from: "git tag v2.0.3"
      to: "JitPack artifact"
      via: "push to Tolaseeq/aero-compose-ui; JitPack build"
      pattern: "v2.0.3"
---

<objective>
Release v2.0.3: bump the root `build.gradle.kts` version `2.0.2`→`2.0.3` (version bump precedes the tag, per standing user preference — see MEMORY.md "Bump version on milestone"), update the shipping docs, then tag `v2.0.3`, push to `Tolaseeq/aero-compose-ui`, and confirm JitPack resolves `com.github.Tolaseeq:aero-compose-ui:2.0.3`.

Purpose: Ship the fix on the project's distribution channel (JitPack) and record the milestone in the planning docs.
Output: Version bump committed, docs updated, `v2.0.3` tag pushed, JitPack build confirmed green.
Depends on plans 01 + 02 (the fix and the repro must be committed before tagging).
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-panelgroup-recompose-fix-v2-0-3-release/14-CONTEXT.md

<interfaces>
<!-- Exact current state of the version declaration (read 2026-06-26). -->

build.gradle.kts (root, full file):
```kotlin
// Root build — no plugins applied here. Per-module build files own their plugins.
allprojects {
    group = "com.mordred"
    version = "2.0.2"
}
```
The library inherits this version via `allprojects`; `library/build.gradle.kts` declares no version. Changing line 4 is the complete bump (RESEARCH §"Release Path").

Git remote: `origin → https://github.com/Tolaseeq/aero-compose-ui.git`. Existing tags: v2.0.0, v2.0.1, v2.0.2 (v2.0.3 does not exist yet).

PROJECT.md "Key Decisions" table header (line ~189):
```
| Decision | Rationale | Outcome |
|----------|-----------|---------|
```

ROADMAP.md / STATE.md currently mark Phase 14 "Not started / Ready to plan" and v2.0.3 "In Progress".
</interfaces>

<precedent>
v2.0.1 and v2.0.2 were released the same way: version bump in root build.gradle.kts → commit → `git tag vX.Y.Z` → `git push origin vX.Y.Z` → JitPack auto-builds. jitpack.yml (openjdk17) needs no change for v2.0.3 (RESEARCH §"JitPack Release Path").
</precedent>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 2.0.3 and update shipping docs</name>
  <files>build.gradle.kts, .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - build.gradle.kts (root — confirm `version = "2.0.2"` is on line 4)
    - .planning/MILESTONES.md (read the v2.0.2 entry to mirror its format for the new v2.0.3 entry)
    - .planning/RETROSPECTIVE.md (read the most recent milestone section to mirror format)
    - .planning/PROJECT.md (the "Key Decisions" table at ~line 187 — to append a v2.0.3 row)
    - .planning/ROADMAP.md (Phase 14 row + the v2.0.3 milestone line + Progress table)
    - .planning/STATE.md (frontmatter status + Current Position + Shipped Milestones table)
    - .claude (MEMORY note "Bump version on milestone": version bump precedes the tag; "No iteration-narrative comments": docs describe the shipped state, not the round-by-round fix history)
  </read_first>
  <action>
DOC + VERSION edits only (no git tag yet — that is Task 2).

1. VERSION BUMP (REL-01): In root `build.gradle.kts`, change line 4 from `    version = "2.0.2"` to `    version = "2.0.3"`. This is the only line changed in that file.

2. PROJECT.md "Key Decisions": Append one new row to the table describing the v2.0.3 fix. Use this row (wording is Claude's Discretion per CONTEXT.md, but keep it factual and aligned to the table's existing bilingual style):
```
| v2.0.3: `AeroPanelGroup` size-math reads `isExpanded()` each composition; `expandedState` sync moved to `SideEffect` | Запись в наблюдаемый snapshot-state во время композиции (BoxWithConstraints/SubcomposeLayout) вызывала ×N дублирование секций в horizontal+controlled при рекомпозиции во время drag | ✓ Fixed — no write-during-composition; vertical/uncontrolled byte-identical; 12 PanelGroupLogicTest GREEN |
```

3. MILESTONES.md: Add a v2.0.3 entry mirroring the v2.0.2 format. State: Phase 14, recompose-during-drag duplication fix (RCMP-01..04 + REG-01..02 + REL-01..02), 3 plans, shipped <today's date>. Do NOT narrate the round-by-round fix iterations (MEMORY: no iteration-narrative comments) — describe the shipped outcome.

4. RETROSPECTIVE.md: Add the v2.0.3 lessons (Claude's Discretion wording). Capture at minimum: the write-during-composition root cause + the SideEffect/isExpanded() fix pattern, and that BoxWithConstraints amplifies the loop into ×N. Mirror the existing milestone section structure.

5. ROADMAP.md: Mark Phase 14 done — change the `- [ ] **Phase 14:...` to `- [x]`, change the v2.0.3 milestone line from "🚧 (in progress)" to "✅ ... — SHIPPED <date>", update the Progress table row for Phase 14 to `3/3 | Complete | <date>`, and update the "Next Milestone" / footer note.

6. STATE.md: Update frontmatter (`status:` to reflect shipped/complete, `completed_phases`/`completed_plans`, `last_updated`, `last_activity`), the "Current Position" block, and add a v2.0.3 row to the "Shipped Milestones" table mirroring the v2.0.2 row format.

Use today's date (2026-06-26) for all "shipped" dates.
  </action>
  <verify>
    <automated>cd c:/1A_WORK/ui_lib && grep -q 'version = "2.0.3"' build.gradle.kts && grep -q '2.0.3' .planning/PROJECT.md && echo "PASS: version + project doc updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'version = "2.0.3"' build.gradle.kts` returns 1 match; `grep -c 'version = "2.0.2"' build.gradle.kts` returns 0.
    - `grep -c "2.0.3" .planning/PROJECT.md` returns at least 1 (the new Key Decisions row).
    - `grep -c "2.0.3" .planning/MILESTONES.md` returns at least 1.
    - `grep -c "2.0.3" .planning/RETROSPECTIVE.md` returns at least 1.
    - `grep -n "\[x\] \*\*Phase 14" .planning/ROADMAP.md` returns 1 match (Phase 14 marked done).
    - `grep -c "2.0.3" .planning/STATE.md` returns at least 1 (Shipped Milestones row / position).
    - No iteration-narrative comments were added (MEMORY): docs describe the shipped fix outcome, not per-round UAT history.
  </acceptance_criteria>
  <done>Root version is 2.0.3; PROJECT.md, MILESTONES.md, RETROSPECTIVE.md, ROADMAP.md, and STATE.md all reflect v2.0.3 shipped / Phase 14 complete.</done>
</task>

<task type="auto">
  <name>Task 2: Commit, tag v2.0.3, and push to Tolaseeq/aero-compose-ui</name>
  <files>build.gradle.kts (git add/commit/tag/push — no content edit)</files>
  <read_first>
    - build.gradle.kts (confirm version = "2.0.3")
    - .planning/STATE.md (confirm git remote is origin → Tolaseeq/aero-compose-ui; confirm tags v2.0.0/v2.0.1/v2.0.2 exist and v2.0.3 does not)
  </read_first>
  <action>
Confirm the fix (plan 01) and repro (plan 02) commits are already on the current branch, then commit the version bump + docs, tag, and push.

1. Stage and commit the version bump + docs:
```
cd c:/1A_WORK/ui_lib && git add build.gradle.kts .planning/MILESTONES.md .planning/RETROSPECTIVE.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md
```
Commit message (the bump precedes the tag, per MEMORY):
```
release(14): bump version to 2.0.3 for PanelGroup recompose-during-drag fix
```
End the commit body with the required co-author line:
```
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

2. Push the branch commits:
```
cd c:/1A_WORK/ui_lib && git push origin HEAD
```

3. Create and push the annotated tag (must point at a commit that includes the fix + repro + version bump):
```
cd c:/1A_WORK/ui_lib && git tag -a v2.0.3 -m "v2.0.3 — AeroPanelGroup recompose-during-drag duplication fix" && git push origin v2.0.3
```

If the push or tag-push fails on authentication, surface the auth error to the user (an auth gate) and stop — do not force or rewrite history.
  </action>
  <verify>
    <automated>cd c:/1A_WORK/ui_lib && git tag | grep -qx "v2.0.3" && git ls-remote --tags origin v2.0.3 | grep -q "v2.0.3" && echo "PASS: v2.0.3 tag pushed"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag | grep -x "v2.0.3"` returns `v2.0.3` (local tag exists).
    - `git ls-remote --tags origin v2.0.3` returns a line containing `refs/tags/v2.0.3` (tag pushed to Tolaseeq/aero-compose-ui).
    - `git log --oneline -1 v2.0.3` shows the tag points at a commit, and `git show v2.0.3:build.gradle.kts | grep 'version = "2.0.3"'` returns the bumped version (tag includes the version bump).
    - `git show v2.0.3:library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt | grep -c "expandedState.toBooleanArray"` returns 0 (tag includes the plan-01 fix).
    - `git show v2.0.3:showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt | grep -c "RCMP-04"` returns at least 1 (tag includes the plan-02 repro).
  </acceptance_criteria>
  <done>v2.0.3 is committed and tagged on a commit containing the fix + repro + version bump, and the tag is pushed to origin (Tolaseeq/aero-compose-ui).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
v2.0.3 tagged and pushed to `Tolaseeq/aero-compose-ui`, with the root version bumped to 2.0.3 and all planning docs updated. JitPack auto-builds on the new tag.
  </what-built>
  <how-to-verify>
JitPack builds asynchronously after the tag push and cannot be triggered/confirmed locally — verify in the browser:
1. Open `https://jitpack.io/#Tolaseeq/aero-compose-ui` (or `https://jitpack.io/com/github/Tolaseeq/aero-compose-ui/2.0.3/`).
2. Find the `2.0.3` row. Trigger the build if it has not started (clicking the version "Get it" / log link kicks JitPack).
3. Wait for the build log to show a green/OK status for `2.0.3`.
4. CONFIRM: the artifact resolves as `com.github.Tolaseeq:aero-compose-ui:2.0.3` (the build log ends successfully and the badge turns green).

If the JitPack build fails, capture the log tail and report it — the most common cause is an environment/Gradle issue, not the fix (the fix already compiled + tested locally in plans 01/02).
  </how-to-verify>
  <resume-signal>Type "approved" once the JitPack build for 2.0.3 is green and `com.github.Tolaseeq:aero-compose-ui:2.0.3` resolves; otherwise paste the JitPack build-log error.</resume-signal>
</task>

</tasks>

<verification>
- build.gradle.kts version is 2.0.3 (REL-01).
- v2.0.3 tag exists locally and on origin, pointing at a commit with the fix + repro + bump (REL-02).
- JitPack resolves com.github.Tolaseeq:aero-compose-ui:2.0.3 (REL-02, human-confirmed).
- MILESTONES.md / RETROSPECTIVE.md / PROJECT.md / ROADMAP.md / STATE.md updated for v2.0.3.
</verification>

<success_criteria>
v2.0.3 is bumped, tagged, pushed, and resolves on JitPack; all planning docs record the shipped milestone and Phase 14 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/14-panelgroup-recompose-fix-v2-0-3-release/14-03-SUMMARY.md`

Note: after this plan, run `/gsd:complete-milestone` to archive v2.0.3 (per CONTEXT.md), consistent with how v2.0.1 / v2.0.2 were closed.
</output>
