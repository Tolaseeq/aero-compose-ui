---
phase: 13-aeropanelgroup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt
autonomous: false
requirements: [PNL-07]
must_haves:
  truths:
    - "Dragging the spike divider writes section sizes instantly with no visible animation lag"
    - "Toggling the spike collapse button animates the section height over 200ms"
    - "Collapsing a section then immediately dragging the adjacent divider produces no snap-back and no oscillation"
  artifacts:
    - path: "showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt"
      provides: "Throwaway animation-vs-drag coexistence POC (3 sections, animateFloatAsState target + aeroDragSplitter write on one sizePx state)"
      contains: "animateFloatAsState"
  key_links:
    - from: "PanelGroupSpikeSection.kt aeroDragSplitter onDrag"
      to: "sizePx SnapshotStateList write"
      via: "direct sizePx[i] write inside onDrag lambda"
      pattern: "aeroDragSplitter"
    - from: "PanelGroupSpikeSection.kt animateFloatAsState"
      to: "sizePx-derived render height (read-only target)"
      via: "targetValue reads renderHeight, never writes sizePx"
      pattern: "animateFloatAsState"
---

<objective>
Build the mandatory animation-vs-drag SPIKE (build-order step 1) — a throwaway proof-of-concept in the showcase that empirically resolves PNL-PITFALL-01 BEFORE any library code is written. The POC proves `animateFloatAsState` (reading section size as an animation target) and direct drag writes (writing the same `sizePx` state) coexist on one size variable with NO snap-back and NO oscillation.

Purpose: PNL-PITFALL-01 (animation-vs-drag coexistence) is the only architectural unknown in this phase and is empirically unverified in this codebase. It is a HARD GATE — NO Compose layout/drag/animation work in any later plan may begin until this spike's gate passes. If the spike fails, the fallback (split intent-state from display-state; disable drag while an adjacent animation is in-flight, ~20 lines) is documented in 13-RESEARCH.md Pattern 3 and must be recorded in the SUMMARY for downstream plans.
Output: A throwaway `PanelGroupSpikeSection.kt` composable wired into the showcase, plus a human-verify checkpoint confirming the gate.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-aeropanelgroup/13-RESEARCH.md
@.planning/phases/13-aeropanelgroup/13-CONTEXT.md

<interfaces>
<!-- aeroDragSplitter — reuse verbatim, Orientation.Vertical. Verified AeroDragSplitter.kt. -->
internal fun Modifier.aeroDragSplitter(
    orientation: Orientation,
    onDrag: (deltaPx: Float) -> Unit,
    onDragEnd: () -> Unit = {},
    enabled: Boolean = true,
): Modifier
<!-- Keys pointerInput on (orientation, enabled) only — onDrag captured once, MUST read LIVE state. -->

<!-- clampDividerPx — reuse verbatim. SplitClamp.kt lines 21-24. Inverted-range guard inside (safeMax). -->
internal fun clampDividerPx(currentPx: Float, deltaPx: Float, minFirstPx: Float, maxPx: Float): Float

<!-- animateFloatAsState easing precedent — AeroSidebar tween(200, FastOutSlowInEasing); AeroAccordion caret tween(160). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write throwaway animation-vs-drag spike composable in showcase</name>
  <files>showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSplitPane.kt (lines 98-124 BoxWithConstraints to totalPx + rememberUpdatedState(totalPx) drag pattern; lines 179-232 SplitPaneDivider visual)
    - library/src/main/kotlin/com/mordred/aero/components/internal/drag/AeroDragSplitter.kt (aeroDragSplitter signature; pointerInput(orientation, enabled) — no extra keys)
    - library/src/main/kotlin/com/mordred/aero/components/layout/internal/splitpane/SplitClamp.kt (clampDividerPx safeMax guard)
    - showcase/src/main/kotlin/com/mordred/showcase/sections/LayoutSection.kt (import idiom, section composable shape, AeroTheme.colors usage)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Pattern 3 Animation-vs-Drag Coexistence, exact spike state shape)
  </read_first>
  <action>
    Create `showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt` with a public `@Composable fun PanelGroupSpikeSection()`. THROWAWAY code (deleted after gate passes) — keep self-contained, do NOT add any library code.

    Structure (per 13-RESEARCH.md Pattern 3):
    - `BoxWithConstraints(modifier = Modifier.fillMaxSize())`; `val totalPx = constraints.maxHeight.toFloat()`.
    - State: `val sizePx = remember { mutableStateListOf(300f, 300f, 300f) }` (3 sections) — NO `remember(totalPx)` key (PITFALL-A). `val expanded = remember { mutableStateListOf(true, true, true) }`.
    - Derived each recompose (NOT stored): `val density = LocalDensity.current`; `val headerHeightPx = with(density) { 36.dp.toPx() }`; `val dividerThicknessPx = with(density) { 8.dp.toPx() }`; `val activeDividerCount = expanded.zipWithNext().count { (a, b) -> a && b }`; `val collapsedCount = expanded.count { !it }`; `val availableForExpanded = totalPx - collapsedCount * headerHeightPx - activeDividerCount * dividerThicknessPx`; `val expandedSizeSum = sizePx.indices.filter { expanded[it] }.sumOf { sizePx[it].toDouble() }.toFloat()`.
    - `fun renderHeight(i: Int): Float = if (!expanded[i]) headerHeightPx else if (expandedSizeSum > 0f) (sizePx[i] / expandedSizeSum) * availableForExpanded else availableForExpanded`.
    - Per-section animated height: `val animatedHeight = sizePx.indices.map { i -> animateFloatAsState(targetValue = renderHeight(i), animationSpec = tween(durationMillis = 200, easing = FastOutSlowInEasing), label = "spikeHeight_$i").value }`. CRITICAL: `animateFloatAsState` only READS `renderHeight(i)` as target; it NEVER writes `sizePx`.
    - Drag lambda (live-state read per FIXSP-01): `val liveTotalPx by rememberUpdatedState(totalPx)`. `val onDragBetween: (Int, Int, Float) -> Unit = { above, below, delta -> val combined = sizePx[above] + sizePx[below]; val minPx = with(density) { 60.dp.toPx() }; val newAbove = clampDividerPx(sizePx[above], delta, minPx, combined - minPx); sizePx[above] = newAbove; sizePx[below] = combined - newAbove }`. Read `sizePx[above]`/`sizePx[below]` DIRECTLY inside the lambda — do NOT capture a plain local copy.
    - Render a `Column`: for each section a header strip `Box(Modifier.fillMaxWidth().height(36.dp).background(...))` with a toggle flipping `expanded[i]`, then a content `Box(Modifier.fillMaxWidth().height(with(density) { animatedHeight[i].toDp() }))` when expanded. Between two adjacent expanded sections (`expanded[i] && i < lastIndex && expanded[i+1]`) an 8dp-tall hit-area `Box` with `Modifier.aeroDragSplitter(orientation = Orientation.Vertical, onDrag = { delta -> onDragBetween(i, i + 1, delta) })`.
    - Add a collapse toggle button (flips `expanded[1] = !expanded[1]`) so the human can test "collapse-then-immediately-drag".
    - Easing imports: `androidx.compose.animation.core.FastOutSlowInEasing`, `tween`, `animateFloatAsState`.
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :showcase:compileKotlin -q 2>&1 | tail -5; grep -q "animateFloatAsState" showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt && grep -q "aeroDragSplitter" showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt && grep -q "rememberUpdatedState" showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt && echo SPIKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `PanelGroupSpikeSection.kt` exists and `:showcase:compileKotlin` exits 0.
    - File contains `animateFloatAsState`, `aeroDragSplitter`, `rememberUpdatedState`, `mutableStateListOf`, `FastOutSlowInEasing`, `tween(durationMillis = 200`.
    - File does NOT contain `remember(totalPx)` (PITFALL-A): `grep -c "remember(totalPx)" PanelGroupSpikeSection.kt` returns 0.
    - File does NOT contain `animateContentSize` (PNL-PITFALL-10): grep count 0.
    - File does NOT contain `detectDragGestures` (PITFALL-03): grep count 0.
    - The `onDrag` lambda reads `sizePx[above]`/`sizePx[below]` directly (no captured plain-val copy of a section size before the lambda body).
  </acceptance_criteria>
  <done>Spike composable compiles; uses animateFloatAsState target + direct aeroDragSplitter write on one sizePx state; no banned anti-pattern tokens present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire spike section into the showcase app for manual testing</name>
  <files>showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt</files>
  <read_first>
    - showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt (how sections are registered/navigated)
    - showcase/src/main/kotlin/com/mordred/showcase/sections/PanelGroupSpikeSection.kt (the composable just created)
  </read_first>
  <action>
    Wire `PanelGroupSpikeSection()` into the showcase so it is reachable at runtime. Add the minimal registration the existing navigation/section mechanism in `ShowcaseApp.kt` uses (a nav entry + call site, mirroring how `LayoutSection()` is registered). This is a temporary wiring for the spike gate; record in the SUMMARY that both this wiring AND `PanelGroupSpikeSection.kt` are throwaway and must be removed in plan 13-05 (showcase sign-off) once the real `AeroPanelGroup` demo replaces it. Do not alter any other section.
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :showcase:compileKotlin -q 2>&1 | tail -5; grep -rq "PanelGroupSpikeSection" showcase/src/main/kotlin/com/mordred/showcase/ShowcaseApp.kt && echo WIRED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ShowcaseApp.kt` references `PanelGroupSpikeSection` (grep match).
    - `:showcase:compileKotlin` exits 0.
    - No other existing section composable removed or renamed (diff touches only the spike registration lines).
  </acceptance_criteria>
  <done>Running the showcase reaches the spike section; ready for human gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human gate — confirm animation-vs-drag coexistence (PNL-PITFALL-01)</name>
  <action>Pause for human verification. The executor presents the running spike and the human confirms the coexistence gate per how-to-verify below. Record the PASS/FALLBACK outcome in the SUMMARY.</action>
  <what-built>Throwaway animation-vs-drag spike: 3 sections with `animateFloatAsState` reading section size as the animation target while `aeroDragSplitter` writes the same `sizePx` state directly. A collapse toggle button flips section 1.</what-built>
  <how-to-verify>
    1. Run the showcase: `./gradlew :showcase:run` and navigate to the spike section.
    2. Drag the divider between sections 0 and 1 slowly, then rapidly. Confirm the boundary tracks the cursor INSTANTLY with no visible lag and no rubber-banding.
    3. Click the collapse toggle for section 1. Confirm it animates smoothly over ~200ms (FastOutSlowInEasing), not instantly and not janky.
    4. CRITICAL GATE: Collapse section 1, then IMMEDIATELY (within the animation) drag the divider between sections 0 and 1 (or 0 and the next expanded neighbor). Confirm NO snap-back to the start position and NO oscillation between drag value and animation target.
    5. Resize the showcase window while dragging — confirm proportions re-derive with no reset.
  </how-to-verify>
  <resume-signal>Type "approved" if the gate passes (PNL-PITFALL-01 resolved — proceed with Pattern 3 as designed). If you see snap-back/oscillation/lag, describe it: this triggers the documented fallback (split intent-state from display-state; gate drag on `animatedHeight == renderHeight`) which the SUMMARY must record for plan 13-03/13-04.</resume-signal>
</task>

</tasks>

<verification>
- `:showcase:compileKotlin` green.
- Spike composable present with required tokens; no banned anti-pattern tokens.
- Human gate explicitly recorded as PASS or FAIL-with-fallback in the SUMMARY.
</verification>

<success_criteria>
- PNL-07 spike gate resolved: collapse-then-immediate-drag is clean (no snap-back, no oscillation), OR the fallback path is recorded for downstream plans.
- All later Compose work is now unblocked (gate decision documented).
</success_criteria>

<output>
After completion, create `.planning/phases/13-aeropanelgroup/13-01-SUMMARY.md`. MUST record: (a) the gate outcome (PASS = Pattern 3 as designed, or FAIL = fallback shape to use); (b) explicit note that `PanelGroupSpikeSection.kt` and its `ShowcaseApp.kt` wiring are THROWAWAY and must be removed in plan 13-05.
</output>
