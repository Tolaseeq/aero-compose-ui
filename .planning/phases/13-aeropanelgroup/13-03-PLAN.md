---
phase: 13-aeropanelgroup
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt
autonomous: true
requirements: [PNL-01, PNL-02, PNL-03, PNL-04, PNL-13, PNL-15]
must_haves:
  truths:
    - "Developer can declare N vertical sections via section(key, title) { content } scope-DSL"
    - "Sections fill parent height; expanded sections divide availableForExpanded proportionally"
    - "Window resize re-derives heights from new totalPx with no proportion reset"
    - "Clicking a header toggles collapse/expand (uncontrolled path); collapse animates 200ms FastOutSlowInEasing"
    - "Re-expanding restores lastExpandedFraction; neighbors shrink back"
    - "Render loop is keyed by section.key (stable across duplicate titles / reorder)"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt"
      provides: "AeroPanelGroup composable + AeroPanelGroupScope DSL + uncontrolled expansion + 200ms animation (no drag, no polish)"
      min_lines: 120
      contains: "AeroPanelGroupScope"
  key_links:
    - from: "AeroPanelGroup BoxWithConstraints"
      to: "PanelDistribution computeAvailablePx / distributePx"
      via: "derive render heights each recompose from totalPx"
      pattern: "computeAvailablePx"
    - from: "AeroPanelGroup collapse/expand toggle"
      to: "shareTransferOnCollapse / shareTransferOnExpand + lastExpandedFraction"
      via: "uncontrolled internal state mutation on toggle"
      pattern: "shareTransferOn"
---

<objective>
Build-order steps 3 + 4: the layout skeleton (BoxWithConstraints + fraction state, scope-DSL, key(section.key) render loop, static dividers) AND collapse/expand animation (200ms FastOutSlowInEasing via animateFloatAsState, share-transfer wiring, lastExpandedFraction save/restore). NO drag, NO controlled path, NO visual polish yet — those are plans 13-04 and 13-05.

Purpose: Establishes the public AeroPanelGroup + AeroPanelGroupScope skeleton consuming the GREEN pure-logic from plan 13-02. Wires proportional sizing (PNL-04), window-resize survival (PNL-04, criterion 3), collapse/expand neighbor absorption (PNL-02/03), edge cases (PNL-15), stable section identity (PNL-13). All Compose anti-patterns must be avoided concretely (no remember(totalPx), no animateContentSize, no weight-on-all-sections, no drag divider on collapsed boundary).
Output: AeroPanelGroup.kt with uncontrolled expansion + animation; drag and polish deferred.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-aeropanelgroup/13-RESEARCH.md
@.planning/phases/13-aeropanelgroup/13-CONTEXT.md
@.planning/phases/13-aeropanelgroup/13-01-SUMMARY.md
@.planning/phases/13-aeropanelgroup/13-02-SUMMARY.md

<interfaces>
Consume verbatim from plan 13-02 PanelDistribution.kt (confirm exact signatures in 13-02-SUMMARY):
- clampPanelDividerPx(aboveSizePx, deltaPx, minAbovePx, minBelowPx, totalBudgetPx): Float
- computeAvailablePx(totalPx, expanded BooleanArray, headerPx, dividerPx): Float
- activeDividerCount(expanded BooleanArray): Int
- distributePx(sizePx, expanded, totalPx, headerPx, dividerPx): FloatArray
- shareTransferOnCollapse(sizePx, expanded, index): FloatArray
- shareTransferOnExpand(sizePx, expanded, index, restorePx): FloatArray

AeroSidebarScope DSL idiom (AeroSidebarState.kt line 116): public class internal constructor; @Composable member fns.
AeroAccordion uncontrolled hybrid (AeroAccordion.kt 117-164): val controlled = onExpandedChange != null.
AeroSplitPane fraction-state (AeroSplitPane.kt 104-108): var by remember with NO totalPx key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AeroPanelGroupScope DSL + section config + skeleton (no animation, no drag)</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSidebarState.kt (lines 116-230 AeroSidebarScope DSL idiom)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSplitPane.kt (lines 98-108 BoxWithConstraints totalPx + no-key fraction state)
    - library/src/main/kotlin/com/mordred/aero/components/layout/internal/panelgroup/PanelDistribution.kt (the pure fns to call)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Pattern 5 Scope-DSL; Pattern 1 fraction state; Pattern 5 divider placement; Anti-Patterns)
    - .planning/phases/13-aeropanelgroup/13-CONTEXT.md (section parameter list; 36dp header; clip content; default all-expanded equal)
  </read_first>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt`, package `com.mordred.aero.components.layout`.

    Internal constants: `private val HEADER_HEIGHT = 36.dp`; `private val DIVIDER_THICKNESS = 8.dp`; `private val DEFAULT_SECTION_MIN_SIZE = 60.dp`.

    Section config: `internal class AeroPanelSectionConfig(val key: String, val title: String, val minSize: Dp, val collapsible: Boolean, val resizable: Boolean, val defaultExpanded: Boolean, val defaultSize: Dp?, val leadingIcon: ImageVector?, val headerActions: (@Composable RowScope.() -> Unit)?, val content: @Composable () -> Unit)`.

    Scope-DSL: `public class AeroPanelGroupScope internal constructor()` with `internal val sections = mutableListOf<AeroPanelSectionConfig>()` and `public fun section(key: String, title: String, minSize: Dp = DEFAULT_SECTION_MIN_SIZE, collapsible: Boolean = true, resizable: Boolean = true, defaultExpanded: Boolean = true, defaultSize: Dp? = null, leadingIcon: ImageVector? = null, headerActions: (@Composable RowScope.() -> Unit)? = null, content: @Composable () -> Unit) { sections.add(AeroPanelSectionConfig(...)) }`. Order: key first (mandatory), title, flags, optional slots, content last.

    Public composable: `public fun AeroPanelGroup(modifier: Modifier = Modifier, initiallyExpanded: Set<String>? = null, onLayoutChange: ((List<Float>) -> Unit)? = null, content: @Composable AeroPanelGroupScope.() -> Unit)`. Leave a TODO marker: controlled params onExpandedChange/expandedKeys land in plan 13-04.

    Body:
    - Collect sections fresh each recompose (AeroSidebar idiom): build `AeroPanelGroupScope()`, run `scope.content()`, `val sections = scope.sections`.
    - `val density = LocalDensity.current`; `val headerPx = with(density) { HEADER_HEIGHT.toPx() }`; `val dividerPx = with(density) { DIVIDER_THICKNESS.toPx() }`.
    - State, NO `remember(totalPx)` key (PITFALL-A): `val sizePx = remember { mutableStateListOf<Float>() }` seeded equal shares or from defaultSize; `val expandedState = remember { mutableStateListOf<Boolean>() }` seeded from defaultExpanded / initiallyExpanded by key; `val lastExpandedFractionState = remember { mutableStateListOf<Float>() }`.
    - `BoxWithConstraints(modifier = modifier.fillMaxSize())`: `val totalPx = constraints.maxHeight.toFloat()`. `val expandedArr = expandedState.toBooleanArray()`; call `computeAvailablePx(totalPx, expandedArr, headerPx, dividerPx)` for availableForExpanded; call `distributePx(...)` for renderHeights.
    - Render `Column`: `sections.forEachIndexed { i, section -> key(section.key) { ... } }` — `key(section.key)` MANDATORY (PNL-PITFALL-08). Header `Row` at fixed `HEADER_HEIGHT`, `.clickable { onToggle(i) }` guarded by collapsible. When expanded: content `Box(Modifier.fillMaxWidth().height(with(density){ renderHeights[i].toDp() }).clipToBounds()) { section.content() }`. Static 1dp divider `Box` ONLY where `expandedState[i]` and `i < sections.lastIndex` and `expandedState[i + 1]` (PNL-PITFALL-09, no grip yet). Give `Modifier.weight(1f)` to the LAST EXPANDED section content only (PNL-PITFALL-11) to absorb rounding; others explicit `.height(dp)`.
    - Toggle (uncontrolled): collapse saves `lastExpandedFractionState[i]` as `sizePx[i] / availableForExpanded` (0 if available is 0), sets `expandedState[i] = false`, copies `shareTransferOnCollapse(...)` back into sizePx. Expand sets `expandedState[i] = true`, copies `shareTransferOnExpand(sizePx, expanded, i, restorePx = lastExpandedFractionState[i] * availableForExpanded)` back. `collapsible == false` → no toggle.
    - NO `animateContentSize` (PNL-PITFALL-10). NO drag (13-04). NO glassPanel/caret polish (13-05).
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:compileKotlin -q 2>&1 | tail -6 && echo "remember_totalPx=$(grep -c 'remember(totalPx)' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)" && echo "animateContentSize=$(grep -c 'animateContentSize' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `AeroPanelGroup.kt` compiles (`:library:compileKotlin` exits 0).
    - Contains: `public class AeroPanelGroupScope`, `public fun section(`, `public fun AeroPanelGroup(`, `BoxWithConstraints`, `key(section.key)`, `computeAvailablePx`, `distributePx`, `shareTransferOnCollapse`, `shareTransferOnExpand`, `clipToBounds`, `weight(1f)`.
    - `grep -c "remember(totalPx)"` returns 0 (PITFALL-A).
    - `grep -c "animateContentSize"` returns 0 (PNL-PITFALL-10).
    - `grep -c "detectDragGestures"` returns 0 (PITFALL-03).
    - Divider render condition references `expandedState[i + 1]` (PNL-PITFALL-09 — no divider on collapsed boundary).
  </acceptance_criteria>
  <done>Skeleton compiles; scope-DSL + key(section.key) loop + uncontrolled toggle + window-resize-safe derivation; no banned tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Add 200ms collapse/expand animation via animateFloatAsState</name>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroSidebar.kt (animateDpAsState tween(200, FastOutSlowInEasing) — 200ms precedent)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroAccordion.kt (lines 198-202 animateFloatAsState; line 250 animateContentSize is the NEGATIVE precedent)
    - library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt (skeleton from Task 1)
    - .planning/phases/13-aeropanelgroup/13-RESEARCH.md (Pattern 3 lines 240-250; Anti-Pattern 4)
    - .planning/phases/13-aeropanelgroup/13-01-SUMMARY.md (spike gate outcome — if FALLBACK recorded, apply the split intent/display-state shape instead)
  </read_first>
  <action>
    Add per-section animated height. If 13-01 spike PASSED (Pattern 3): `val animatedHeights = sections.indices.map { i -> animateFloatAsState(targetValue = if (expandedState[i]) renderHeights[i] else headerPx, animationSpec = tween(durationMillis = 200, easing = FastOutSlowInEasing), label = "panelHeight_" + sections[i].key).value }`. Apply `animatedHeights[i]` (not raw renderHeights[i]) to the content Box `.height(with(density){ animatedHeights[i].toDp() })`. animateFloatAsState only READS the derived target — it NEVER writes sizePx (PNL-PITFALL-01 architecture).

    If 13-01 recorded the FALLBACK: implement the documented split intent-state/display-state shape from 13-RESEARCH.md Pattern 3 fallback (animate only on expandedState[i] flip; later gate drag on animatedHeight == renderHeight). Use whichever the spike SUMMARY mandates.

    Easing: `tween(durationMillis = 200, easing = FastOutSlowInEasing)` — import `androidx.compose.animation.core.FastOutSlowInEasing`, `tween`, `animateFloatAsState`. Each section has its own animateFloatAsState keyed by label per section.key so concurrent collapse-A/expand-B animate independently (full PNL-PITFALL-03 drag-race guard lands in 13-04).
  </action>
  <verify>
    <automated>cd "C:/1A_WORK/ui_lib" && ./gradlew :library:test --tests "*.PanelGroupLogicTest" 2>&1 | tail -4 && ./gradlew :library:compileKotlin -q 2>&1 | tail -4 && echo "anim=$(grep -c 'animateFloatAsState' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt) easing=$(grep -c 'FastOutSlowInEasing' library/src/main/kotlin/com/mordred/aero/components/layout/AeroPanelGroup.kt)"</automated>
  </verify>
  <acceptance_criteria>
    - `:library:compileKotlin` exits 0 and `./gradlew :library:test --tests "*.PanelGroupLogicTest"` stays GREEN.
    - File contains `animateFloatAsState`, `tween(durationMillis = 200`, `FastOutSlowInEasing`.
    - Content Box height is driven by `animatedHeights[i]` (animated value), not raw `renderHeights[i]`.
    - Still `grep -c "animateContentSize"` returns 0 (PNL-PITFALL-10 preserved).
  </acceptance_criteria>
  <done>200ms FastOutSlowInEasing collapse/expand animation wired per section; pure-logic suite still GREEN; no animateContentSize.</done>
</task>

</tasks>

<verification>
- `:library:compileKotlin` green; `./gradlew :library:test --tests "*.PanelGroupLogicTest"` green.
- AeroPanelGroup.kt contains required tokens; contains none of the banned tokens (remember(totalPx), animateContentSize, detectDragGestures).
</verification>

<success_criteria>
- PNL-01 scope-DSL, PNL-04 proportional + resize-safe sizing, PNL-02/03 collapse/expand with restore, PNL-13 key identity, PNL-15 edge cases all present at skeleton+animation level.
- Drag (13-04) and polish (13-05) cleanly deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/13-aeropanelgroup/13-03-SUMMARY.md`. Record the final AeroPanelGroup signature, AeroPanelGroupScope.section signature, and the seed/normalization approach for sizePx, so plan 13-04 wires drag + controlled path against them.
</output>
