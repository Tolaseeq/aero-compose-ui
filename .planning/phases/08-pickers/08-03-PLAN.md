---
phase: 08-pickers
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt
  - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt
  - library/src/test/kotlin/com/mordred/aero/components/pickers/AeroTimePickerTest.kt
autonomous: true
requirements: [PICK-03]
must_haves:
  truths:
    - "AeroTimePicker shows an hour field and a minute field (24-hour); values clamp to 0-23 and 0-59"
    - "Selecting/typing a time fires onValueChange with the correct kotlinx.datetime.LocalTime"
    - "showSeconds=true adds a third (seconds) field; when false the emitted LocalTime has second=0"
    - "minuteStep controls the minute spinner step"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt"
      provides: "public AeroTimePicker composable"
      contains: "public fun AeroTimePicker"
      min_lines: 70
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt"
      provides: "internal hour/minute/second AeroNumberSpinner row reused by DateTimePicker"
      contains: "internal fun TimeFields"
    - path: "library/src/test/kotlin/com/mordred/aero/components/pickers/AeroTimePickerTest.kt"
      provides: "PICK-03 LocalTime emission + showSeconds + minuteStep tests"
      contains: "class AeroTimePickerTest"
  key_links:
    - from: "TimeFields.kt"
      to: "AeroNumberSpinner"
      via: "three spinner instances (hour 0..23, minute 0..59 step=minuteStep, second 0..59)"
      pattern: "AeroNumberSpinner\\("
---

<objective>
Build `AeroTimePicker` (PICK-03): hour + minute (+ optional seconds) entry using `AeroNumberSpinner`, emitting `kotlinx.datetime.LocalTime`. 24-hour only (the 12h/AM-PM mode is explicitly descoped per user decision — DO NOT add a `use12Hour` parameter). Extract the spinner row as an internal `TimeFields` composable so `AeroDateTimePicker` (plan 04) reuses it without duplication.

Purpose: SMALL component; also produces the reusable time-fields primitive consumed by DateTimePicker.
Output: `AeroTimePicker.kt` (public), `TimeFields.kt` (internal), `AeroTimePickerTest.kt`.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pickers/08-CONTEXT.md
@.planning/phases/08-pickers/08-RESEARCH.md

<interfaces>
AeroNumberSpinner (public, components/input/AeroNumberSpinner.kt):
```kotlin
public fun AeroNumberSpinner(value: Int, onValueChange: (Int) -> Unit, modifier: Modifier = Modifier,
    enabled: Boolean = true, min: Int = Int.MIN_VALUE, max: Int = Int.MAX_VALUE, step: Int = ...)
// Already clamps to [min,max] via coerceIn; supports step; keyboard + up/down + scroll.
```
NEW-PICK-04 (from research): do NOT debounce/transform AeroNumberSpinner callbacks — pass them straight through to TimePicker state, or the spinner's remember(value) text desyncs.

kotlinx.datetime.LocalTime constructor: `LocalTime(hour: Int, minute: Int, second: Int = 0)`.
PickerPopupContainer (internal, plan 02) is available if a popup form is desired, but per CONTEXT the TimePicker trigger is a read-only AeroTextField + AeroIconButton(AeroIcons.Clock); the popup hosts the TimeFields row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimeFields internal spinner row + LocalTime assembly helper</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/input/AeroNumberSpinner.kt (exact value/onValueChange/min/max/step contract; remember(value) note)
    - library/src/main/kotlin/com/mordred/aero/theme/AeroColorScheme.kt (onSurface for the ":" separators)
  </read_first>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt`, package `com.mordred.aero.components.pickers.internal`:

    ```kotlin
    @Composable
    internal fun TimeFields(
        time: LocalTime,
        onTimeChange: (LocalTime) -> Unit,
        modifier: Modifier = Modifier,
        enabled: Boolean = true,
        showSeconds: Boolean = false,
        minuteStep: Int = 1,
    )
    ```

    Implementation (concrete):
    - `Row(modifier, horizontalArrangement = Arrangement.spacedBy(8.dp), verticalAlignment = Alignment.CenterVertically)`.
    - Hour: `AeroNumberSpinner(value = time.hour, onValueChange = { h -> onTimeChange(assembleTime(h, time.minute, time.second, showSeconds)) }, min = 0, max = 23, step = 1, enabled = enabled)`.
    - Separator `Text(":", style = AeroTheme.typography.bodyLarge, color = AeroTheme.colors.onSurface)`.
    - Minute: `AeroNumberSpinner(value = time.minute, onValueChange = { m -> onTimeChange(assembleTime(time.hour, m, time.second, showSeconds)) }, min = 0, max = 59, step = minuteStep, enabled = enabled)`.
    - If `showSeconds`: another ":" then `AeroNumberSpinner(value = time.second, onValueChange = { s -> onTimeChange(assembleTime(time.hour, time.minute, s, true)) }, min = 0, max = 59, step = 1, enabled = enabled)`.
    - DO NOT wrap the spinner callbacks in any transform/debounce (NEW-PICK-04).

    Add an internal testable assembler:
    ```kotlin
    internal fun assembleTime(hour: Int, minute: Int, second: Int, showSeconds: Boolean): LocalTime =
        LocalTime(hour.coerceIn(0,23), minute.coerceIn(0,59), if (showSeconds) second.coerceIn(0,59) else 0)
    ```
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:compileKotlin && grep -c "AeroNumberSpinner(" library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt</automated>
  </verify>
  <acceptance_criteria>
    - File contains `internal fun TimeFields(` and `internal fun assembleTime(`.
    - `grep -c "AeroNumberSpinner(" TimeFields.kt` returns >= 2 (hour+minute; seconds is conditional).
    - File contains `min = 0` and `max = 23` (hour) and `max = 59` (minute).
    - `grep -c "use12Hour\|amPm\|AM_PM\|AmPm" TimeFields.kt` returns 0 (12h descoped).
    - `./gradlew :library:compileKotlin` exits 0.
  </acceptance_criteria>
  <done>TimeFields renders hour/minute(/second) spinners with correct clamps and emits LocalTime via assembleTime; no 12h handling.</done>
</task>

<task type="auto">
  <name>Task 2: AeroTimePicker public composable (trigger + popup hosting TimeFields)</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt (from Task 1)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/PickerPopupContainer.kt (plan 02 — popup surface)
    - library/src/main/kotlin/com/mordred/aero/components/internal/popup/AeroCalendarPositionProvider.kt (position provider for the popup)
    - library/src/main/kotlin/com/mordred/aero/components/input/AeroTextField.kt (read-only trigger)
    - library/src/main/kotlin/com/mordred/aero/components/buttons/AeroIconButton.kt (Clock trigger button)
  </read_first>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt`, package `com.mordred.aero.components.pickers`:

    ```kotlin
    @Composable
    public fun AeroTimePicker(
        value: LocalTime?,
        onValueChange: (LocalTime) -> Unit,
        modifier: Modifier = Modifier,
        formatter: (LocalTime) -> String = { it.toString() },  // ISO default e.g. 14:30
        placeholder: String = "Select time",
        clearable: Boolean = false,
        onClear: (() -> Unit)? = null,
        enabled: Boolean = true,
        showSeconds: Boolean = false,
        minuteStep: Int = 1,
    )
    ```

    Implementation (concrete):
    - Mirror AeroDatePicker's trigger structure: `Box(modifier)` -> `Row` -> read-only `AeroTextField(value = value?.let(formatter) ?: "", onValueChange = {}, readOnly = true, placeholder = placeholder, modifier = Modifier.weight(1f), trailingIcon = { AeroIconButton(onClick = { if (enabled) expanded = !expanded }) { Icon(AeroIcons.Clock, contentDescription = "Open time picker", tint = AeroTheme.colors.onSurface) } ... optional clear AeroIcons.X })`.
    - `var expanded by remember { mutableStateOf(false) }`.
    - Popup INSIDE the trigger Box: `if (expanded) Popup(popupPositionProvider = remember { AeroCalendarPositionProvider(gap = 4) }, onDismissRequest = { expanded = false }, properties = PopupProperties(focusable = true, dismissOnBackPress = true, dismissOnClickOutside = true)) { PickerPopupContainer { Box(Modifier.padding(12.dp)) { TimeFields(time = value ?: LocalTime(0,0,0), onTimeChange = { onValueChange(it) }, enabled = enabled, showSeconds = showSeconds, minuteStep = minuteStep) } } }`.
    - Note: TimePicker emits on every spinner change (no Apply gate — Apply is only DateTimePicker, plan 04). The popup stays open while the user adjusts; it dismisses on click-outside.
    - Use `Popup` not `Dialog`; no `transparent = true` (W11-01).
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:compileKotlin && grep -c "transparent = true\|androidx.compose.ui.window.Dialog" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public fun AeroTimePicker(` with `value: LocalTime?` and `onValueChange: (LocalTime) -> Unit`.
    - File contains `TimeFields(`, `PickerPopupContainer`, `AeroCalendarPositionProvider`, `AeroIcons.Clock`.
    - `grep -c "transparent = true" AeroTimePicker.kt` returns 0; `grep -c "androidx.compose.ui.window.Dialog" AeroTimePicker.kt` returns 0.
    - `grep -c "use12Hour\|AM_PM\|AmPm" AeroTimePicker.kt` returns 0.
    - `./gradlew :library:compileKotlin` exits 0.
  </acceptance_criteria>
  <done>AeroTimePicker opens a popup with hour/minute(/second) spinners and emits LocalTime on change; 24-hour only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AeroTimePicker / TimeFields LocalTime tests</name>
  <files>library/src/test/kotlin/com/mordred/aero/components/pickers/AeroTimePickerTest.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt (assembleTime contract)
    - library/src/test/kotlin/com/mordred/aero/components/pickers/AeroDatePickerTest.kt (test style from plan 02)
  </read_first>
  <behavior>
    - assembleTime(14, 30, 45, showSeconds=false) == LocalTime(14, 30, 0) (seconds dropped).
    - assembleTime(14, 30, 45, showSeconds=true) == LocalTime(14, 30, 45).
    - assembleTime(25, 70, 0, false) clamps to LocalTime(23, 59, 0).
    - assembleTime(-1, -5, -5, true) clamps to LocalTime(0, 0, 0).
  </behavior>
  <action>
    Create `library/src/test/kotlin/com/mordred/aero/components/pickers/AeroTimePickerTest.kt`, package `com.mordred.aero.components.pickers`. Import `assembleTime` from `com.mordred.aero.components.pickers.internal`. Assert each behavior bullet using `kotlinx.datetime.LocalTime` equality.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroTimePickerTest" -x :showcase:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - File `AeroTimePickerTest.kt` exists, contains `class AeroTimePickerTest`.
    - `grep -c "@Test" AeroTimePickerTest.kt` returns >= 4.
    - The gradle test command exits 0.
  </acceptance_criteria>
  <done>assembleTime drops seconds when showSeconds=false and clamps out-of-range h/m/s; tests pass.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroTimePickerTest"` green.
- `./gradlew :library:compileKotlin` green.
- `grep -rn "use12Hour\|AM_PM" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroTimePicker.kt library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt` returns 0 (12h descoped).
- `grep -rn "transparent = true" AeroTimePicker.kt` returns 0.
</verification>

<success_criteria>
AeroTimePicker is a public 24-hour time picker emitting correct LocalTime values with range clamping, optional seconds, and configurable minuteStep; TimeFields is reusable by DateTimePicker.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pickers/08-03-SUMMARY.md`.
</output>
