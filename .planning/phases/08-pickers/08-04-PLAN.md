---
phase: 08-pickers
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt
  - library/src/test/kotlin/com/mordred/aero/components/pickers/AeroDateTimePickerTest.kt
autonomous: true
requirements: [PICK-04]
must_haves:
  truths:
    - "AeroDateTimePicker popup shows a calendar above time controls in one popup"
    - "Clicking a day sets pending date but does NOT close the popup and does NOT fire onValueChange"
    - "onValueChange(LocalDateTime) fires ONLY when Apply is clicked; Cancel dismisses with no callback"
    - "After Apply, the popup closes and the emitted LocalDateTime combines the chosen date and time"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt"
      provides: "public AeroDateTimePicker composable with Apply/Cancel commit gate"
      contains: "public fun AeroDateTimePicker"
      min_lines: 110
    - path: "library/src/test/kotlin/com/mordred/aero/components/pickers/AeroDateTimePickerTest.kt"
      provides: "PICK-04 Apply-only emission test (pending state not leaked)"
      contains: "class AeroDateTimePickerTest"
  key_links:
    - from: "AeroDateTimePicker.kt"
      to: "pendingDate/pendingTime internal state"
      via: "Apply button combines them into LocalDateTime; calendar onDateSelected does NOT close or emit"
      pattern: "pendingDate"
---

<objective>
Build `AeroDateTimePicker` (PICK-04): a single popup with a calendar on top and time controls below, plus explicit `Apply` / `Cancel` buttons. The CRITICAL behavior (NEW-PICK-02): clicking a day sets pending state but does NOT close the popup or emit — `onValueChange(LocalDateTime)` fires ONLY on Apply. This composes the Phase 7 `AeroCalendarGrid` and the plan-03 `TimeFields` directly (it does NOT embed AeroDatePicker/AeroTimePicker composables).

Purpose: MEDIUM composition component; demonstrates the commit-gate pattern that prevents the auto-close-before-time-set bug.
Output: `AeroDateTimePicker.kt` (public), `AeroDateTimePickerTest.kt`.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pickers/08-CONTEXT.md
@.planning/phases/08-pickers/08-RESEARCH.md

<interfaces>
AeroCalendarGrid (internal): `AeroCalendarGrid(displayMonth, selected, onDateSelected, onMonthChange, modifier, isDisabled)`.
TimeFields (internal, plan 03): `TimeFields(time: LocalTime, onTimeChange: (LocalTime) -> Unit, modifier, enabled, showSeconds, minuteStep)`.
PickerPopupContainer (internal, plan 02): `PickerPopupContainer(modifier) { content }`.
AeroCalendarPositionProvider (internal): position provider for the popup.
kotlinx.datetime: `LocalDateTime(date: LocalDate, time: LocalTime)` and `LocalDateTime(year, monthNumber, dayOfMonth, hour, minute, second)`; `.date` and `.time` accessors.
Buttons for Apply/Cancel: prefer `AeroButton` (primary) and `AeroOutlinedButton` — check components/buttons/ for exact signatures during read_first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: combineDateTime helper + tests scaffold</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt, library/src/test/kotlin/com/mordred/aero/components/pickers/AeroDateTimePickerTest.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt (LocalTime assembly reference)
    - library/src/test/kotlin/com/mordred/aero/components/pickers/AeroTimePickerTest.kt (test style)
  </read_first>
  <behavior>
    - combineDateTime(LocalDate(2026,6,18), LocalTime(14,30,0)) == LocalDateTime(2026,6,18,14,30,0).
    - combineDateTime returns a LocalDateTime whose .date equals the input date and .time equals the input time.
  </behavior>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt`, package `com.mordred.aero.components.pickers`, starting with the internal helper:
    ```kotlin
    internal fun combineDateTime(date: LocalDate, time: LocalTime): LocalDateTime =
        LocalDateTime(date.year, date.monthNumber, date.dayOfMonth, time.hour, time.minute, time.second)
    ```
    Then create `library/src/test/kotlin/com/mordred/aero/components/pickers/AeroDateTimePickerTest.kt` (package `com.mordred.aero.components.pickers`) asserting both behavior bullets above plus a simulation of the Apply gate logic:
    - Model the commit logic as a pure helper if helpful: a test that a pending (date,time) pair only produces a LocalDateTime via combineDateTime when "apply" is invoked. At minimum, assert combineDateTime correctness; the Apply-gate composable wiring is covered by acceptance-criteria greps in Task 2.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroDateTimePickerTest" -x :showcase:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - File `AeroDateTimePicker.kt` contains `internal fun combineDateTime(`.
    - File `AeroDateTimePickerTest.kt` contains `class AeroDateTimePickerTest` and `grep -c "@Test"` returns >= 2.
    - The gradle test command exits 0.
  </acceptance_criteria>
  <done>combineDateTime merges date+time into LocalDateTime correctly; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: AeroDateTimePicker composable with pending state + Apply/Cancel gate</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt (trigger structure + popup anchoring + dateIsDisabled to reuse)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/TimeFields.kt (time control row)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/PickerPopupContainer.kt (popup surface)
    - library/src/main/kotlin/com/mordred/aero/components/buttons/AeroButton.kt (Apply button signature)
    - library/src/main/kotlin/com/mordred/aero/components/buttons/AeroOutlinedButton.kt (Cancel button signature)
  </read_first>
  <action>
    Add the public composable to `AeroDateTimePicker.kt`:

    ```kotlin
    @Composable
    public fun AeroDateTimePicker(
        value: LocalDateTime?,
        onValueChange: (LocalDateTime) -> Unit,
        modifier: Modifier = Modifier,
        formatter: (LocalDateTime) -> String = { it.toString() },  // ISO default
        placeholder: String = "Select date & time",
        clearable: Boolean = false,
        onClear: (() -> Unit)? = null,
        minDate: LocalDate? = null,
        maxDate: LocalDate? = null,
        selectableDates: (LocalDate) -> Boolean = { true },
        enabled: Boolean = true,
        showSeconds: Boolean = false,
        minuteStep: Int = 1,
    )
    ```

    Implementation (concrete — the commit gate is the whole point, NEW-PICK-02):
    - Trigger row identical pattern to AeroDatePicker: read-only `AeroTextField` + `AeroIconButton(AeroIcons.Calendar)` (+ optional clear).
    - `var expanded by remember { mutableStateOf(false) }`.
    - When the popup OPENS, initialize pending state from `value`: `var pendingDate by remember(expanded) { mutableStateOf(value?.date) }`, `var pendingTime by remember(expanded) { mutableStateOf(value?.time ?: LocalTime(0,0,0)) }`, `var displayMonth by remember(expanded) { mutableStateOf(value?.date ?: todayLocalDate()) }`.
    - Popup INSIDE trigger Box via `AeroCalendarPositionProvider`. `dismissOnClickOutside = true` is fine (acts as implicit cancel), but selecting a day must NEVER close.
    - Popup content via `PickerPopupContainer { Column { ... } }`:
      - `AeroCalendarGrid(displayMonth = displayMonth, selected = pendingDate, onDateSelected = { pendingDate = it /* NO close, NO onValueChange */ }, onMonthChange = { displayMonth = it }, isDisabled = { dateIsDisabled(it, minDate, maxDate, selectableDates) })`  (reuse the internal `dateIsDisabled` from AeroDatePicker — import it).
      - `TimeFields(time = pendingTime, onTimeChange = { pendingTime = it }, showSeconds = showSeconds, minuteStep = minuteStep)`.
      - A `Row` with `AeroOutlinedButton(onClick = { expanded = false }) { Text("Cancel") }` and `AeroButton(onClick = { val d = pendingDate; if (d != null) { onValueChange(combineDateTime(d, pendingTime)); expanded = false } }, enabled = pendingDate != null) { Text("Apply") }`.
    - CRITICAL: `onValueChange` is called ONLY in the Apply onClick. `onDateSelected` only mutates `pendingDate`. Cancel sets `expanded = false` with no callback.
    - Use `Popup` not `Dialog`; no `transparent = true` (W11-01). Add `todayLocalDate()` helper (or import from AeroDatePicker if exposed; otherwise re-declare privately).
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:compileKotlin && grep -c "transparent = true\|androidx.compose.ui.window.Dialog" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDateTimePicker.kt</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public fun AeroDateTimePicker(` with `value: LocalDateTime?` and `onValueChange: (LocalDateTime) -> Unit`.
    - File contains `pendingDate`, `pendingTime`, `AeroCalendarGrid(`, `TimeFields(`, `combineDateTime(`, and an Apply button with `enabled = pendingDate != null`.
    - The calendar's `onDateSelected` lambda does NOT contain `expanded = false` and does NOT contain `onValueChange` — verify: the only `onValueChange(` call in the file is inside the Apply `onClick`. (`grep -c "onValueChange(combineDateTime" AeroDateTimePicker.kt` returns 1; `grep -c "onValueChange(" AeroDateTimePicker.kt` returns 1.)
    - `grep -c "transparent = true" AeroDateTimePicker.kt` returns 0; `grep -c "androidx.compose.ui.window.Dialog" AeroDateTimePicker.kt` returns 0.
    - `./gradlew :library:compileKotlin` exits 0.
  </acceptance_criteria>
  <done>AeroDateTimePicker holds pending date/time, never auto-closes on day click, and emits LocalDateTime only on Apply; Cancel dismisses silently.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroDateTimePickerTest"` green.
- `./gradlew :library:compileKotlin` green.
- In `AeroDateTimePicker.kt`, the only `onValueChange(` call is in the Apply onClick (single hit), proving PICK-04 commit gate.
- `grep -rn "transparent = true" AeroDateTimePicker.kt` returns 0.
</verification>

<success_criteria>
AeroDateTimePicker is a public component combining calendar + time controls in one popup with an Apply/Cancel commit gate; day selection never leaks a value or closes the popup; onValueChange(LocalDateTime) fires only on Apply.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pickers/08-04-SUMMARY.md`.
</output>
