---
phase: 08-pickers
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt
  - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt
  - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorSwatches.kt
  - library/src/test/kotlin/com/mordred/aero/components/pickers/AeroColorPickerTest.kt
autonomous: true
requirements: [PICK-05, PICK-06, PICK-07]
must_haves:
  truths:
    - "HSV square drag and hue strip drag update the color on the first pixel of movement and stay mutually synchronized"
    - "All five controls (HSV square, hue strip, R/G/B sliders, HEX input) stay synced with no round-trip drift: #FF0000 -> sat 50% -> sat 100% reads #FF0000 again (PITFALL-15)"
    - "Clicking a preset swatch sets the active color; a before/after preview bar is shown"
    - "enableAlpha=true adds an alpha slider with checkerboard backdrop and widens HEX to #RRGGBBAA; when false the emitted Color has alpha=1f"
  artifacts:
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt"
      provides: "public inline AeroColorPicker panel; HSV is the single source of truth"
      contains: "public fun AeroColorPicker"
      min_lines: 160
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt"
      provides: "public swatch-trigger Popup wrapper around the same panel"
      contains: "public fun AeroColorPickerButton"
    - path: "library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorSwatches.kt"
      provides: "default ~16-color Aero swatch list"
      contains: "internal val DefaultAeroSwatches"
    - path: "library/src/test/kotlin/com/mordred/aero/components/pickers/AeroColorPickerTest.kt"
      provides: "PICK-05/06/07 round-trip + swatch + alpha tests"
      contains: "class AeroColorPickerTest"
  key_links:
    - from: "AeroColorPicker.kt"
      to: "AeroColorMath (rgbToHsv/hexToRgb/rgbToHex)"
      via: "RGB and HEX derived from HSV; never store Color/RGB alongside HSV"
      pattern: "rgbToHsv|hexToRgb|rgbToHex"
    - from: "AeroColorPicker.kt"
      to: "AeroHsvColorSquare + AeroHueSlider"
      via: "drag callbacks mutate hue/saturation/value floats"
      pattern: "AeroHsvColorSquare|AeroHueSlider"
    - from: "AeroColorPickerButton.kt"
      to: "AeroColorPicker"
      via: "Popup inside trigger Box hosts the same panel"
      pattern: "AeroColorPicker\\("
---

<objective>
Build `AeroColorPicker` (PICK-05/06/07, LARGE): an inline panel wiring the Phase 7 `AeroHsvColorSquare`, `AeroHueSlider`, and `AeroColorMath` plus 3x `AeroSlider` (RGB), an `AeroTextField` (HEX), and a swatch row. HSV(A) floats are the SINGLE source of truth — RGB and HEX are derived on every emit; Color is NEVER stored alongside HSV (PITFALL-15). Also ship `AeroColorPickerButton`, a thin swatch-trigger `Popup` wrapper around the same panel.

Purpose: LARGE component; the no-drift HSV state machine is the crux. All drag uses the Phase 7 awaitPointerEventScope primitives (PITFALL-03 already resolved there).
Output: `AeroColorPicker.kt` (public panel), `AeroColorPickerButton.kt` (public), `AeroColorSwatches.kt` (internal defaults), `AeroColorPickerTest.kt`.
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pickers/08-CONTEXT.md
@.planning/phases/08-pickers/08-RESEARCH.md

<interfaces>
<!-- Phase 7 color primitives — exact signatures (read from source). -->
AeroHsvColorSquare (internal): `AeroHsvColorSquare(hue: Float /*[0,360]*/, saturation: Float /*[0,1]*/, value: Float /*[0,1]*/, onSatValChange: (saturation: Float, value: Float) -> Unit, modifier: Modifier = Modifier)` — 256x256dp; drag fires on first pixel.
AeroHueSlider (internal): `AeroHueSlider(hue: Float, onHueChange: (Float) -> Unit, modifier: Modifier = Modifier)` — 24x256dp.
AeroColorMath (internal, all pure):
  rgbToHsv(r,g,b in [0,1]) -> Triple(hue[0,360], s[0,1], v[0,1])
  hexToRgb(hex) -> Triple(r,g,b)[0,255] or null
  hexToRgba(hex) -> IntArray[r,g,b,a][0,255] or null
  rgbToHex(r,g,b, alpha: Int? = null) -> uppercase hex, no '#'; alpha!=null => RRGGBBAA
AeroSlider (public): `AeroSlider(value, onValueChange, modifier, enabled, valueRange = 0f..1f, steps, showTooltip)` — use valueRange = 0f..255f for R/G/B.
AeroTextField (public): `AeroTextField(value, onValueChange, modifier, enabled, readOnly, placeholder, trailingIcon, height, ...)`.
NEW-PICK-01: Color.hsv(h,s,v,a) throws if hue outside [0,360]; always coerce via a safeHsvColor wrapper.
NEW-PICK-05: AeroColorPickerButton's Popup MUST be inside the trigger Box so it anchors to the swatch.
PITFALL-15: HSV(A) floats are the only stored state; Color/RGB/HEX are derived per emit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Default swatches + HSV round-trip derivation helpers + tests</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorSwatches.kt, library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt, library/src/test/kotlin/com/mordred/aero/components/pickers/AeroColorPickerTest.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorMath.kt (rgbToHsv/hexToRgb/hexToRgba/rgbToHex contracts)
    - library/src/test/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorMathTest.kt (existing round-trip test style; build on it)
    - library/src/main/kotlin/com/mordred/aero/theme/AeroColorScheme.kt (palette reference for the 16 swatch colors)
  </read_first>
  <behavior>
    - safeHsvColor coerces hue into [0,360] and s/v/a into [0,1] before Color.hsv (no throw on hue==360f or hue<0f).
    - Round-trip (PITFALL-15): pure red HSV (0f,1f,1f) -> derive HEX -> "FF0000"; driving saturation to 0.5 then back to 1.0 and deriving HEX again still yields "FF0000".
    - hexToHsv: valid "#FF0000" parses to (hue,sat,value) approx (0f,1f,1f) within tolerance; invalid/partial "#FF" returns null (length 6/8 guard inside).
    - alpha: hsvToHex with includeAlpha=false yields a 6-char string; with includeAlpha=true yields 8 chars.
  </behavior>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroColorSwatches.kt`, package `com.mordred.aero.components.pickers.internal.color`:
    `internal val DefaultAeroSwatches: List<Color> = listOf( ... )` with EXACTLY 16 Aero-fit colors (pure primaries red/orange/yellow/green/cyan/blue/violet/magenta + white/black + 3-4 grays + a couple Aero blues). Claude's discretion on exact values.

    In `AeroColorPicker.kt` (package `com.mordred.aero.components.pickers`) add internal pure helpers (testable without the Compose UI harness):
    - `internal fun safeHsvColor(hue: Float, sat: Float, v: Float, alpha: Float = 1f): Color = Color.hsv(hue.coerceIn(0f,360f), sat.coerceIn(0f,1f), v.coerceIn(0f,1f), alpha.coerceIn(0f,1f))`
    - `internal fun hsvToHex(hue: Float, sat: Float, v: Float, alpha: Float, includeAlpha: Boolean): String` — derive via safeHsvColor -> r,g,b[,a] ints (round of channel*255) -> `rgbToHex(r,g,b, if (includeAlpha) a else null)`.
    - `internal fun hexToHsv(hex: String): FloatArray?` — strip '#'; only proceed when cleaned length is 6 or 8; use `hexToRgba` then `rgbToHsv`; return `floatArrayOf(hue, sat, value, alpha)` or null.

    Then create `library/src/test/kotlin/com/mordred/aero/components/pickers/AeroColorPickerTest.kt`, package `com.mordred.aero.components.pickers`, kotlin-test + JUnit Jupiter style. Cover every behavior bullet. Include explicitly:
    - the drift gate: a test that computes `hsvToHex` for pure-red HSV, then simulates sat 0.5 -> 1.0, and asserts the final `hsvToHex(... , includeAlpha=false)` equals `"FF0000"`.
    - `DefaultAeroSwatches.size == 16`.
    - hexToHsv("#FF") == null and hexToHsv("FF0000") non-null.
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroColorPickerTest" -x :showcase:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `AeroColorSwatches.kt` contains `internal val DefaultAeroSwatches`; a test asserts its size == 16.
    - `AeroColorPicker.kt` contains `internal fun safeHsvColor(`, `internal fun hsvToHex(`, `internal fun hexToHsv(`.
    - `AeroColorPickerTest.kt` contains `class AeroColorPickerTest`; `grep -c "@Test"` returns >= 5; one test body contains `"FF0000"` (drift gate).
    - The gradle test command exits 0.
  </acceptance_criteria>
  <done>HSV<->HEX derivation round-trips with no drift; safeHsvColor never throws; 16 default swatches defined; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: AeroColorPicker inline panel (HSV truth + 5 synced controls + swatches + preview + alpha)</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt (helpers from Task 1)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroHsvColorSquare.kt (onSatValChange contract)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/internal/color/AeroHueSlider.kt (onHueChange contract)
    - library/src/main/kotlin/com/mordred/aero/components/range/AeroSlider.kt (RGB slider usage, valueRange)
    - library/src/main/kotlin/com/mordred/aero/components/input/AeroTextField.kt (HEX input)
    - library/src/main/kotlin/com/mordred/aero/theme/GlassModifiers.kt (glassPanel for the panel surface)
  </read_first>
  <action>
    Add the public inline panel to `AeroColorPicker.kt`:

    Signature:
    `public fun AeroColorPicker(value: Color, onValueChange: (Color) -> Unit, modifier: Modifier = Modifier, enableAlpha: Boolean = false, swatches: List<Color> = DefaultAeroSwatches, enabled: Boolean = true)`

    State (PITFALL-15 — store ONLY HSV(A) floats; never store Color/RGB):
    - Initialize ONCE from `value`: `rgbToHsv(value.red, value.green, value.blue)` -> seed `var hue`, `var saturation`, `var brightness` (name it `brightness` to avoid clashing with the `value: Color` param) via `remember { mutableFloatStateOf(...) }`; `var alpha by remember { mutableFloatStateOf(value.alpha) }`.
    - `val currentColor = safeHsvColor(hue, saturation, brightness, if (enableAlpha) alpha else 1f)`.
    - Emit derived (never stored): `LaunchedEffect(hue, saturation, brightness, alpha) { onValueChange(currentColor) }`.
    - `var beforeColor by remember { mutableStateOf(value) }` captured once for the preview bar.

    Layout top->bottom inside `Column(modifier.glassPanel(cornerRadius = 8.dp).padding(12.dp), verticalArrangement = Arrangement.spacedBy(8.dp))`:
    1. `Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) { AeroHsvColorSquare(hue, saturation, brightness, onSatValChange = { s, v -> saturation = s; brightness = v }); AeroHueSlider(hue, onHueChange = { hue = it }) }`.
    2. Preview bar: a Row of two boxes — left filled `beforeColor`, right filled `currentColor` (each `Modifier.weight(1f).height(24.dp)`).
    3. RGB sliders: for Red `AeroSlider(value = currentColor.red * 255f, onValueChange = { r -> val (h,s,v) = rgbToHsv(r/255f, currentColor.green, currentColor.blue); hue = h.coerceIn(0f,360f); saturation = s; brightness = v }, valueRange = 0f..255f)`; analogous for Green and Blue. Label each with a `Text` ("R"/"G"/"B").
    4. HEX input: `AeroTextField` whose value is `hsvToHex(hue, saturation, brightness, alpha, enableAlpha)` when not editing; on Enter (KeyboardActions onDone) and on focus-loss (`Modifier.onFocusChanged { if (!it.isFocused) commit() }`), `commit()` runs `hexToHsv(text)?.let { hue = it[0].coerceIn(0f,360f); saturation = it[1]; brightness = it[2]; if (enableAlpha) alpha = it[3] }` (null = ignore, keep state).
    5. If `enableAlpha`: alpha `AeroSlider(value = alpha, onValueChange = { alpha = it }, valueRange = 0f..1f)` rendered over a checkerboard via `Modifier.drawBehind { /* 8dp tiles, white / lightGray alternating */ }`.
    6. Swatch row: `swatches.forEach { sw -> Box(Modifier.size(20.dp).background(sw).clickable { val (h,s,v) = rgbToHsv(sw.red, sw.green, sw.blue); hue = h.coerceIn(0f,360f); saturation = s; brightness = v; if (enableAlpha) alpha = sw.alpha }) }`.

    Rules: every `Color.hsv` construction goes through `safeHsvColor` (NEW-PICK-01). Do NOT store any `Color`/RGB-int as state (HSV(A) floats + the one-shot `beforeColor` snapshot are the only state). The component is `public` (explicitApi).
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:compileKotlin && grep -c "AeroHsvColorSquare\|AeroHueSlider\|hsvToHex\|safeHsvColor" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public fun AeroColorPicker(` with `value: Color` and `onValueChange: (Color) -> Unit` and `enableAlpha: Boolean = false`.
    - File contains `AeroHsvColorSquare(`, `AeroHueSlider(`, three `AeroSlider(` calls with `valueRange = 0f..255f`, `AeroTextField(`, and `swatches.forEach`.
    - File contains `safeHsvColor(` for every emit and `LaunchedEffect(` driving `onValueChange`.
    - File does NOT store a Color/RGB as mutableState: `grep -c "mutableStateOf(currentColor)\|by remember.*Color>(.*onValueChange" AeroColorPicker.kt` returns 0 (only HSV floats + beforeColor snapshot allowed).
    - `./gradlew :library:compileKotlin` exits 0.
  </acceptance_criteria>
  <done>AeroColorPicker renders all five controls synchronized from a single HSV(A) state, shows before/after preview, supports swatches and optional alpha; drift-free per Task 1 tests.</done>
</task>

<task type="auto">
  <name>Task 3: AeroColorPickerButton (swatch trigger + Popup wrapping the panel)</name>
  <files>library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt</files>
  <read_first>
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt (the panel to wrap)
    - library/src/main/kotlin/com/mordred/aero/components/internal/popup/AeroCalendarPositionProvider.kt (position provider)
    - library/src/main/kotlin/com/mordred/aero/components/pickers/AeroDatePicker.kt (Popup-inside-trigger-Box anchoring pattern, NEW-PICK-05)
  </read_first>
  <action>
    Create `library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt`, package `com.mordred.aero.components.pickers`:

    Signature:
    `public fun AeroColorPickerButton(value: Color, onValueChange: (Color) -> Unit, modifier: Modifier = Modifier, enableAlpha: Boolean = false, swatches: List<Color> = DefaultAeroSwatches, enabled: Boolean = true)`

    Implementation (concrete):
    - `var expanded by remember { mutableStateOf(false) }`.
    - Trigger: `Box(modifier.size(32.dp).clip(RoundedCornerShape(4.dp)).background(value).border(1.dp, AeroTheme.colors.glassBorder, RoundedCornerShape(4.dp)).clickable(enabled = enabled) { expanded = !expanded })` — a swatch showing the current `value`.
    - Popup INSIDE that same trigger Box (NEW-PICK-05 — must anchor to the swatch): `if (expanded) Popup(popupPositionProvider = remember { AeroCalendarPositionProvider(gap = 4) }, onDismissRequest = { expanded = false }, properties = PopupProperties(focusable = true, dismissOnBackPress = true, dismissOnClickOutside = true)) { AeroColorPicker(value = value, onValueChange = onValueChange, enableAlpha = enableAlpha, swatches = swatches, enabled = enabled) }`.
    - The panel already carries its own glassPanel surface, so no extra PickerPopupContainer needed (but it is acceptable to wrap for visual consistency).
    - Use `Popup` not `Dialog`; no `transparent = true` (W11-01).
  </action>
  <verify>
    <automated>cd C:/1A_WORK/ui_lib && ./gradlew :library:compileKotlin && grep -c "transparent = true\|androidx.compose.ui.window.Dialog" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public fun AeroColorPickerButton(` and an `AeroColorPicker(` call inside a `Popup(`.
    - The `Popup(` call uses `AeroCalendarPositionProvider` and `PopupProperties(`.
    - `grep -c "transparent = true" AeroColorPickerButton.kt` returns 0; `grep -c "androidx.compose.ui.window.Dialog" AeroColorPickerButton.kt` returns 0.
    - `./gradlew :library:compileKotlin` exits 0.
  </acceptance_criteria>
  <done>AeroColorPickerButton is a public swatch trigger whose anchored Popup hosts the AeroColorPicker panel; no transparent dialog.</done>
</task>

</tasks>

<verification>
- `./gradlew :library:test --tests "com.mordred.aero.components.pickers.AeroColorPickerTest"` green (includes the #FF0000 drift gate).
- `./gradlew :library:compileKotlin` green with explicitApi.
- `grep -rn "transparent = true" library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPicker.kt library/src/main/kotlin/com/mordred/aero/components/pickers/AeroColorPickerButton.kt` returns 0.
- No stored Color/RGB state in AeroColorPicker — HSV(A) floats are the single source of truth.
</verification>

<success_criteria>
AeroColorPicker (inline) and AeroColorPickerButton (popup) are public; all five controls stay synchronized from a single HSV(A) source with no round-trip drift; swatches and a before/after preview work; enableAlpha adds a checkerboard alpha slider and #RRGGBBAA HEX, and is opaque when disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pickers/08-06-SUMMARY.md`.
</output>
