---
phase: 04-aeroicons-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/research/SUMMARY.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/STACK.md
  - tools/phosphor-svgs/.pin
  - tools/phosphor-svgs/LICENSE
  - tools/phosphor-svgs/README.md
  - tools/phosphor-svgs/regular/x.svg
  - tools/phosphor-svgs/regular/caret-down.svg
  - tools/phosphor-svgs/regular/magnifying-glass.svg
  - tools/phosphor-svgs/regular/check.svg
  - tools/phosphor-svgs/regular/info.svg
  - tools/phosphor-svgs/regular/frame-corners.svg
  - tools/phosphor-svgs/regular/square.svg
  - library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/X.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/CaretDown.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/MagnifyingGlass.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/Check.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/Info.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/FrameCorners.kt
  - library/src/main/kotlin/com/mordred/aero/icons/internal/Square.kt
autonomous: false
requirements:
  - ICN-01
  - ICN-02
  - ICN-03

must_haves:
  truths:
    - "All planning + research docs reference 138 icons (not 139) and strokeLineWidth=16f (not 12f)"
    - "tools/phosphor-svgs/ contains 7 vendored Phosphor Regular SVGs at a pinned commit SHA, plus LICENSE and README"
    - "library/src/main/kotlin/com/mordred/aero/icons/internal/ contains 7 generated Kotlin files (X, CaretDown, MagnifyingGlass, Check, Info, FrameCorners, Square)"
    - "library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt declares public object AeroIcons with 7 alphabetized lazy-cached ImageVector properties (or matches Valkyrie BackingProperty extension-property output if that is the actual generated shape) and an object-level KDoc covering 6+ structural requirements"
    - "./gradlew :library:compileKotlin succeeds with the icons/ package present, satisfying explicitApi()"
  artifacts:
    - path: "tools/phosphor-svgs/.pin"
      provides: "Phosphor source revision lock — line 1 is the 40-char SHA, line 2 is the GitHub tree URL"
    - path: "tools/phosphor-svgs/LICENSE"
      provides: "Verbatim Phosphor MIT license text"
    - path: "tools/phosphor-svgs/README.md"
      provides: "Valkyrie command, what was pinned, recovery instructions for unvendored icons"
      contains: "valkyrie svgxml2imagevector"
    - path: "tools/phosphor-svgs/regular/"
      provides: "7 vendored stroke-based Phosphor Regular SVGs"
      contains: "x.svg, caret-down.svg, magnifying-glass.svg, check.svg, info.svg, frame-corners.svg, square.svg"
    - path: "library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt"
      provides: "Public facade: object AeroIcons with object-level KDoc + 7 alphabetized lazy ImageVector properties"
      exports: ["AeroIcons"]
      min_lines: 60
    - path: "library/src/main/kotlin/com/mordred/aero/icons/internal/"
      provides: "7 Valkyrie-generated builder files; each contains viewportWidth=256f, strokeLineWidth=16f, StrokeCap.Round, StrokeJoin.Round"
  key_links:
    - from: "library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt"
      to: "library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt"
      via: "loadX()/loadCaretDown()/etc. function calls — OR — extension property bodies if Valkyrie generates pattern A"
      pattern: "(loadX\\(\\)|val AeroIcons\\.X)"
    - from: "tools/phosphor-svgs/regular/*.svg"
      to: "library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt"
      via: "Valkyrie CLI 1.1.1 svgxml2imagevector batch invocation"
      pattern: "viewportWidth = 256f"
    - from: "tools/phosphor-svgs/.pin"
      to: "https://github.com/phosphor-icons/core/raw/regular/"
      via: "40-char commit SHA on line 1; tree URL on line 2"
      pattern: "^[0-9a-f]{40}$"
---

<objective>
Phase 4 spike plan. Two distinct deliverables:

1. **Doc corrections (atomic first commit):** propagate two research-uncovered fixes through every planning/research document so the rest of execution has a single canonical source of truth — (a) icon count 139→138 (FEATURES.md row #139 `arrow-up-right` is a duplicate of row #10), and (b) Phosphor Regular stroke-width 12→16 (direct SVG inspection contradicts prior research; Valkyrie translates `stroke-width="16"` faithfully to `strokeLineWidth = 16f`).

2. **AeroIcons spike (7 icons end-to-end):** vendor 7 representative Phosphor SVGs, run Valkyrie 1.1.1 once to generate 7 `internal/*.kt` builder files, author `AeroIcons.kt` facade with 7 alphabetized lazy properties + full object-level KDoc, prove `./gradlew :library:compileKotlin` passes under `explicitApi()`, and confirm Valkyrie's actual output shape (extension property vs `internal fun loadX()`) before scaling to 138.

Purpose: De-risk the full 138-icon batch (Plan 04-02) by discovering ALL of Valkyrie's behaviours (output-shape, file placement, AeroIcons.kt regeneration question, explicit-mode visibility) on a 7-icon spike where mistakes are cheap. Lock in icon-spec correctness (256-unit viewBox, 16-unit stroke, rounded caps) on a sample that includes the two glassmorphic-background-sensitive icons (FrameCorners, Square) that PITFALLS.md flagged.

Output:
- 9 corrected planning/research docs
- `tools/phosphor-svgs/` directory: `.pin`, `LICENSE`, `README.md`, `regular/` with 7 SVGs
- `library/src/main/kotlin/com/mordred/aero/icons/internal/` with 7 generated `.kt` files
- `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` facade (7 props + KDoc)
- Green `./gradlew :library:compileKotlin`
</objective>

<execution_context>
@C:/Users/1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-aeroicons-foundation/04-CONTEXT.md
@.planning/phases/04-aeroicons-foundation/04-RESEARCH.md
@.planning/phases/04-aeroicons-foundation/04-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@library/build.gradle.kts

<interfaces>
<!-- Existing :library context the spike must respect. No new types created here that other plans depend on; 04-02 will extend AeroIcons.kt. -->

From library/build.gradle.kts (lines 1–32):
```kotlin
kotlin {
    jvmToolchain(17)
    explicitApi()  // ← every public declaration must specify visibility
}

dependencies {
    implementation(compose.desktop.common)
    implementation(compose.material3)
    implementation(compose.materialIconsExtended)  // ← Phase 4 leaves this; Phase 5 removes
    implementation(compose.foundation)
    implementation(compose.ui)
    // ...
}
```

Phase 4 must NOT modify build.gradle.kts. `explicitApi()` is active — every `val` in `AeroIcons` MUST be prefixed `public`.

From androidx.compose.ui.graphics.vector (CMP 1.7.3 transitive on compose.ui):
```kotlin
public class ImageVector
public class ImageVector.Builder(
    name: String,
    defaultWidth: Dp,
    defaultHeight: Dp,
    viewportWidth: Float,
    viewportHeight: Float,
    /* ... */
)
public fun ImageVector.Builder.path(
    fill: Brush? = null,
    stroke: Brush? = null,
    strokeLineWidth: Float = 0f,
    strokeLineCap: StrokeCap = StrokeCap.Butt,
    strokeLineJoin: StrokeJoin = StrokeJoin.Miter,
    /* ... */
    pathBuilder: PathBuilder.() -> Unit
): ImageVector.Builder
```

From androidx.compose.ui.graphics:
```kotlin
public sealed class Brush
public class SolidColor(value: Color) : Brush
public object Color { public val Transparent: Color; public val Black: Color }
public enum class StrokeCap { Butt, Round, Square }
public enum class StrokeJoin { Miter, Round, Bevel }
```

Generated `internal/*.kt` files MUST use these types. Valkyrie 1.1.1 produces them automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic doc-corrections commit — propagate 139→138 and strokeLineWidth=12→16 across all planning + research docs</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md,
    .planning/research/SUMMARY.md,
    .planning/research/FEATURES.md,
    .planning/research/ARCHITECTURE.md,
    .planning/research/PITFALLS.md,
    .planning/research/STACK.md
  </files>
  <read_first>
    .planning/phases/04-aeroicons-foundation/04-CONTEXT.md (decisions block — locks the count fix as the FIRST commit),
    .planning/phases/04-aeroicons-foundation/04-RESEARCH.md (Pitfall 1 / Summary — locks strokeLineWidth=16 correction),
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (rows 04-01-01 and 04-01-02 — exact grep gates),
    .planning/ROADMAP.md (Phase 4 §Success Criteria, Phase 4 §Phase notes),
    .planning/REQUIREMENTS.md (ICN-03 line),
    .planning/STATE.md (`[v1.1 locked decisions]` block),
    .planning/research/SUMMARY.md (Executive Summary + Recommended Stack + Phase 4 deliverables),
    .planning/research/FEATURES.md (Context block + Part 3 master list incl. row #139 + Kotlin Implementation Pattern),
    .planning/research/ARCHITECTURE.md (Source-Revision block + Pattern 1 worked example),
    .planning/research/PITFALLS.md (Source-Revision block + Pitfall 1/6/9/12),
    .planning/research/STACK.md (Source-Revision block + ImageVector.Builder constants table)
  </read_first>
  <action>
This is a multi-file find-and-replace pass. Make EVERY change below in the SAME commit. Do not split.

Each edit is specified as `FILE :: SEARCH → REPLACE` with surrounding context where needed for unambiguous matching. After the edits, run the two grep gates from VALIDATION.md rows 04-01-01 and 04-01-02 and confirm zero hits.

═══════════════════════════════════════════════════════════════
PART A — Count fix (139 → 138). Distinguish "icon count" hits from "row #139" hits.
═══════════════════════════════════════════════════════════════

A1. **`.planning/ROADMAP.md`**

  - Line containing `Phase 4 — AeroIcons Foundation | 139 Phosphor Regular ImageVector constants`
    → change to `Phase 4 — AeroIcons Foundation | 138 Phosphor Regular ImageVector constants` (in the table near line 43).

  - Phase 4 description bullet: `**Phase 4: AeroIcons Foundation** - Valkyrie CLI conversion of 139 Phosphor Regular SVGs;`
    → `**Phase 4: AeroIcons Foundation** - Valkyrie CLI conversion of 138 Phosphor Regular SVGs;`

  - Success criterion 1: `every one of the 139 Phosphor Regular ImageVector constants`
    → `every one of the 138 Phosphor Regular ImageVector constants`

  - Success criterion 5: any reference to "of the 139" → "of the 138"

  - Phase 5 phase notes: `Phosphor stroke at the button's render size` etc. — leave Phase 5 stroke math alone in this commit (Phase 5 plan owns it). DO update Phase 6 success-criterion 1 references: `displays all 139 icons` → `displays all 138 icons` (twice in Phase 6 success criteria), `restores all 139 icons` → `restores all 138 icons`, `all 139 icons are visible` → `all 138 icons are visible` (3 instances in Phase 6 success criteria 1, 2, 3, 4 — replace every "139" referring to icon count).

  - Phase 6 phase notes: `LazyVerticalGrid of all 139 icons` → `LazyVerticalGrid of all 138 icons` (if present; check both Phase 5 and Phase 6 sections).

A2. **`.planning/REQUIREMENTS.md`**

  - Section §v1.1 Requirements intro: `(порт Phosphor Regular, ~139 шт.)` → `(порт Phosphor Regular, ~138 шт.)`

  - **ICN-03**: `139 ImageVector-констант (порт Phosphor Regular, viewBox 256×256, stroke 12, rounded caps/joins, defaultWidth=defaultHeight=24.dp) доступны через AeroIcons.* autocomplete; полный список соответствует Part 3 из .planning/research/FEATURES.md`
    → `138 ImageVector-констант (порт Phosphor Regular, viewBox 256×256, stroke 16, rounded caps/joins, defaultWidth=defaultHeight=24.dp) доступны через AeroIcons.* autocomplete; полный список соответствует Part 3 из .planning/research/FEATURES.md`
    (note: this combines Part A count fix + Part B stroke fix in a single line — apply both at once)

  - SHW-04: `LazyVerticalGrid всех 139 иконок` → `LazyVerticalGrid всех 138 иконок`

A3. **`.planning/STATE.md`**

  - `## v1.1 Phase Summary` table row: `4 — AeroIcons Foundation | 139 Phosphor Regular ImageVector constants, lazy init, KDoc, explicitApi`
    → `4 — AeroIcons Foundation | 138 Phosphor Regular ImageVector constants, lazy init, KDoc, explicitApi`

  - `[v1.1 locked decisions]` block: scan every bullet for `139`. The known target is the bullet beginning `Lazy backing-property pattern mandatory for all 139 constants`
    → `Lazy backing-property pattern mandatory for all 138 constants`

A4. **`.planning/research/SUMMARY.md`**

  Replace ALL occurrences of `139` (when referring to icon count) with `138`:
  - Line in Executive Summary: `unified typed vector icon set ... ~139 typed constants` → `~138 typed constants`
  - Section §Architecture: `139 typed ImageVector constants` (multiple instances) → `138 typed ImageVector constants`
  - Section §Critical Pitfalls: `at 139 icons` → `at 138 icons` (if present)
  - Phase 4 section: `139 Phosphor Regular SVGs` → `138 Phosphor Regular SVGs`; `139-icon set` → `138-icon set`; `the 139 ImageVector constants` → `the 138 ImageVector constants`
  - Phase 6 section: `all 139 icons` (multiple instances) → `all 138 icons`
  - Section §Confidence Assessment: any `139` icon-count reference → `138`
  - Section §Standard Stack: `defaultWidth = 24.dp` block — leave unchanged; Phosphor Regular `viewportWidth = 256f` — unchanged.

  Replace stroke-12 references with stroke-16:
  - Section §Recommended Stack: `stroke-width 12 spec` → `stroke-width 16 spec`; `strokeLineWidth = 12f (Feather was 2f -- Valkyrie handles this automatically)` → `strokeLineWidth = 16f (Valkyrie handles this automatically based on the source SVG stroke-width attribute)`
  - Section §Recommended Stack: `viewBox 256x256, stroke-width 12, stroke-linecap round` → `viewBox 256x256, stroke-width 16, stroke-linecap round`
  - Any subsequent reference to `stroke-width 12` or `strokeLineWidth=12f` or `stroke 12` (icon-spec context) → `stroke-width 16` / `strokeLineWidth=16f` / `stroke 16` accordingly.

A5. **`.planning/research/FEATURES.md`**

  - Top-of-doc context block: `Stroke-width: 12 (equivalent ~1.5 px at 24 dp render)` → `Stroke-width: 16 (equivalent ~1.5 px at 24 dp render)`
  - REVISION NOTE block (top): no change to "Phosphor names replace Feather names" content; this commit doesn't touch Feather→Phosphor substitution narrative.
  - §Part 3 (Master Reference) heading: `Complete v1.1 Icon List (Master Reference — ~140 icons)` → `Complete v1.1 Icon List (Master Reference — 138 icons)`
  - §Part 3 master table: **DELETE the entire row #139** (`| 139 | arrow-up-right | ArrowUpRight | Actions | |`). The icon already exists at row #10. Confirm the table now ends at row #138 (`x-circle | XCircle | Actions/Status | YES`).
  - §Part 3 footer: `**Total: 139 icons** (15 required for migration + 124 standard).` → `**Total: 138 icons** (17 required for migration + 121 standard).` (Required count of 17 already correct in the body; 138 - 17 = 121 standard.)
  - §Part 3 footer: `Within the 120–150 target.` — leave unchanged.
  - §Part 4 (Naming Convention Decision): any `~139 icons` or `139 icons` count reference → `138 icons`.
  - Any `139` in "all 139 icons" / "the 139 icons" anywhere else in the doc → `138`.
  - §Part 1 stroke reference (if present in any code example showing `strokeLineWidth = 12f`): change to `strokeLineWidth = 16f`.

A6. **`.planning/research/ARCHITECTURE.md`**

  - SOURCE REVISION block code example (lines ~22–28): `strokeLineWidth = 12f` → `strokeLineWidth = 16f`. Also update the surrounding prose `Only viewportWidth/viewportHeight (24f → 256f) and strokeLineWidth (2f → 12f) change` → `Only viewportWidth/viewportHeight (24f → 256f) and strokeLineWidth (2f → 16f) change`.
  - §1 Package layout: any `~140` or `140 icons` or `139 icons` icon-count reference → `138 icons`. The diagram uses `~140 in one package` — update to `138 in one package`.
  - §2 Public API Surface — Pattern 1 worked example: if any `strokeLineWidth = 12f` literal appears in a per-icon code snippet, change to `strokeLineWidth = 16f`. Per-icon worked example (close.kt) currently shows `strokeLineWidth = 2f` (Feather legacy) — leave that one alone (it's Feather not Phosphor) OR update if it's been migrated to Phosphor in a previous edit. **Heuristic:** if the snippet declares `viewportWidth = 256f`, the stroke must be `16f`; if it declares `viewportWidth = 24f`, leave the snippet (Feather example).
  - Anti-Patterns section / `Sizing Convention` / any other `139` icon-count → `138`.
  - §6 Showcase: `all ~140 icons in alphabetical order` → `all 138 icons in alphabetical order`.

A7. **`.planning/research/PITFALLS.md`**

  - SOURCE REVISION block (top), Pitfall 1/6/11 calibration: `Phosphor Regular stroke is 12 in a 256-unit viewBox = 12/256 ≈ 4.7%` → `Phosphor Regular stroke is 16 in a 256-unit viewBox = 16/256 ≈ 6.25%`. **CRITICAL: also update the derived numbers**: `at 24dp render the stroke is 24 × 0.047 ≈ 1.13dp` → `at 24dp render the stroke is 24 × 0.0625 = 1.5dp`; `at 16dp render it's 16 × 0.047 ≈ 0.75dp` → `at 16dp render it's 16 × 0.0625 = 1.0dp`; `at 10dp ... it's 10 × 0.047 ≈ 0.47dp — sub-pixel at 96 DPI` → `at 10dp ... it's 10 × 0.0625 ≈ 0.63dp — sub-pixel at 96 DPI`.
  - Pitfall 12 in the SOURCE REVISION block: `strokeLineWidth must be 12f (NOT 2f)` → `strokeLineWidth must be 16f (NOT 2f)`.
  - Pitfall 9 (SVG-to-ImageVector Conversion Artifacts): `viewportWidth = 24f` references in Feather examples — leave (those describe the Feather artifact). If the doc shows `strokeLineWidth = 12f` as the post-conversion target, change to `16f`.
  - "Looks Done But Isn't" Checklist: `Spot-check 5 icons: fill=transparent, viewportWidth=24f, strokeLineCap=Round` — DO NOT change `viewportWidth=24f` here (this is a CHECK that should FAIL after conversion to 256f). HOWEVER, this whole checklist line is from the Feather legacy section. Look for explicit Phosphor-revised wording elsewhere; if missing, leave checklist as-is (it's superseded by VALIDATION.md per-task table).
  - Any `120-150` icon-count range or `139 icons` in Pitfalls discussion → `138 icons`.

A8. **`.planning/research/STACK.md`**

  - SOURCE REVISION table row: `stroke-width attribute | 2 | 12 (in 256-unit space — equivalent visual weight to ~1.13px when rendered at 24dp; matches Aero soft-outline target)`
    → `stroke-width attribute | 2 | 16 (in 256-unit space — equivalent visual weight to ~1.5px when rendered at 24dp; matches Aero soft-outline target)`
  - SOURCE REVISION ImageVector.Builder constants list: `strokeLineWidth = 12f (in viewport units; was 2f)` → `strokeLineWidth = 16f (in viewport units; was 2f)`
  - §v1.1 Addition / Generated Code Pattern: if `strokeLineWidth = 12f` appears in any code block claiming Phosphor output, change to `16f`. The legacy `Close.kt` example uses `strokeLineWidth = 2f` (Feather) and `viewportWidth = 24f` — leave that intact (it's the Feather example explicitly).
  - Any other `12f` Phosphor-context stroke reference → `16f`.
  - Any `~120-150` icon-count range that should be the locked 138 → `138`. (Don't aggressively rewrite; the doc has historical ranges. Focus only on lines that claim "the v1.1 set is ~140" — change to "138".)

**Note on 04-CONTEXT.md:** The orchestrator already corrected stroke-width 12→16, the Valkyrie flag form (`backing-property` lowercase), and the raw/regular filename pattern (no `-regular` suffix) in commit 65e5010. CONTEXT.md is NOT in this task's `<files>` list. Do NOT re-edit those items in CONTEXT.md. Any remaining "139" tokens in CONTEXT.md are descriptive references to the FEATURES.md row-#139 duplicate or to "icons referenced as 139 in the OTHER docs" — they are correctly contextualized as historical facts and should NOT be touched.

═══════════════════════════════════════════════════════════════
PART B — Final verification (run BEFORE committing)
═══════════════════════════════════════════════════════════════

After all edits above, run these greps. They MUST be empty (zero results) — if anything remains it is an oversight to be fixed in this same commit:

```bash
# Gate 1: no remaining 139-icon-count references in planning/research docs
# (NOTE: row tables like ROADMAP "Phase 1 — 13 reqs" naturally contain non-139 numbers; we only care about literal "139" tokens)
grep -nE "\b139\b" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md \
  .planning/research/SUMMARY.md .planning/research/FEATURES.md \
  .planning/research/ARCHITECTURE.md .planning/phases/04-aeroicons-foundation/04-CONTEXT.md
# Acceptable hits: a row labelled `139` only if it's outside the icon-count discussion (verify by hand).
# All "139 icons" / "all 139" / "139 ImageVector" / "the 139" must be gone.

# Gate 2: no Phosphor-context strokeLineWidth=12 references in planning/research docs
grep -nE "strokeLineWidth\s*=\s*12f|stroke-width.*\b12\b|stroke 12\b" .planning/research/ \
  .planning/phases/04-aeroicons-foundation/04-CONTEXT.md .planning/REQUIREMENTS.md
# Acceptable hits: NONE in Phosphor context. Feather legacy code blocks (viewportWidth=24f surrounding) may remain UNCHANGED — those describe the OLD Feather code.
```

If gates fail: read each remaining hit and decide whether it is Phosphor-context (must fix) or Feather-legacy-example (leave). Re-run until clean. Then commit:

```
git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md \
  .planning/research/SUMMARY.md .planning/research/FEATURES.md \
  .planning/research/ARCHITECTURE.md .planning/research/PITFALLS.md \
  .planning/research/STACK.md
git commit -m "docs(04): correct icon count 139→138 and Phosphor stroke 12→16

- FEATURES.md row #139 (arrow-up-right) was a duplicate of row #10; total is 138 unique icons.
- Direct inspection of phosphor-icons/core raw/regular/*.svg shows stroke-width=16, not 12.
  Valkyrie translates this faithfully to strokeLineWidth=16f in generated Kotlin.
- Recalibrated PITFALLS.md sub-pixel math: stroke fraction is 16/256 (6.25%), not 12/256 (4.7%).
- Single source-of-truth fix before any code lands."
```
  </action>
  <verify>
    <automated>grep -nE "\b139\b" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/research/SUMMARY.md .planning/research/FEATURES.md .planning/research/ARCHITECTURE.md ; echo "---" ; grep -nE "strokeLineWidth\s*=\s*12f|stroke-width.*\b12\b|stroke 12\b" .planning/research/ .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - First grep (icon-count): no "139 icons" / "all 139" / "139 ImageVector" / "the 139" / "of 139" / "139 typed" hits in any of the 7 listed files. Any surviving `139` token must be unrelated to icon count (e.g., a row number in an unrelated table) and verified by hand.
    - Second grep (stroke 12): zero hits across `.planning/research/` and the listed phase/requirements docs in PHOSPHOR-context (i.e. paragraphs that also mention 256 or Phosphor). Surviving `12f` literals are acceptable only when surrounded by `viewportWidth = 24f` (Feather legacy code blocks).
    - `git log -1 --format=%s` shows `docs(04): correct icon count 139→138 and Phosphor stroke 12→16`.
    - Single commit only — no doc edits split across multiple commits.
  </acceptance_criteria>
  <done>Both grep gates green; single commit on master; all 8 files modified (ROADMAP / REQUIREMENTS / STATE / 5 research docs); each references 138 (not 139) for icon count and 16 (not 12) for Phosphor stroke-width. CONTEXT.md was already corrected by the orchestrator in commit 65e5010 — not touched here.</done>
</task>

<task type="auto">
  <name>Task 2: Pin Phosphor revision — write .pin, vendor LICENSE, write README with Valkyrie command + recovery instructions</name>
  <files>
    tools/phosphor-svgs/.pin,
    tools/phosphor-svgs/LICENSE,
    tools/phosphor-svgs/README.md
  </files>
  <read_first>
    .planning/phases/04-aeroicons-foundation/04-CONTEXT.md (§Decisions/Phosphor SVG vendoring — locks .pin format and README contents),
    .planning/phases/04-aeroicons-foundation/04-RESEARCH.md (§Pitfall 7 — .pin format example with two lines; §Common Pitfalls / §Code Examples — exact Valkyrie command),
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (row 04-01-03 — file existence check)
  </read_first>
  <action>
Vendor Phosphor's MIT license and write a self-contained provenance + recovery package in `tools/phosphor-svgs/`.

═══════════════════════════════════════════════════════════════
STEP 1 — Resolve the pinned commit SHA (DO NOT trust the date in 04-RESEARCH.md verbatim)
═══════════════════════════════════════════════════════════════

The 04-RESEARCH.md proposes SHA `2b75f3ad12b420c9504ef05df8d2564a28f8500e` but flags "verify before pinning". Verify by hitting the GitHub API for the current `main` HEAD:

```bash
SHA=$(curl -fsSL https://api.github.com/repos/phosphor-icons/core/commits/main \
  | grep -E '^  "sha"' | head -1 | sed 's/.*"\([0-9a-f]\{40\}\)".*/\1/')
echo "Phosphor main HEAD = $SHA"
# Sanity-check: SHA must be exactly 40 hex chars
test ${#SHA} -eq 40 || { echo "FAIL: SHA length is ${#SHA}, expected 40"; exit 1; }
```

If `curl` to api.github.com fails (rate limit, no network), fall back to the documented SHA `2b75f3ad12b420c9504ef05df8d2564a28f8500e` and note in the commit message that fallback was used.

═══════════════════════════════════════════════════════════════
STEP 2 — Write `tools/phosphor-svgs/.pin` (exactly two lines, plain text, no trailing whitespace)
═══════════════════════════════════════════════════════════════

```
<40-char SHA from STEP 1>
https://github.com/phosphor-icons/core/tree/<same SHA>
```

E.g. (placeholder — substitute the real SHA):

```
2b75f3ad12b420c9504ef05df8d2564a28f8500e
https://github.com/phosphor-icons/core/tree/2b75f3ad12b420c9504ef05df8d2564a28f8500e
```

No header, no YAML, no JSON. Plain text. Line 1 must match `^[0-9a-f]{40}$`. Line 2 must end with `/tree/<same-sha-as-line-1>`.

═══════════════════════════════════════════════════════════════
STEP 3 — Vendor `tools/phosphor-svgs/LICENSE`
═══════════════════════════════════════════════════════════════

Fetch the verbatim Phosphor MIT license at the pinned SHA:

```bash
SHA="<from STEP 1>"
curl -fsSL "https://raw.githubusercontent.com/phosphor-icons/core/${SHA}/LICENSE" \
  -o tools/phosphor-svgs/LICENSE

# Verify the file is the MIT license (must contain these two strings):
grep -q "MIT License" tools/phosphor-svgs/LICENSE
grep -q "Phosphor Icons" tools/phosphor-svgs/LICENSE
```

If either grep fails, fetch from `https://github.com/phosphor-icons/core/blob/main/LICENSE` and re-verify. Do NOT generate the license text — vendor verbatim.

═══════════════════════════════════════════════════════════════
STEP 4 — Write `tools/phosphor-svgs/README.md`
═══════════════════════════════════════════════════════════════

Use this exact content (substitute `<SHA>` with the real 40-char SHA from STEP 1):

```markdown
# tools/phosphor-svgs/

Vendored subset of [Phosphor Icons](https://github.com/phosphor-icons/core) Regular weight,
used by Phase 4 of the v1.1 Icon System milestone to generate the `AeroIcons` typed icon set.

## What is here

| File / Dir | Purpose |
|------------|---------|
| `.pin` | Two-line provenance: line 1 = 40-char Phosphor commit SHA used; line 2 = GitHub tree URL at that SHA |
| `LICENSE` | Verbatim copy of Phosphor's MIT license at the pinned SHA |
| `regular/` | Vendored stroke-based SVGs from `<phosphor-icons/core>/raw/regular/` at the pinned SHA |
| `README.md` | This file |

## Pinned revision

- **SHA**: `<SHA>` (line 1 of `.pin`)
- **GitHub tree**: <https://github.com/phosphor-icons/core/tree/<SHA>>
- **License**: MIT (see `LICENSE` for the verbatim text at this revision)

## Why we vendor (rather than fetch at build time)

- **Reproducibility**: The Gradle build does NOT call out to the network. Generated `.kt` files are committed; this directory is the audit trail explaining where they came from.
- **Lean repo**: We vendor only the 138 SVGs we use, not the full ~1300-icon Regular set. Total ~200–300 KB.
- **Recovery path**: The `.pin` file is sufficient to retroactively fetch any unvendored Phosphor icon at the exact same revision (see "Adding a new icon" below).

## Valkyrie CLI 1.1.1 — exact command used to generate the `internal/*.kt` files

Run from the repository root (`C:/1A_WORK/ui_lib/` or equivalent):

```bash
./tools/valkyrie svgxml2imagevector \
  --input-path tools/phosphor-svgs/regular/ \
  --output-path library/src/main/kotlin/com/mordred/aero/icons/internal/ \
  --package-name com.mordred.aero.icons.internal \
  --iconpack-name AeroIcons \
  --output-format backing-property \
  --explicit-mode
```

Notes:
- `--output-format` takes `backing-property` (lowercase, hyphenated). NOT `BackingProperty` (camelCase).
- `--explicit-mode` adds `public` modifiers to satisfy `:library`'s `explicitApi()`.
- `--input-path` accepts a directory; Valkyrie processes every `.svg` and `.xml` file inside.
- If Valkyrie writes an `AeroIcons.kt` file into `internal/`, move it one level up to `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` (the facade lives at `com.mordred.aero.icons.AeroIcons`, not `com.mordred.aero.icons.internal.AeroIcons`).

Valkyrie 1.1.1 download: <https://github.com/ComposeGears/Valkyrie/releases/tag/cli-1.1.1>

## Adding a new icon (recovery / extension procedure)

The vendored set covers Phase 4's 138 icons. To add an icon not in the vendored set:

1. Read the pinned SHA:
   ```bash
   SHA=$(head -1 tools/phosphor-svgs/.pin)
   ```

2. Fetch the SVG at the pinned revision (note: filename in `raw/regular/` is `<name>.svg` — NO `-regular` suffix):
   ```bash
   ICON="some-icon-name"   # kebab-case, e.g. "caret-down"
   curl -fsSL "https://raw.githubusercontent.com/phosphor-icons/core/${SHA}/raw/regular/${ICON}.svg" \
     -o "tools/phosphor-svgs/regular/${ICON}.svg"
   ```

3. Re-run the Valkyrie command above. Valkyrie regenerates only the affected `internal/*.kt` files; the others remain untouched (it's a deterministic SVG→Kotlin transformation).

4. Add the new property to `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` in alphabetical order:
   ```kotlin
   public val SomeIconName: ImageVector get() = _SomeIconName ?: loadSomeIconName().also { _SomeIconName = it }
   private var _SomeIconName: ImageVector? = null
   ```
   (or, if Valkyrie generates extension properties on `AeroIcons` directly, no facade edit is needed — verify by inspecting the generated file shape.)

5. Compile-verify:
   ```bash
   ./gradlew :library:compileKotlin
   ```

## Why `raw/regular/` (NOT `assets/regular/`)

Phosphor's `core` repository has TWO icon directories with the same names:
- `raw/regular/<name>.svg` — STROKE-based (stroke="currentColor", fill="none", stroke-width=16). **This is what we use.**
- `assets/regular/<name>-regular.svg` — FILL-based (fill="currentColor"). Wrong for our soft-outline aesthetic.

Always fetch from `raw/regular/`. The filename in this directory has NO `-regular` suffix.

## License attribution

Phosphor Icons is © Helena Zhang and Tobias Fried, licensed MIT. See `LICENSE` for the
verbatim text. Per MIT, attribution is preserved here and propagated by the `AeroIcons.kt`
KDoc, which references the source repository and license.
```

Substitute `<SHA>` (4 occurrences) with the real 40-char SHA from STEP 1.

═══════════════════════════════════════════════════════════════
STEP 5 — Commit
═══════════════════════════════════════════════════════════════

```bash
git add tools/phosphor-svgs/.pin tools/phosphor-svgs/LICENSE tools/phosphor-svgs/README.md
git commit -m "feat(04): pin Phosphor revision and vendor LICENSE + README

- tools/phosphor-svgs/.pin: <40-char-SHA>\\n<github-tree-url-at-SHA>
- tools/phosphor-svgs/LICENSE: verbatim Phosphor MIT license at pinned revision
- tools/phosphor-svgs/README.md: pinned-SHA provenance, exact Valkyrie 1.1.1 command,
  recovery procedure for adding unvendored icons.

Vendor strategy: lean (only icons we use) + .pin file as the recovery path."
```
  </action>
  <verify>
    <automated>test -f tools/phosphor-svgs/.pin && test -f tools/phosphor-svgs/LICENSE && test -f tools/phosphor-svgs/README.md && head -1 tools/phosphor-svgs/.pin | grep -qE "^[0-9a-f]{40}$" && head -2 tools/phosphor-svgs/.pin | tail -1 | grep -qE "^https://github\.com/phosphor-icons/core/tree/[0-9a-f]{40}$" && grep -q "MIT License" tools/phosphor-svgs/LICENSE && grep -q "Phosphor" tools/phosphor-svgs/LICENSE && grep -q "valkyrie svgxml2imagevector" tools/phosphor-svgs/README.md && grep -q "backing-property" tools/phosphor-svgs/README.md && grep -q "explicit-mode" tools/phosphor-svgs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/phosphor-svgs/.pin` exists and is exactly 2 lines.
    - Line 1 of `.pin` matches `^[0-9a-f]{40}$` (40 lowercase hex chars).
    - Line 2 of `.pin` matches `^https://github\.com/phosphor-icons/core/tree/[0-9a-f]{40}$` and the SHA matches line 1.
    - File `tools/phosphor-svgs/LICENSE` exists, contains the strings `MIT License` and `Phosphor`.
    - File `tools/phosphor-svgs/README.md` exists, contains the strings `valkyrie svgxml2imagevector`, `backing-property`, `explicit-mode`, `--input-path tools/phosphor-svgs/regular/`, `--package-name com.mordred.aero.icons.internal`, `--iconpack-name AeroIcons`.
    - README mentions filename rule: `raw/regular/<name>.svg` (NO `-regular` suffix).
    - Single commit titled `feat(04): pin Phosphor revision and vendor LICENSE + README`.
  </acceptance_criteria>
  <done>`.pin` (40-char SHA + tree URL), `LICENSE` (verbatim Phosphor MIT), and `README.md` (Valkyrie command + recovery) all present in `tools/phosphor-svgs/`. Verifier grep passes.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor 7 spike SVGs from raw/regular/ at pinned SHA</name>
  <files>
    tools/phosphor-svgs/regular/x.svg,
    tools/phosphor-svgs/regular/caret-down.svg,
    tools/phosphor-svgs/regular/magnifying-glass.svg,
    tools/phosphor-svgs/regular/check.svg,
    tools/phosphor-svgs/regular/info.svg,
    tools/phosphor-svgs/regular/frame-corners.svg,
    tools/phosphor-svgs/regular/square.svg
  </files>
  <read_first>
    tools/phosphor-svgs/.pin (created in Task 2 — line 1 is the SHA),
    .planning/phases/04-aeroicons-foundation/04-CONTEXT.md (§Decisions — 7 spike icons listed),
    .planning/phases/04-aeroicons-foundation/04-RESEARCH.md (§Pitfall 2 — `raw/regular/` not `assets/regular/`; filename `<name>.svg` not `<name>-regular.svg`),
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (row 04-01-04 — count must equal 7 after this task)
  </read_first>
  <action>
Fetch the 7 spike SVGs from Phosphor's `raw/regular/` directory at the pinned SHA. These are kebab-case names; files in `raw/regular/` have NO `-regular` suffix.

The 7 icons (from CONTEXT.md decisions):

| Phosphor name (kebab) | Kotlin name (PascalCase) | Why this icon is in the spike |
|-----------------------|--------------------------|-------------------------------|
| `x` | `X` | Most common close glyph; single-letter name edge case |
| `caret-down` | `CaretDown` | Most common dropdown indicator; kebab→PascalCase mapping |
| `magnifying-glass` | `MagnifyingGlass` | Search field icon; multi-segment kebab name |
| `check` | `Check` | Checkbox checked state |
| `info` | `Info` | Alert/banner enum (Phase 5 MIG-10/11 dependency) |
| `frame-corners` | `FrameCorners` | TitleBar restore button — flagged in PITFALLS for glassmorphic visual weight |
| `square` | `Square` | TitleBar maximize button — flagged in PITFALLS for glassmorphic visual weight |

═══════════════════════════════════════════════════════════════
STEP 1 — Read the pinned SHA
═══════════════════════════════════════════════════════════════

```bash
SHA=$(head -1 tools/phosphor-svgs/.pin)
echo "Using pinned SHA: $SHA"
test ${#SHA} -eq 40 || { echo "FAIL: bad SHA in .pin"; exit 1; }
```

═══════════════════════════════════════════════════════════════
STEP 2 — Fetch the 7 SVGs
═══════════════════════════════════════════════════════════════

```bash
mkdir -p tools/phosphor-svgs/regular

BASE="https://raw.githubusercontent.com/phosphor-icons/core/${SHA}/raw/regular"
for name in x caret-down magnifying-glass check info frame-corners square; do
  echo "Fetching ${name}.svg ..."
  curl -fsSL "${BASE}/${name}.svg" -o "tools/phosphor-svgs/regular/${name}.svg"
  test -s "tools/phosphor-svgs/regular/${name}.svg" || { echo "FAIL: empty file ${name}.svg"; exit 1; }
done
```

═══════════════════════════════════════════════════════════════
STEP 3 — Verify each SVG is stroke-based Phosphor Regular (NOT fill-based assets/)
═══════════════════════════════════════════════════════════════

Every Phosphor Regular SVG from `raw/regular/` should contain the attributes:
- `viewBox="0 0 256 256"`
- `stroke-width="16"`
- `stroke-linecap="round"` (or set on the SVG element / per-path)

Run:

```bash
for name in x caret-down magnifying-glass check info frame-corners square; do
  f="tools/phosphor-svgs/regular/${name}.svg"
  grep -q 'viewBox="0 0 256 256"' "$f" || { echo "FAIL: ${name}.svg missing viewBox=0 0 256 256"; exit 1; }
  grep -qE 'stroke-width="?16"?' "$f"  || { echo "FAIL: ${name}.svg missing stroke-width=16"; exit 1; }
done
echo "All 7 SVGs verified stroke-based with 256 viewBox + stroke-width 16"
```

If any SVG fails verification, you grabbed `assets/regular/` (fill-based) by mistake — re-fetch from `raw/regular/`.

═══════════════════════════════════════════════════════════════
STEP 4 — Sanity-check file count
═══════════════════════════════════════════════════════════════

```bash
COUNT=$(ls tools/phosphor-svgs/regular/*.svg 2>/dev/null | wc -l)
test "$COUNT" -eq 7 || { echo "FAIL: expected 7 SVGs, found $COUNT"; exit 1; }
```

═══════════════════════════════════════════════════════════════
STEP 5 — Commit
═══════════════════════════════════════════════════════════════

```bash
git add tools/phosphor-svgs/regular/
git commit -m "feat(04): vendor 7 spike Phosphor SVGs (X, CaretDown, MagnifyingGlass, Check, Info, FrameCorners, Square)

Fetched from raw/regular/ at pinned SHA. All confirmed:
- viewBox 0 0 256 256
- stroke-width 16 (corrects prior research stroke=12 claim)
- stroke-linecap round, stroke-linejoin round
- stroke-based (not fill-based assets/regular/)

Spike covers: most common glyphs (X, Check, CaretDown, MagnifyingGlass, Info)
plus the two TitleBar icons (FrameCorners, Square) flagged in PITFALLS for
glassmorphic visual weight."
```
  </action>
  <verify>
    <automated>ls tools/phosphor-svgs/regular/*.svg | wc -l | tr -d ' \r' | grep -qE "^7$" && for n in x caret-down magnifying-glass check info frame-corners square; do f="tools/phosphor-svgs/regular/${n}.svg"; test -s "$f" && grep -q 'viewBox="0 0 256 256"' "$f" && grep -qE 'stroke-width="?16"?' "$f" || { echo "FAIL: $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls tools/phosphor-svgs/regular/*.svg | wc -l` returns exactly 7.
    - All 7 expected files exist (x.svg, caret-down.svg, magnifying-glass.svg, check.svg, info.svg, frame-corners.svg, square.svg) — none with `-regular` suffix.
    - Every SVG contains `viewBox="0 0 256 256"`.
    - Every SVG contains `stroke-width="16"` (or `stroke-width=16` unquoted) — NOT 12, NOT 2.
    - No SVG is empty (each is `&gt; 0` bytes).
    - Single commit referencing the 7 file names; message mentions stroke-width 16.
  </acceptance_criteria>
  <done>7 stroke-based Phosphor Regular SVGs vendored at the pinned SHA, all confirmed 256-unit viewBox and stroke-width 16. Filename rule confirmed correct (no -regular suffix). Validation row 04-01-04 green.</done>
</task>

<task type="auto">
  <name>Task 4: Run Valkyrie 1.1.1 on the 7 spike SVGs — generate internal/*.kt files; resolve output-shape open question; relocate AeroIcons.kt if Valkyrie generates it inside internal/</name>
  <files>
    library/src/main/kotlin/com/mordred/aero/icons/internal/X.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/CaretDown.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/MagnifyingGlass.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/Check.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/Info.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/FrameCorners.kt,
    library/src/main/kotlin/com/mordred/aero/icons/internal/Square.kt,
    tools/phosphor-svgs/README.md
  </files>
  <read_first>
    tools/phosphor-svgs/README.md (Valkyrie command — written in Task 2),
    .planning/phases/04-aeroicons-foundation/04-RESEARCH.md (§Open Questions — Valkyrie output shape; §Pitfall 6 — AeroIcons.kt may land in internal/ subdir),
    .planning/phases/04-aeroicons-foundation/04-CONTEXT.md (§Claude's Discretion — match Valkyrie's actual output rather than fighting the tool),
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (rows 04-01-05..09 — file count + grep gates),
    library/build.gradle.kts (lines 1–32 — confirm explicitApi() active)
  </read_first>
  <action>
═══════════════════════════════════════════════════════════════
STEP 1 — Locate / install Valkyrie CLI 1.1.1
═══════════════════════════════════════════════════════════════

Valkyrie is NOT a Gradle dependency. Check if a binary already exists in `tools/`:

```bash
ls -la tools/valkyrie* 2>/dev/null
```

If not present, download from <https://github.com/ComposeGears/Valkyrie/releases/tag/cli-1.1.1>:

- On Windows (Git Bash) — release ships a Windows-runnable JAR or wrapper. Download the `cli-1.1.1` archive, extract to `tools/valkyrie-cli-1.1.1/`, and create a wrapper script `tools/valkyrie` (or `tools/valkyrie.bat`) that invokes the bundled binary or `java -jar tools/valkyrie-cli-1.1.1/valkyrie.jar "$@"`.
- On Linux/macOS: `chmod +x tools/valkyrie`.

Verify:

```bash
./tools/valkyrie --version 2>&1 | head -1
# Expect: valkyrie cli-1.1.1 (or similar)
```

If the binary is unavailable, this is a CHECKPOINT — surface to user ("cannot proceed without Valkyrie CLI 1.1.1; please install or provide path").

═══════════════════════════════════════════════════════════════
STEP 2 — Create the output directory
═══════════════════════════════════════════════════════════════

```bash
mkdir -p library/src/main/kotlin/com/mordred/aero/icons/internal
```

═══════════════════════════════════════════════════════════════
STEP 3 — Run Valkyrie EXACTLY as documented in tools/phosphor-svgs/README.md
═══════════════════════════════════════════════════════════════

```bash
./tools/valkyrie svgxml2imagevector \
  --input-path tools/phosphor-svgs/regular/ \
  --output-path library/src/main/kotlin/com/mordred/aero/icons/internal/ \
  --package-name com.mordred.aero.icons.internal \
  --iconpack-name AeroIcons \
  --output-format backing-property \
  --explicit-mode
```

Critical flag values (all locked by 04-RESEARCH.md, do not improvise):
- `--input-path tools/phosphor-svgs/regular/` (directory, trailing slash optional)
- `--output-path library/src/main/kotlin/com/mordred/aero/icons/internal/`
- `--package-name com.mordred.aero.icons.internal` (matches output dir's Kotlin package)
- `--iconpack-name AeroIcons` (Valkyrie wires generated extension properties to this object name)
- `--output-format backing-property` (lowercase hyphenated, NOT `BackingProperty`)
- `--explicit-mode` (adds `public` modifiers — required by `:library`'s explicitApi())

═══════════════════════════════════════════════════════════════
STEP 4 — Inspect Valkyrie's actual output shape (resolves open question #1)
═══════════════════════════════════════════════════════════════

This is the FIRST place we discover Valkyrie's real behavior. Two valid shapes (per RESEARCH.md):

**Shape A — Extension property on AeroIcons:**
```kotlin
// internal/X.kt
package com.mordred.aero.icons.internal
import com.mordred.aero.icons.AeroIcons
public val AeroIcons.X: ImageVector
    get() {
        if (_x != null) return _x!!
        _x = ImageVector.Builder( name = "X", ..., viewportWidth = 256f, ...).apply { ... }.build()
        return _x!!
    }
@Suppress("ObjectPropertyName")
private var _x: ImageVector? = null
```
In this shape: `AeroIcons.kt` only declares `public object AeroIcons` (empty body or near-empty). All property bodies live in `internal/*.kt`.

**Shape B — Standalone internal function called from facade getter:**
```kotlin
// internal/X.kt
package com.mordred.aero.icons.internal
internal fun loadX(): ImageVector = ImageVector.Builder( name = "X", ...).apply { ... }.build()
```
In this shape: `AeroIcons.kt` declares the lazy getter pattern explicitly (`public val X: ImageVector get() = _X ?: loadX().also { _X = it }; private var _X: ImageVector? = null`).

Run:
```bash
ls library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt
cat library/src/main/kotlin/com/mordred/aero/icons/internal/X.kt | head -50
```

Note which shape Valkyrie produced. Both are accepted per CONTEXT.md `Claude's Discretion`. Document the decision in `tools/phosphor-svgs/README.md` by appending a section:

```markdown
## Valkyrie 1.1.1 actual output shape (verified 2026-04-29 spike)

Valkyrie 1.1.1 with `--output-format backing-property --iconpack-name AeroIcons` generates:
- **<Shape A | Shape B — fill in based on observed output>** — <one-sentence summary>
- Per-icon files contain: `<paste 1–2 representative lines, e.g. 'public val AeroIcons.X: ImageVector get() { ... }'>`
- The facade `AeroIcons.kt` <was generated by Valkyrie inside `internal/` and moved up | was NOT generated by Valkyrie and is hand-written in Task 5>.

This determines the structure of `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt`:
- If Shape A: `AeroIcons.kt` is just `public object AeroIcons`; all properties are extension properties in `internal/*.kt`.
- If Shape B: `AeroIcons.kt` lists `public val X: ImageVector get() = _X ?: loadX().also { _X = it }` for every icon.
```

═══════════════════════════════════════════════════════════════
STEP 5 — Resolve `AeroIcons.kt` placement (Pitfall 6)
═══════════════════════════════════════════════════════════════

Valkyrie may write an `AeroIcons.kt` file into `internal/`. Check:

```bash
ls library/src/main/kotlin/com/mordred/aero/icons/internal/AeroIcons.kt 2>/dev/null
```

If present:
- **Inspect its contents.** It declares `public object AeroIcons` in package `com.mordred.aero.icons.internal`.
- **Move and re-package** to the correct location:

  ```bash
  mv library/src/main/kotlin/com/mordred/aero/icons/internal/AeroIcons.kt \
     library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt
  ```

- **Edit the package line** of the moved file: `package com.mordred.aero.icons.internal` → `package com.mordred.aero.icons`.

If absent:
- Valkyrie did not generate `AeroIcons.kt`. Task 5 will hand-author it.

═══════════════════════════════════════════════════════════════
STEP 6 — Per-file spot-check (Pitfall 9 — SVG conversion artifacts)
═══════════════════════════════════════════════════════════════

For each of the 7 generated `internal/*.kt` files, verify Valkyrie translated the SVG faithfully:

```bash
for f in library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt; do
  echo "=== $f ==="
  grep -E "viewportWidth|strokeLineWidth|StrokeCap|StrokeJoin|defaultWidth" "$f" | head -10
done
```

Each file MUST contain (Valkyrie may use `=` or `=` without spaces — be flexible):
- `viewportWidth = 256f` (NOT 24f)
- `viewportHeight = 256f` (NOT 24f)
- `defaultWidth = 24.dp` (or `24.0.dp`)
- `defaultHeight = 24.dp`
- `strokeLineWidth = 16f` (NOT 12f, NOT 2f)
- `StrokeCap.Round`
- `StrokeJoin.Round`
- One of these fill forms (Phosphor Regular icons are stroke-only):
  - `fill = SolidColor(Color.Transparent)`, OR
  - `fill = Color.Transparent`, OR
  - `fill = null`

The fill check is critical — Valkyrie sometimes emits `SolidColor(Color.Black)` if the source SVG attribute is malformed; `compileKotlin` would still pass but the rendered glyph would have a black filled body where it should be transparent. ROADMAP success criterion 5 explicitly requires `fill = Color.Transparent` (or equivalent) in every generated `internal/*.kt`.

Run the all-files fill grep:

```bash
echo "=== Fill check (positive — at least 7 hits expected, one per spike file) ==="
HITS_FILL=$(grep -rE "fill\s*=\s*(SolidColor\(Color\.Transparent\)|Color\.Transparent|null)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS_FILL" -ge 7 || { echo "FAIL: $HITS_FILL transparent-fill hits (expected >=7)"; exit 1; }

echo "=== Fill check (inverse — opaque solid colors must be 0) ==="
HITS_OPAQUE=$(grep -rE "fill\s*=\s*SolidColor\(Color\.(Black|White|Red|Green|Blue)\)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS_OPAQUE" = "0" || { echo "FAIL: $HITS_OPAQUE opaque-color fills found (expected 0)"; exit 1; }
echo "OK: fill is transparent/null in all 7 files; no opaque fills"
```

If any file deviates, do NOT proceed; the SVG was the wrong file (assets/regular/ instead of raw/regular/) — re-vendor that specific SVG and re-run Valkyrie on it alone.

═══════════════════════════════════════════════════════════════
STEP 7 — Commit
═══════════════════════════════════════════════════════════════

```bash
git add library/src/main/kotlin/com/mordred/aero/icons/internal/ \
       library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt 2>/dev/null \
       tools/phosphor-svgs/README.md

git commit -m "feat(04): generate 7 spike ImageVector builders via Valkyrie 1.1.1

Ran: ./tools/valkyrie svgxml2imagevector --input-path tools/phosphor-svgs/regular/
  --output-path library/src/main/kotlin/com/mordred/aero/icons/internal/
  --package-name com.mordred.aero.icons.internal --iconpack-name AeroIcons
  --output-format backing-property --explicit-mode

Generated 7 files: X.kt, CaretDown.kt, MagnifyingGlass.kt, Check.kt, Info.kt,
FrameCorners.kt, Square.kt. All confirmed:
- viewportWidth/viewportHeight = 256f (Phosphor 256-unit viewBox)
- strokeLineWidth = 16f (corrects prior 12f assumption; matches actual SVG)
- StrokeCap.Round + StrokeJoin.Round
- public modifiers per --explicit-mode (explicitApi compatible)

Valkyrie output shape resolved: <Shape A | Shape B — fill in>. Documented in
tools/phosphor-svgs/README.md.

[Where applicable] Moved Valkyrie-generated AeroIcons.kt out of internal/
subpackage to icons/ root, re-packaged to com.mordred.aero.icons."
```
  </action>
  <verify>
    <automated>bash -c 'set -e; COUNT=$(ls library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt 2>/dev/null | wc -l | tr -d " "); test "$COUNT" = "7" || { echo "FAIL: expected 7 internal/*.kt files, got $COUNT"; exit 1; }; HITS_24=$(grep -rE "viewportWidth\s*=\s*24f" library/src/main/kotlin/com/mordred/aero/icons/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_24" = "0" || { echo "FAIL: $HITS_24 hits of viewportWidth=24f"; exit 1; }; HITS_256=$(grep -rE "viewportWidth\s*=\s*256f" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_256" -ge "7" || { echo "FAIL: $HITS_256 hits of viewportWidth=256f"; exit 1; }; HITS_STROKE=$(grep -rE "strokeLineWidth\s*=\s*16f" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_STROKE" -ge "7" || { echo "FAIL: $HITS_STROKE hits of strokeLineWidth=16f"; exit 1; }; HITS_CAP=$(grep -rE "StrokeCap\.(Round|Companion\.Round)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_CAP" -ge "7" || { echo "FAIL: $HITS_CAP hits of StrokeCap.Round"; exit 1; }; HITS_FILL=$(grep -rE "fill\s*=\s*(SolidColor\(Color\.Transparent\)|Color\.Transparent|null)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_FILL" -ge "7" || { echo "FAIL: $HITS_FILL transparent-fill hits (expected >=7) — ROADMAP SC5 (VALIDATION row 04-01-09b)"; exit 1; }; HITS_OPAQUE=$(grep -rE "fill\s*=\s*SolidColor\(Color\.(Black|White|Red|Green|Blue)\)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " "); test "$HITS_OPAQUE" = "0" || { echo "FAIL: $HITS_OPAQUE opaque-color fills found (expected 0) — ROADMAP SC5"; exit 1; }; echo "OK: 7 files, 256 viewport, stroke 16, round caps, transparent fill"'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 7 files exist in `library/src/main/kotlin/com/mordred/aero/icons/internal/` (X.kt, CaretDown.kt, MagnifyingGlass.kt, Check.kt, Info.kt, FrameCorners.kt, Square.kt — Valkyrie may also produce an extra `AeroIcons.kt` in this directory; if so it is moved up by Step 5 before this gate runs).
    - `grep -rE "viewportWidth\s*=\s*24f" library/src/main/kotlin/com/mordred/aero/icons/` returns 0 hits (VALIDATION row 04-01-06).
    - `grep -rE "viewportWidth\s*=\s*256f" library/src/main/kotlin/com/mordred/aero/icons/internal/` returns at least 7 hits (VALIDATION row 04-01-07).
    - `grep -rE "strokeLineWidth\s*=\s*16f" library/src/main/kotlin/com/mordred/aero/icons/internal/` returns at least 7 hits (VALIDATION row 04-01-08).
    - `grep -rE "StrokeCap\.(Round|Companion\.Round)" library/src/main/kotlin/com/mordred/aero/icons/internal/` returns at least 7 hits (VALIDATION row 04-01-09).
    - **Fill check (positive)** — `grep -rE "fill\s*=\s*(SolidColor\(Color\.Transparent\)|Color\.Transparent|null)" library/src/main/kotlin/com/mordred/aero/icons/internal/` returns at least 7 hits (one transparent-fill statement per spike file). Maps to ROADMAP success criterion 5; recorded in VALIDATION row 04-01-09b. Accepts any of: `SolidColor(Color.Transparent)`, `Color.Transparent`, or `null` (Valkyrie may emit any of the three forms for stroke-only paths).
    - **Fill check (inverse)** — `grep -rE "fill\s*=\s*SolidColor\(Color\.(Black|White|Red|Green|Blue)\)" library/src/main/kotlin/com/mordred/aero/icons/internal/` returns 0 hits. No opaque solid fills. Catches the case where Valkyrie emits a default-color fill instead of transparent.
    - `tools/phosphor-svgs/README.md` has a new "## Valkyrie 1.1.1 actual output shape" section recording either Shape A or Shape B with one paste of representative generated code.
    - If Valkyrie produced an `AeroIcons.kt` inside `internal/`, it has been moved to `icons/AeroIcons.kt` and its `package` line has been edited from `com.mordred.aero.icons.internal` to `com.mordred.aero.icons`.
    - Single commit titled with `feat(04): generate 7 spike ImageVector builders via Valkyrie 1.1.1` and the body explicitly names the resolved Shape (A or B).
  </acceptance_criteria>
  <done>7 generated `internal/*.kt` files exist, all with viewportWidth=256f, strokeLineWidth=16f, StrokeCap.Round, StrokeJoin.Round, and transparent fill (`SolidColor(Color.Transparent)` / `Color.Transparent` / `null`); 0 opaque-color fills (ROADMAP SC5 satisfied). Valkyrie's actual output shape (extension property vs internal fun) is recorded in `tools/phosphor-svgs/README.md`. If `AeroIcons.kt` was emitted into `internal/`, it has been relocated and re-packaged.</done>
</task>

<task type="auto">
  <name>Task 5: Author / finalize AeroIcons.kt facade — 7 alphabetized properties + full object-level KDoc satisfying ICN-02</name>
  <files>
    library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt
  </files>
  <read_first>
    library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt (whatever Task 4 left here — could be Valkyrie-generated stub, hand-relocated stub, or absent),
    library/src/main/kotlin/com/mordred/aero/icons/internal/X.kt (representative file — confirms Shape A or Shape B),
    tools/phosphor-svgs/README.md (the "Valkyrie 1.1.1 actual output shape" section appended by Task 4),
    .planning/phases/04-aeroicons-foundation/04-CONTEXT.md (§Decisions/KDoc — locks 6 structural KDoc requirements),
    .planning/phases/04-aeroicons-foundation/04-RESEARCH.md (§Pattern 1 — KDoc text reference; §Architecture Patterns — accepted shapes),
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (rows 04-01-10..15 — file existence + grep gates for facade)
  </read_first>
  <action>
═══════════════════════════════════════════════════════════════
STEP 1 — Determine Shape A vs Shape B from Task 4's findings
═══════════════════════════════════════════════════════════════

Read Task 4's appendix in `tools/phosphor-svgs/README.md` (the "Valkyrie 1.1.1 actual output shape" section). Two possibilities:

**If Shape A (Valkyrie generated extension properties on `AeroIcons` in each `internal/*.kt`):**
- The 7 `internal/*.kt` files contain `public val AeroIcons.X: ImageVector get() { ... }` etc.
- An `AeroIcons.kt` file was generated (most likely inside `internal/` — Task 4 moved it up). It declares `public object AeroIcons` with no body (or a near-empty body).
- Your job in this task: REPLACE that stub `AeroIcons.kt` with a hand-authored version that adds ONLY the object-level KDoc on top of `public object AeroIcons`. Do NOT add per-property declarations — extension properties live in `internal/*.kt`.

**If Shape B (Valkyrie generated standalone `internal fun loadX(): ImageVector` etc.):**
- The 7 `internal/*.kt` files contain `internal fun loadX(): ImageVector = ImageVector.Builder(...).apply { ... }.build()`.
- `AeroIcons.kt` was NOT generated by Valkyrie. You will hand-author it from scratch.
- Your job: Write `AeroIcons.kt` containing object-level KDoc + `public object AeroIcons { ... }` body with 7 alphabetized properties using the lazy backing-field pattern.

═══════════════════════════════════════════════════════════════
STEP 2 — Author the file
═══════════════════════════════════════════════════════════════

Write `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` with this exact structure (substitute body per Shape A or B):

```kotlin
package com.mordred.aero.icons

import androidx.compose.ui.graphics.vector.ImageVector

/**
 * Typed [ImageVector] constants — Phosphor Icons Regular weight.
 *
 * All constants are initialized lazily on first access. Each icon is cached after first use
 * and reused for the lifetime of the process.
 *
 * ## Naming convention
 *
 * Phosphor kebab-case names map 1-to-1 to PascalCase Kotlin identifiers. Look up icons at
 * [phosphoricons.com](https://phosphoricons.com) and use the source name directly — no
 * renaming layer. Phosphor's vocabulary differs from Material/Feather conventions:
 *
 * | Phosphor name      | AeroIcons identifier | NOT (other libraries)   |
 * |--------------------|----------------------|-------------------------|
 * | `caret-down`       | `CaretDown`          | ~~ChevronDown~~         |
 * | `magnifying-glass` | `MagnifyingGlass`    | ~~Search~~              |
 * | `house`            | `House`              | ~~Home~~                |
 * | `funnel`           | `Funnel`             | ~~Filter~~              |
 * | `gear`             | `Gear`               | ~~Settings~~            |
 * | `x`                | `X`                  | ~~Close~~               |
 * | `eye-slash`        | `EyeSlash`           | ~~EyeOff~~              |
 * | `envelope`         | `Envelope`           | ~~Mail~~                |
 *
 * ## Source and license
 *
 * Source: [Phosphor Icons](https://github.com/phosphor-icons/core) Regular weight, MIT license
 * (see `tools/phosphor-svgs/LICENSE`). Each glyph is a 256×256 viewBox with stroke-width 16,
 * round line caps, and round line joins.
 *
 * ## Recommended size
 *
 * Render at **16dp–32dp**. The default `Icon()` size of 24dp is the canonical target. Below
 * 14dp the 16-unit stroke (`16/256 ≈ 6.25%` of the viewBox) renders thinner than 1dp at 96 DPI
 * and antialiases to a faint line — for very small contexts (e.g. AeroNumberSpinner up/down
 * buttons) consider a heavier mitigation (Canvas draw or larger button).
 *
 * ## Tint is mandatory
 *
 * `androidx.compose.material3.Icon()` defaults its tint to `LocalContentColor`, which
 * [com.mordred.aero.theme.AeroTheme] does **not** set. On AeroDark and Classic themes the
 * inherited `LocalContentColor` is unpredictable and may render icons invisible. Every
 * `Icon()` call site inside `:library` and `:showcase` MUST pass an explicit tint:
 *
 * ```kotlin
 * Icon(
 *     imageVector = AeroIcons.X,
 *     contentDescription = "Close",
 *     tint = AeroTheme.colors.onSurface
 * )
 * ```
 *
 * Look-up: <https://phosphoricons.com>.
 */
public object AeroIcons {

    // -- Shape A path (extension properties live in internal/*.kt) --
    // Body is empty here; properties are pulled in at the use site via the import chain.
    // (When Shape B is used, replace this comment with the alphabetized property list below.)

    // -- Shape B path (delete the comment block above and inline these properties) --
    // public val CaretDown: ImageVector get() = _CaretDown ?: loadCaretDown().also { _CaretDown = it }
    // private var _CaretDown: ImageVector? = null
    //
    // public val Check: ImageVector get() = _Check ?: loadCheck().also { _Check = it }
    // private var _Check: ImageVector? = null
    //
    // public val FrameCorners: ImageVector get() = _FrameCorners ?: loadFrameCorners().also { _FrameCorners = it }
    // private var _FrameCorners: ImageVector? = null
    //
    // public val Info: ImageVector get() = _Info ?: loadInfo().also { _Info = it }
    // private var _Info: ImageVector? = null
    //
    // public val MagnifyingGlass: ImageVector get() = _MagnifyingGlass ?: loadMagnifyingGlass().also { _MagnifyingGlass = it }
    // private var _MagnifyingGlass: ImageVector? = null
    //
    // public val Square: ImageVector get() = _Square ?: loadSquare().also { _Square = it }
    // private var _Square: ImageVector? = null
    //
    // public val X: ImageVector get() = _X ?: loadX().also { _X = it }
    // private var _X: ImageVector? = null
}
```

CRITICAL — the deliverable file is NOT the template above with comments. Pick ONE shape based on STEP 1 and emit ONLY that shape's body:

**For Shape A:** delete BOTH the "Shape A path" comment AND the "Shape B path" comment. Body of `public object AeroIcons {}` is empty (or contains only a single-line `// Properties are extension properties; see icons/internal/*.kt` if you want a navigation hint). Add the corresponding `import com.mordred.aero.icons.internal.*` line under the existing imports.

**For Shape B:** delete BOTH comment blocks. Inline the 7 properties verbatim (uncomment them), in the alphabetical order shown above (CaretDown, Check, FrameCorners, Info, MagnifyingGlass, Square, X). Add 7 corresponding `import com.mordred.aero.icons.internal.load<Name>` lines under `import androidx.compose.ui.graphics.vector.ImageVector`.

The KDoc block (header through `Look-up: <https://phosphoricons.com>.`) is identical in both shapes and is REQUIRED verbatim — it satisfies all 6 ICN-02 structural requirements:
1. Phosphor source + MIT license + viewBox 256×256 + stroke 16 ("Source and license" section).
2. kebab→PascalCase mapping rule ("Naming convention" lead paragraph).
3. 8-row naming-convention table (kebab-name → PascalCase identifier → NOT industry name).
4. Recommended size 16dp–32dp ("Recommended size" section).
5. Mandatory explicit tint warning ("Tint is mandatory" section, includes a code example).
6. phosphoricons.com lookup URL (referenced twice — top and bottom).

═══════════════════════════════════════════════════════════════
STEP 3 — Verify the facade compiles in isolation
═══════════════════════════════════════════════════════════════

```bash
./gradlew :library:compileKotlin
```

Expected: BUILD SUCCESSFUL. Common failure modes:
- `Visibility must be specified in explicit API mode` — you forgot `public` on a property declaration. Add it.
- `Unresolved reference: loadX` (Shape B) — you forgot the `import com.mordred.aero.icons.internal.loadX` lines. Add one per property.
- `Conflicting declarations: AeroIcons` — Valkyrie left a duplicate `AeroIcons.kt` somewhere. Confirm there is exactly one file named `AeroIcons.kt` in the project: `find library/src -name AeroIcons.kt`.

═══════════════════════════════════════════════════════════════
STEP 4 — Commit
═══════════════════════════════════════════════════════════════

```bash
git add library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt
git commit -m "feat(04): author AeroIcons facade with object-level KDoc and 7 spike properties

- public object AeroIcons in com.mordred.aero.icons (relocated/re-packaged from
  internal/ if Valkyrie generated it there).
- Object-level KDoc covers all 6 ICN-02 requirements: Phosphor source + MIT + viewBox
  256x256 + stroke 16, kebab->PascalCase rule, 8-row naming table, recommended size
  16-32dp, mandatory explicit tint warning with code example, phosphoricons.com URL.
- Body shape: <Shape A: empty (extension properties in internal/) | Shape B: 7
  alphabetized lazy backing-property declarations (CaretDown, Check, FrameCorners,
  Info, MagnifyingGlass, Square, X)>.
- explicitApi-compatible: every public val carries explicit 'public' modifier."
```
  </action>
  <verify>
    <automated>bash -c 'set -e; F=library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt; test -f "$F" || { echo "FAIL: $F missing"; exit 1; }; grep -qE "^public object AeroIcons" "$F" || { echo "FAIL: missing public object AeroIcons"; exit 1; }; grep -B1 "^public object AeroIcons" "$F" | grep -qE "\*/" || { echo "FAIL: KDoc block does not directly precede object decl"; exit 1; }; grep -qE "caret-down|magnifying-glass" "$F" || { echo "FAIL: KDoc missing kebab-case naming examples"; exit 1; }; grep -qE "PascalCase" "$F" || { echo "FAIL: KDoc missing PascalCase rule"; exit 1; }; grep -qE "tint|LocalContentColor" "$F" || { echo "FAIL: KDoc missing tint warning"; exit 1; }; grep -qE "phosphoricons\.com" "$F" || { echo "FAIL: KDoc missing phosphoricons.com URL"; exit 1; }; grep -qE "stroke[- ]width\s*16|strokeLineWidth\s*16|stroke\s*16" "$F" || { echo "FAIL: KDoc missing stroke-width 16 reference"; exit 1; }; grep -qE "256" "$F" || { echo "FAIL: KDoc missing 256-unit viewBox reference"; exit 1; }; LEAKS=$(grep -E "^\s*val [A-Z][A-Za-z]+:" "$F" | grep -v "public" | wc -l | tr -d " "); test "$LEAKS" = "0" || { echo "FAIL: $LEAKS val without explicit public modifier"; exit 1; }; echo "OK: AeroIcons.kt facade structurally valid"'</automated>
  </verify>
  <acceptance_criteria>
    - File `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` exists at the correct package path (NOT inside `internal/`).
    - File contains exactly one occurrence of `public object AeroIcons` at the start of a line (VALIDATION row 04-01-11).
    - The KDoc `/** ... */` block immediately precedes `public object AeroIcons` (VALIDATION row 04-01-13).
    - KDoc contains the strings `caret-down` AND `magnifying-glass` AND `PascalCase` (kebab-case naming convention table — VALIDATION row 04-01-14).
    - KDoc contains either the string `tint` or `LocalContentColor` (mandatory tint warning — VALIDATION row 04-01-15).
    - KDoc contains the string `phosphoricons.com`.
    - KDoc contains a reference to stroke-width `16` (matches the corrected research finding).
    - KDoc contains a reference to the `256`-unit viewBox.
    - **Shape B only**: `grep -c "public val .*: ImageVector" library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` equals 7 (VALIDATION row 04-01-12). Properties are alphabetical: CaretDown, Check, FrameCorners, Info, MagnifyingGlass, Square, X.
    - **Shape A only**: VALIDATION row 04-01-12 is satisfied by the count of `public val AeroIcons.<Name>: ImageVector` declarations across `library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt` (must be 7).
    - No `val ... ImageVector` line lacks an explicit `public` modifier in the facade file (explicitApi compatibility).
    - Single commit titled `feat(04): author AeroIcons facade with object-level KDoc and 7 spike properties`.
  </acceptance_criteria>
  <done>`AeroIcons.kt` exists with the full structured KDoc satisfying all 6 ICN-02 requirements, `public object AeroIcons` declared, Shape A or Shape B body matching Valkyrie's actual output (recorded in tools/phosphor-svgs/README.md), every public val carries explicit `public` modifier, file lives at `com.mordred.aero.icons.AeroIcons` (not `.internal.AeroIcons`).</done>
</task>

<task type="checkpoint:verify">
  <name>Task 6: Spike verification gate — run :library:compileKotlin and the full VALIDATION.md per-task table for Plan 04-01</name>
  <files>
    (read-only verification — no source files modified)
  </files>
  <read_first>
    .planning/phases/04-aeroicons-foundation/04-VALIDATION.md (Per-Task Verification Map rows 04-01-01..16),
    library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt (final state),
    library/src/main/kotlin/com/mordred/aero/icons/internal/ (all 7 generated files),
    tools/phosphor-svgs/.pin (provenance),
    tools/phosphor-svgs/regular/ (7 vendored SVGs),
    tools/phosphor-svgs/README.md (Valkyrie command + output-shape decision)
  </read_first>
  <action>
This is a no-write verification gate. Run every check from VALIDATION.md per-task table for rows 04-01-01 through 04-01-16 in sequence. Any failure is a blocker — do NOT proceed to Plan 04-02 until every line below returns OK.

═══════════════════════════════════════════════════════════════
GATE 1 — Doc corrections (rows 04-01-01, 04-01-02 from VALIDATION.md)
═══════════════════════════════════════════════════════════════

```bash
echo "=== 04-01-01: no remaining 139-icon-count references in planning/research/04-CONTEXT ==="
grep -nE "\b139\b" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md \
  .planning/research/SUMMARY.md .planning/research/FEATURES.md \
  .planning/research/ARCHITECTURE.md .planning/phases/04-aeroicons-foundation/04-CONTEXT.md \
  || echo "(no hits — OK)"
# Hand-verify any surviving 139 token is unrelated to icon count.

echo "=== 04-01-02: no Phosphor-context strokeLineWidth=12 refs ==="
grep -rnE "strokeLineWidth\s*=\s*12f" .planning/research/ .planning/REQUIREMENTS.md \
  .planning/phases/04-aeroicons-foundation/04-CONTEXT.md || echo "(no hits — OK)"
```

═══════════════════════════════════════════════════════════════
GATE 2 — Provenance (rows 04-01-03)
═══════════════════════════════════════════════════════════════

```bash
echo "=== 04-01-03: provenance files present ==="
test -f tools/phosphor-svgs/.pin && echo "OK: .pin"
test -f tools/phosphor-svgs/LICENSE && echo "OK: LICENSE"
test -f tools/phosphor-svgs/README.md && echo "OK: README.md"

# Sanity check .pin format
head -1 tools/phosphor-svgs/.pin | grep -qE "^[0-9a-f]{40}$" && echo "OK: .pin line 1 is 40-char SHA"
head -2 tools/phosphor-svgs/.pin | tail -1 | grep -qE "^https://github.com/phosphor-icons/core/tree/[0-9a-f]{40}$" && echo "OK: .pin line 2 is GitHub tree URL"

# README contains required strings
grep -q "valkyrie svgxml2imagevector" tools/phosphor-svgs/README.md && echo "OK: README has Valkyrie command"
grep -q "backing-property" tools/phosphor-svgs/README.md && echo "OK: README has --output-format value"
grep -q "explicit-mode" tools/phosphor-svgs/README.md && echo "OK: README has --explicit-mode flag"
grep -q "Valkyrie 1.1.1 actual output shape" tools/phosphor-svgs/README.md && echo "OK: README documents output shape"
```

═══════════════════════════════════════════════════════════════
GATE 3 — SVG vendoring (row 04-01-04)
═══════════════════════════════════════════════════════════════

```bash
echo "=== 04-01-04: 7 vendored SVGs ==="
COUNT=$(ls tools/phosphor-svgs/regular/*.svg 2>/dev/null | wc -l | tr -d " ")
test "$COUNT" = "7" && echo "OK: $COUNT vendored SVGs" || { echo "FAIL: $COUNT (expected 7)"; exit 1; }

for n in x caret-down magnifying-glass check info frame-corners square; do
  test -s "tools/phosphor-svgs/regular/${n}.svg" && echo "OK: ${n}.svg" || { echo "FAIL: ${n}.svg"; exit 1; }
done
```

═══════════════════════════════════════════════════════════════
GATE 4 — Generated builders (rows 04-01-05..09)
═══════════════════════════════════════════════════════════════

```bash
echo "=== 04-01-05: 7 internal/*.kt files ==="
KTCOUNT=$(ls library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt 2>/dev/null | wc -l | tr -d " ")
# Note: if Valkyrie generated AeroIcons.kt and Task 4 moved it out, count is 7. If it left a stray file, fail.
test "$KTCOUNT" = "7" && echo "OK: $KTCOUNT generated files" || { echo "FAIL: $KTCOUNT (expected 7)"; exit 1; }

echo "=== 04-01-06: no viewportWidth=24f anywhere under icons/ ==="
HITS=$(grep -rnE "viewportWidth\s*=\s*24f" library/src/main/kotlin/com/mordred/aero/icons/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS" = "0" && echo "OK: 0 hits of viewportWidth=24f" || { echo "FAIL: $HITS hits"; exit 1; }

echo "=== 04-01-07: viewportWidth=256f >= 7 hits in internal/ ==="
HITS=$(grep -rnE "viewportWidth\s*=\s*256f" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS" -ge 7 && echo "OK: $HITS hits of viewportWidth=256f" || { echo "FAIL: $HITS"; exit 1; }

echo "=== 04-01-08: strokeLineWidth=16f >= 7 hits in internal/ ==="
HITS=$(grep -rnE "strokeLineWidth\s*=\s*16f" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS" -ge 7 && echo "OK: $HITS hits of strokeLineWidth=16f" || { echo "FAIL: $HITS"; exit 1; }

echo "=== 04-01-09: StrokeCap.Round >= 7 hits in internal/ ==="
HITS=$(grep -rnE "StrokeCap\.(Round|Companion\.Round)" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
test "$HITS" -ge 7 && echo "OK: $HITS hits of StrokeCap.Round" || { echo "FAIL: $HITS"; exit 1; }
```

═══════════════════════════════════════════════════════════════
GATE 5 — Facade structure (rows 04-01-10..15)
═══════════════════════════════════════════════════════════════

```bash
F=library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt

echo "=== 04-01-10: AeroIcons.kt exists at the correct package path ==="
test -f "$F" && echo "OK: $F exists"

echo "=== 04-01-11: public object AeroIcons declared ==="
grep -qE "^public object AeroIcons" "$F" && echo "OK"

echo "=== 04-01-12: 7 public val ImageVector properties (Shape B) OR 7 extension properties in internal/ (Shape A) ==="
SHAPE_B_COUNT=$(grep -cE "public val [A-Z][A-Za-z]*: ImageVector" "$F" 2>/dev/null | tr -d " ")
SHAPE_A_COUNT=$(grep -rE "public val AeroIcons\.[A-Z][A-Za-z]*: ImageVector" library/src/main/kotlin/com/mordred/aero/icons/internal/ 2>/dev/null | wc -l | tr -d " ")
echo "Shape B properties in facade: $SHAPE_B_COUNT, Shape A extension properties in internal/: $SHAPE_A_COUNT"
test "$SHAPE_B_COUNT" = "7" -o "$SHAPE_A_COUNT" = "7" && echo "OK: total of 7 typed ImageVector properties" || { echo "FAIL: neither shape produced 7 props"; exit 1; }

echo "=== 04-01-13: KDoc block precedes public object AeroIcons ==="
grep -B2 "^public object AeroIcons" "$F" | grep -qE "\*/" && echo "OK: KDoc closer */ within 2 lines above"

echo "=== 04-01-14: KDoc naming-convention table present ==="
HITS=$(grep -cE "caret-down|magnifying-glass|kebab|PascalCase" "$F")
test "$HITS" -ge 4 && echo "OK: $HITS naming-convention markers" || { echo "FAIL: $HITS"; exit 1; }

echo "=== 04-01-15: KDoc tint warning present ==="
grep -qE "tint|LocalContentColor" "$F" && echo "OK: tint warning"
```

═══════════════════════════════════════════════════════════════
GATE 6 — Compile (row 04-01-16) — the keystone
═══════════════════════════════════════════════════════════════

```bash
echo "=== 04-01-16: ./gradlew :library:compileKotlin passes ==="
./gradlew :library:compileKotlin
test $? -eq 0 && echo "OK: compileKotlin BUILD SUCCESSFUL"
```

If any gate fails: do NOT commit anything. The failure is in a previous task — go back and fix it. Do not paper over with a "fix-up" commit; correct the previous task's commit if necessary.

═══════════════════════════════════════════════════════════════
STEP 7 — Final gate-pass commit (no source changes; just records the green)
═══════════════════════════════════════════════════════════════

This task does not modify source. Skip the commit. The green compile + greens are recorded in this task's verification step in the plan summary.
  </action>
  <verify>
    <automated>./gradlew :library:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - All 6 gates above print "OK" lines and no "FAIL" lines.
    - `./gradlew :library:compileKotlin` exits 0 (BUILD SUCCESSFUL) — VALIDATION row 04-01-16.
    - VALIDATION rows 04-01-01..15 ALL green (confirmed by the gate output above).
    - Phase 4 spike done: ICN-01 (object + lazy + explicitApi) demonstrated on 7 icons; ICN-02 (KDoc) satisfied; ICN-03 demonstrated structurally on 7 icons (full 138 deferred to Plan 04-02).
    - No new commits in this task — verification only.
  </acceptance_criteria>
  <done>Spike is verifiably complete. `:library:compileKotlin` is green. All 16 VALIDATION rows for Plan 04-01 are green. Plan 04-02 (full batch) is unblocked.</done>
</task>

</tasks>

<verification>
After all 6 tasks pass their per-task verifications:

1. **Repository state check:**
   ```bash
   git log --oneline -10
   ```
   Expected sequence (top is most recent):
   - `feat(04): author AeroIcons facade with object-level KDoc and 7 spike properties`
   - `feat(04): generate 7 spike ImageVector builders via Valkyrie 1.1.1`
   - `feat(04): vendor 7 spike Phosphor SVGs (X, CaretDown, MagnifyingGlass, Check, Info, FrameCorners, Square)`
   - `feat(04): pin Phosphor revision and vendor LICENSE + README`
   - `docs(04): correct icon count 139→138 and Phosphor stroke 12→16`
   - (prior commits…)

2. **Compile gate (the single most important check):**
   ```bash
   ./gradlew :library:compileKotlin
   ```
   Must pass. This proves: explicitApi() is satisfied; lazy backing-property pattern compiles; all 7 properties + 7 builder fns resolve.

3. **Structural greps (replay every VALIDATION.md row):**
   - All gates from Task 6 run green.

4. **Open-question status:**
   - Open Q1 (Valkyrie output shape): RESOLVED — recorded in `tools/phosphor-svgs/README.md`.
   - Open Q2 / Q3 (sort-ascending, sort-descending, spinner SVG existence): NOT addressed in 04-01 (these icons are NOT in the spike's 7); deferred to Plan 04-02 Task 1.

5. **What is NOT done (correctly deferred to Plan 04-02):**
   - The remaining 131 SVGs are not vendored.
   - The remaining 131 internal builders are not generated.
   - The facade's full 138-property complement is not present (spike has 7 of 138).
   - No visual rendering check (Phase 6 territory).
   - No three-theme tint check (Phase 6 territory).
</verification>

<success_criteria>
Plan 04-01 succeeds when:

1. **All 9 planning/research docs corrected:** count 139→138 and Phosphor stroke 12→16 propagated atomically. Filename rule corrected (`raw/regular/<name>.svg`, no `-regular` suffix). Verifiable via two grep gates from VALIDATION.md rows 04-01-01 and 04-01-02.

2. **Provenance complete:** `tools/phosphor-svgs/.pin` (40-char SHA + tree URL), `tools/phosphor-svgs/LICENSE` (verbatim Phosphor MIT), `tools/phosphor-svgs/README.md` (Valkyrie command + recovery instructions + Valkyrie's actual output shape).

3. **7 spike SVGs vendored:** `tools/phosphor-svgs/regular/{x,caret-down,magnifying-glass,check,info,frame-corners,square}.svg`, all confirmed `viewBox="0 0 256 256"` + `stroke-width="16"`.

4. **7 spike builders generated:** `library/src/main/kotlin/com/mordred/aero/icons/internal/*.kt` (7 files) with `viewportWidth=256f`, `strokeLineWidth=16f`, `StrokeCap.Round`, `StrokeJoin.Round` (rows 04-01-05..09 green).

5. **Facade authored:** `library/src/main/kotlin/com/mordred/aero/icons/AeroIcons.kt` declares `public object AeroIcons` with full 6-requirement object-level KDoc; either Shape A (extension properties via `internal/*.kt`) or Shape B (7 alphabetized lazy properties); explicitApi-compatible; rows 04-01-10..15 green.

6. **Valkyrie open question resolved:** README documents Shape A vs Shape B based on actual output.

7. **Compile gate green:** `./gradlew :library:compileKotlin` passes (row 04-01-16).

8. **5 commits on master, in order:** doc-correction commit FIRST, then provenance, SVGs, generated builders, facade authoring. Each is a clean atomic step.

9. **Plan 04-02 unblocked:** Spike findings (output shape, file placement, compile green, doc canonical) are ready to scale to the remaining 131 icons.
</success_criteria>

<output>
After completion, create `.planning/phases/04-aeroicons-foundation/04-01-SUMMARY.md` capturing:
- Resolved Valkyrie output shape (A or B) with one representative code snippet
- Pinned Phosphor SHA used
- 5 commits in order with their SHAs
- VALIDATION.md row status (all 16 green)
- Compile time of the green compile
- Any deviations from the plan and their rationale
- Specific notes for Plan 04-02 (questions about `sort-ascending`, `sort-descending`, `spinner` still open — Plan 04-02 Task 1 must verify before bulk-fetching)
</output>
